# .NET build output.
bin/
obj/

# Fable transpilation output (regenerated by `dotnet fable -o output --noCache`).
output/

# Node module install tree (regenerated by `npm ci`).
node_modules/

# Vite bundle the harness serves (regenerated by `npm run build`).
dist/
