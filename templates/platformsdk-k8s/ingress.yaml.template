# Generated by `toolup k8s emit` — do not hand-edit; re-run the command.
# Hosts come from the deploy manifest's `domains` entries, so they are
# re-emitted literally rather than exposed as chart values: a host set is
# a manifest fact, not a per-install knob. Change the manifest, re-emit.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{name}}"
  labels:
    app.kubernetes.io/name: "{{slug}}"
    app.kubernetes.io/instance: "{{slug}}"
    app.kubernetes.io/component: "{{role}}"
    app.kubernetes.io/managed-by: toolup
spec:
{{tls-block}}
  rules:
{{rules-block}}
