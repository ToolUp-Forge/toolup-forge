# Generated by `toolup k8s emit --format helm` — do not hand-edit; re-run
# the command. `version` is the chart's own version and is independent of
# the application image tag (which lives in values.yaml).
apiVersion: v2
name: "{{slug}}"
description: "{{app-name}} — generated from a ToolUp deploy manifest (schema v{{schema-version}})."
type: application
version: 0.1.0
appVersion: "{{app-version}}"
