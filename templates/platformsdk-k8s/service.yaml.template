# Generated by `toolup k8s emit` — do not hand-edit; re-run the command.
# Fronts the {{role}} role. Worker / dispatcher roles mount no HTTP
# pipeline and get no Service.
apiVersion: v1
kind: Service
metadata:
  name: "{{name}}"
  labels:
    app.kubernetes.io/name: "{{slug}}"
    app.kubernetes.io/instance: "{{slug}}"
    app.kubernetes.io/component: "{{role}}"
    app.kubernetes.io/managed-by: toolup
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: "{{slug}}"
    app.kubernetes.io/component: "{{role}}"
  ports:
    - name: http
      port: {{service-port}}
      targetPort: http
      protocol: TCP
