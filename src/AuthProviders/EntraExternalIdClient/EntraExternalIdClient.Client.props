<Project>
  <!-- Phase 11.B follow-up client-only companion cutover (2026-05-08):
       source files live under <Compile> in EntraExternalIdClient.fsproj
       and ship as EntraExternalIdClient.dll referenced via
       <ProjectReference> from the consuming Fable client project. This
       file is retained as an empty marker. -->
  <ItemGroup>
    <!-- (No source items — companion ships as a referenced assembly.) -->
  </ItemGroup>
</Project>
