// SPDX-License-Identifier: Apache-2.0
// Copyright (c) Andrew J. Willshire / ToolUp Analytics Ltd (UK)
//
// <auto-generated>
//   Phase 69k — emitted by ToolUp.Remoting.Generator from the SAME
//   record/union metadata `Write.writeRecord` and `Write.writeUnion`
//   enumerate, so field positions and union tags are the writer's own
//   ordering rather than a second derivation of it.
//
//   DO NOT EDIT. Re-run the generator; a contract test in
//   ToolUp.Platform.Tests holds this file to what the generator emits.
// </auto-generated>

namespace ToolUp.Remoting

open System
open ToolUp.Remoting
open ToolUp.Platform
open ToolUp.Platform.Usage

/// Generated algebra decoders: one per wire type the covered API records
/// carry, registered by `registerAll` (or, verified against the reflection
/// reader first, by `registerAllVerified`). Emitted by ToolUp.Remoting.Generator;
/// do not edit.
[<RequireQualifiedAccess>]
module PlatformDecoders =

    /// Generated decoder for `ColumnMappingTypes.DateOrder` — one combinator per field or case, read off the type's own shape.
    let dateOrder: Decoder<ColumnMappingTypes.DateOrder> =
        Decode.union "ColumnMappingTypes.DateOrder" (function
            | 0 -> Some(Decode.case0 ColumnMappingTypes.DateOrder.DayFirst)
            | 1 -> Some(Decode.case0 ColumnMappingTypes.DateOrder.MonthFirst)
            | 2 -> Some(Decode.case0 ColumnMappingTypes.DateOrder.YearFirst)
            | _ -> None)

    /// Generated decoder for `ColumnMappingTypes.CellTransform` — one combinator per field or case, read off the type's own shape.
    let cellTransform: Decoder<ColumnMappingTypes.CellTransform> =
        Decode.union "ColumnMappingTypes.CellTransform" (function
            | 0 -> Some(Decode.case0 ColumnMappingTypes.CellTransform.Trim)
            | 1 -> Some(Decode.case0 ColumnMappingTypes.CellTransform.StripThousandsSeparators)
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map ColumnMappingTypes.CellTransform.StripCurrency))
            | 3 -> Some(Decode.case0 ColumnMappingTypes.CellTransform.StripPercent)
            | 4 -> Some(Decode.case0 ColumnMappingTypes.CellTransform.DecimalCommaToDot)
            | 5 -> Some(Decode.case0 ColumnMappingTypes.CellTransform.StripLeadingApostrophe)
            | 6 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map ColumnMappingTypes.CellTransform.BlankNullMarkers))
            | 7 -> Some(Decode.case0 ColumnMappingTypes.CellTransform.NormaliseBoolean)
            | 8 -> Some(Decode.payload (dateOrder |> Decode.map ColumnMappingTypes.CellTransform.ParseDateToIso))
            | _ -> None)

    /// Generated decoder for `ColumnMappingTypes.ColumnExpr` — one combinator per field or case, read off the type's own shape.
    let rec columnExpr: Decoder<ColumnMappingTypes.ColumnExpr> =
        fun value ->
            (Decode.union "ColumnMappingTypes.ColumnExpr" (function
                 | 0 -> Some(Decode.payload (Decode.asString |> Decode.map ColumnMappingTypes.ColumnExpr.SourceColumn))
                 | 1 -> Some(Decode.payload (Decode.asString |> Decode.map ColumnMappingTypes.ColumnExpr.Constant))
                 | 2 ->
                     Some(
                         Decode.fields
                             2
                             (Decode.succeed (fun parts separator -> ColumnMappingTypes.ColumnExpr.Concat(parts, separator))
                              |> Decode.apply (Decode.field "parts" 0 (Decode.list columnExpr))
                              |> Decode.apply (Decode.field "separator" 1 Decode.asString))
                     )
                 | 3 ->
                     Some(
                         Decode.fields
                             3
                             (Decode.succeed (fun source delimiter index -> ColumnMappingTypes.ColumnExpr.SplitTake(source, delimiter, index))
                              |> Decode.apply (Decode.field "source" 0 columnExpr)
                              |> Decode.apply (Decode.field "delimiter" 1 Decode.asString)
                              |> Decode.apply (Decode.field "index" 2 Decode.asInt32))
                     )
                 | 4 ->
                     Some(
                         Decode.fields
                             2
                             (Decode.succeed (fun template args -> ColumnMappingTypes.ColumnExpr.Format(template, args))
                              |> Decode.apply (Decode.field "template" 0 Decode.asString)
                              |> Decode.apply (Decode.field "args" 1 (Decode.list columnExpr)))
                     )
                 | 5 ->
                     Some(
                         Decode.fields
                             3
                             (Decode.succeed (fun source start length -> ColumnMappingTypes.ColumnExpr.Substring(source, start, length))
                              |> Decode.apply (Decode.field "source" 0 columnExpr)
                              |> Decode.apply (Decode.field "start" 1 Decode.asInt32)
                              |> Decode.apply (Decode.field "length" 2 Decode.asInt32))
                     )
                 | _ -> None))
                value

    /// Generated decoder for `ColumnMappingTypes.DerivedColumn` — one combinator per field or case, read off the type's own shape.
    let derivedColumn: Decoder<ColumnMappingTypes.DerivedColumn> =
        Decode.succeed (fun field expr -> ({
            Field = field
            Expr = expr
        }: ColumnMappingTypes.DerivedColumn))
        |> Decode.apply (Decode.field "Field" 0 Decode.asString)
        |> Decode.apply (Decode.field "Expr" 1 columnExpr)

    /// Generated decoder for `ColumnMappingTypes.Conversion` — one combinator per field or case, read off the type's own shape.
    let conversion: Decoder<ColumnMappingTypes.Conversion> =
        Decode.succeed (fun fingerprint targetTypeId mapping remediation sourceHeaders derived createdBy createdAt -> ({
            Fingerprint = fingerprint
            TargetTypeId = targetTypeId
            Mapping = mapping
            Remediation = remediation
            SourceHeaders = sourceHeaders
            Derived = derived
            CreatedBy = createdBy
            CreatedAt = createdAt
        }: ColumnMappingTypes.Conversion))
        |> Decode.apply (Decode.field "Fingerprint" 0 Decode.asString)
        |> Decode.apply (Decode.field "TargetTypeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Mapping" 2 (Decode.asMap Decode.asString Decode.asString))
        |> Decode.apply (Decode.field "Remediation" 3 (Decode.asMap Decode.asString (Decode.list cellTransform)))
        |> Decode.apply (Decode.field "SourceHeaders" 4 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Derived" 5 (Decode.list derivedColumn))
        |> Decode.apply (Decode.field "CreatedBy" 6 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 7 Decode.asDateTime)

    /// Generated decoder for `ColumnMappingTypes.ConversionRecord` — one combinator per field or case, read off the type's own shape.
    let conversionRecord: Decoder<ColumnMappingTypes.ConversionRecord> =
        Decode.succeed (fun producedFile sourceFile fingerprint targetTypeId mapping remediationSteps convertedBy convertedAt -> ({
            ProducedFile = producedFile
            SourceFile = sourceFile
            Fingerprint = fingerprint
            TargetTypeId = targetTypeId
            Mapping = mapping
            RemediationSteps = remediationSteps
            ConvertedBy = convertedBy
            ConvertedAt = convertedAt
        }: ColumnMappingTypes.ConversionRecord))
        |> Decode.apply (Decode.field "ProducedFile" 0 Decode.asString)
        |> Decode.apply (Decode.field "SourceFile" 1 Decode.asString)
        |> Decode.apply (Decode.field "Fingerprint" 2 Decode.asString)
        |> Decode.apply (Decode.field "TargetTypeId" 3 Decode.asString)
        |> Decode.apply (Decode.field "Mapping" 4 (Decode.asMap Decode.asString Decode.asString))
        |> Decode.apply (Decode.field "RemediationSteps" 5 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "ConvertedBy" 6 Decode.asString)
        |> Decode.apply (Decode.field "ConvertedAt" 7 Decode.asDateTime)

    /// Generated decoder for `ColumnMappingTypes.DryRunCellIssue` — one combinator per field or case, read off the type's own shape.
    let dryRunCellIssue: Decoder<ColumnMappingTypes.DryRunCellIssue> =
        Decode.succeed (fun row column expected actual violation -> ({
            Row = row
            Column = column
            Expected = expected
            Actual = actual
            Violation = violation
        }: ColumnMappingTypes.DryRunCellIssue))
        |> Decode.apply (Decode.field "Row" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "Column" 1 Decode.asString)
        |> Decode.apply (Decode.field "Expected" 2 Decode.asString)
        |> Decode.apply (Decode.field "Actual" 3 Decode.asString)
        |> Decode.apply (Decode.field "Violation" 4 (Decode.option Decode.asString))

    /// Generated decoder for `ColumnMappingTypes.DryRunRowIssue` — one combinator per field or case, read off the type's own shape.
    let dryRunRowIssue: Decoder<ColumnMappingTypes.DryRunRowIssue> =
        Decode.succeed (fun row detail -> ({
            Row = row
            Detail = detail
        }: ColumnMappingTypes.DryRunRowIssue))
        |> Decode.apply (Decode.field "Row" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "Detail" 1 Decode.asString)

    /// Generated decoder for `ColumnMappingTypes.DryRunReport` — one combinator per field or case, read off the type's own shape.
    let dryRunReport: Decoder<ColumnMappingTypes.DryRunReport> =
        Decode.succeed (fun targetTypeId totalRows passedRows failedRows cellIssues rowIssues truncated commitBlocked -> ({
            TargetTypeId = targetTypeId
            TotalRows = totalRows
            PassedRows = passedRows
            FailedRows = failedRows
            CellIssues = cellIssues
            RowIssues = rowIssues
            Truncated = truncated
            CommitBlocked = commitBlocked
        }: ColumnMappingTypes.DryRunReport))
        |> Decode.apply (Decode.field "TargetTypeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "TotalRows" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "PassedRows" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "FailedRows" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "CellIssues" 4 (Decode.list dryRunCellIssue))
        |> Decode.apply (Decode.field "RowIssues" 5 (Decode.list dryRunRowIssue))
        |> Decode.apply (Decode.field "Truncated" 6 Decode.asBool)
        |> Decode.apply (Decode.field "CommitBlocked" 7 Decode.asBool)

    /// Generated decoder for `DataManagementTypes.UploadedFileInfo` — one combinator per field or case, read off the type's own shape.
    let uploadedFileInfo: Decoder<DataManagementTypes.UploadedFileInfo> =
        Decode.succeed (fun fileName dataType sizeBytes rowCount uploadedAt -> ({
            FileName = fileName
            DataType = dataType
            SizeBytes = sizeBytes
            RowCount = rowCount
            UploadedAt = uploadedAt
        }: DataManagementTypes.UploadedFileInfo))
        |> Decode.apply (Decode.field "FileName" 0 Decode.asString)
        |> Decode.apply (Decode.field "DataType" 1 Decode.asString)
        |> Decode.apply (Decode.field "SizeBytes" 2 Decode.asInt64)
        |> Decode.apply (Decode.field "RowCount" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "UploadedAt" 4 Decode.asDateTime)

    /// Generated decoder for `ProcessedDataTypes.ProcessedData` — one combinator per field or case, read off the type's own shape.
    let processedData: Decoder<ProcessedDataTypes.ProcessedData> =
        Decode.succeed (fun typeName payload -> ({
            TypeName = typeName
            Payload = payload
        }: ProcessedDataTypes.ProcessedData))
        |> Decode.apply (Decode.field "TypeName" 0 Decode.asString)
        |> Decode.apply (Decode.field "Payload" 1 Decode.asString)

    /// Generated decoder for `ProcessedDataTypes.ProcessedFileEntry` — one combinator per field or case, read off the type's own shape.
    let processedFileEntry: Decoder<ProcessedDataTypes.ProcessedFileEntry> =
        Decode.succeed (fun fileName dataType processedAt error summary -> ({
            FileName = fileName
            DataType = dataType
            ProcessedAt = processedAt
            Error = error
            Summary = summary
        }: ProcessedDataTypes.ProcessedFileEntry))
        |> Decode.apply (Decode.field "FileName" 0 Decode.asString)
        |> Decode.apply (Decode.field "DataType" 1 Decode.asString)
        |> Decode.apply (Decode.field "ProcessedAt" 2 Decode.asDateTime)
        |> Decode.apply (Decode.field "Error" 3 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Summary" 4 (Decode.option processedData))

    /// Generated decoder for `ProcessedDataTypes.FileUploadResponse` — one combinator per field or case, read off the type's own shape.
    let fileUploadResponse: Decoder<ProcessedDataTypes.FileUploadResponse> =
        Decode.succeed (fun fileInfo processed -> ({
            FileInfo = fileInfo
            Processed = processed
        }: ProcessedDataTypes.FileUploadResponse))
        |> Decode.apply (Decode.field "FileInfo" 0 uploadedFileInfo)
        |> Decode.apply (Decode.field "Processed" 1 processedFileEntry)

    /// Generated decoder for `DataManagementTypes.FileIngestionStatus` — one combinator per field or case, read off the type's own shape.
    let fileIngestionStatus: Decoder<DataManagementTypes.FileIngestionStatus> =
        Decode.union "DataManagementTypes.FileIngestionStatus" (function
            | 0 -> Some(Decode.case0 DataManagementTypes.FileIngestionStatus.NotIngested)
            | 1 -> Some(Decode.case0 DataManagementTypes.FileIngestionStatus.Pending)
            | 2 -> Some(Decode.case0 DataManagementTypes.FileIngestionStatus.Indexed)
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map DataManagementTypes.FileIngestionStatus.Failed))
            | _ -> None)

    /// Generated decoder for `ProcessedDataTypes.FileListSnapshot` — one combinator per field or case, read off the type's own shape.
    let fileListSnapshot: Decoder<ProcessedDataTypes.FileListSnapshot> =
        Decode.succeed (fun files processed ingestion -> ({
            Files = files
            Processed = processed
            Ingestion = ingestion
        }: ProcessedDataTypes.FileListSnapshot))
        |> Decode.apply (Decode.field "Files" 0 (Decode.list uploadedFileInfo))
        |> Decode.apply (Decode.field "Processed" 1 (Decode.list processedFileEntry))
        |> Decode.apply (Decode.field "Ingestion" 2 (Decode.list (Decode.tuple2 Decode.asString fileIngestionStatus)))

    /// Generated decoder for `DataManagementTypes.DataFileUpload` — one combinator per field or case, read off the type's own shape.
    let dataFileUpload: Decoder<DataManagementTypes.DataFileUpload> =
        Decode.succeed (fun filename contents dataType -> ({
            filename = filename
            contents = contents
            dataType = dataType
        }: DataManagementTypes.DataFileUpload))
        |> Decode.apply (Decode.field "filename" 0 Decode.asString)
        |> Decode.apply (Decode.field "contents" 1 Decode.asString)
        |> Decode.apply (Decode.field "dataType" 2 Decode.asString)

    /// Generated decoder for `ProcessedDataTypes.SessionStoreInfo` — one combinator per field or case, read off the type's own shape.
    let sessionStoreInfo: Decoder<ProcessedDataTypes.SessionStoreInfo> =
        Decode.succeed (fun epoch fileCount -> ({
            Epoch = epoch
            FileCount = fileCount
        }: ProcessedDataTypes.SessionStoreInfo))
        |> Decode.apply (Decode.field "Epoch" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "FileCount" 1 Decode.asInt32)

    /// Generated decoder for `FlagScope` — one combinator per field or case, read off the type's own shape.
    let flagScope: Decoder<FlagScope> =
        Decode.union "FlagScope" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map FlagScope.User))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map FlagScope.Team))
            | 2 -> Some(Decode.case0 FlagScope.Platform)
            | _ -> None)

    /// Generated decoder for `ModuleVisibilityResolution` — one combinator per field or case, read off the type's own shape.
    let moduleVisibilityResolution: Decoder<ModuleVisibilityResolution> =
        Decode.succeed (fun governedModuleIds selectedModuleIds excludedEntryIds contributingScopes -> ({
            GovernedModuleIds = governedModuleIds
            SelectedModuleIds = selectedModuleIds
            ExcludedEntryIds = excludedEntryIds
            ContributingScopes = contributingScopes
        }: ModuleVisibilityResolution))
        |> Decode.apply (Decode.field "GovernedModuleIds" 0 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "SelectedModuleIds" 1 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "ExcludedEntryIds" 2 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "ContributingScopes" 3 (Decode.list flagScope))

    /// Generated decoder for `AccessibleModulesResponse` — one combinator per field or case, read off the type's own shape.
    let accessibleModulesResponse: Decoder<AccessibleModulesResponse> =
        Decode.succeed (fun managed accessible visibilityProfile -> ({
            Managed = managed
            Accessible = accessible
            VisibilityProfile = visibilityProfile
        }: AccessibleModulesResponse))
        |> Decode.apply (Decode.field "Managed" 0 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Accessible" 1 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "VisibilityProfile" 2 (Decode.option moduleVisibilityResolution))

    /// Generated decoder for `DataManagementTypes.ColumnType` — one combinator per field or case, read off the type's own shape.
    let columnType: Decoder<DataManagementTypes.ColumnType> =
        Decode.union "DataManagementTypes.ColumnType" (function
            | 0 -> Some(Decode.case0 DataManagementTypes.ColumnType.StringColumn)
            | 1 -> Some(Decode.case0 DataManagementTypes.ColumnType.NumberColumn)
            | 2 -> Some(Decode.case0 DataManagementTypes.ColumnType.DateColumn)
            | 3 -> Some(Decode.case0 DataManagementTypes.ColumnType.BooleanColumn)
            | _ -> None)

    /// Generated decoder for `DataManagementTypes.DataTypeColumn` — one combinator per field or case, read off the type's own shape.
    let dataTypeColumn: Decoder<DataManagementTypes.DataTypeColumn> =
        Decode.succeed (fun name type' required description -> ({
            Name = name
            Type = type'
            Required = required
            Description = description
        }: DataManagementTypes.DataTypeColumn))
        |> Decode.apply (Decode.field "Name" 0 Decode.asString)
        |> Decode.apply (Decode.field "Type" 1 columnType)
        |> Decode.apply (Decode.field "Required" 2 Decode.asBool)
        |> Decode.apply (Decode.field "Description" 3 (Decode.option Decode.asString))

    /// Generated decoder for `DataManagementTypes.DataTypeSchema` — one combinator per field or case, read off the type's own shape.
    let dataTypeSchema: Decoder<DataManagementTypes.DataTypeSchema> =
        Decode.succeed (fun description columns -> ({
            Description = description
            Columns = columns
        }: DataManagementTypes.DataTypeSchema))
        |> Decode.apply (Decode.field "Description" 0 Decode.asString)
        |> Decode.apply (Decode.field "Columns" 1 (Decode.list dataTypeColumn))

    /// Generated decoder for `DataManagementTypes.DataTypeInfo` — one combinator per field or case, read off the type's own shape.
    let dataTypeInfo: Decoder<DataManagementTypes.DataTypeInfo> =
        Decode.succeed (fun id displayName schema -> ({
            Id = id
            DisplayName = displayName
            Schema = schema
        }: DataManagementTypes.DataTypeInfo))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Schema" 2 (Decode.option dataTypeSchema))

    /// Generated decoder for `DataManagementTypes.DataTypeCatalogEntry` — one combinator per field or case, read off the type's own shape.
    let dataTypeCatalogEntry: Decoder<DataManagementTypes.DataTypeCatalogEntry> =
        Decode.succeed (fun info producers -> ({
            Info = info
            Producers = producers
        }: DataManagementTypes.DataTypeCatalogEntry))
        |> Decode.apply (Decode.field "Info" 0 dataTypeInfo)
        |> Decode.apply (Decode.field "Producers" 1 (Decode.list Decode.asString))

    /// Generated decoder for `DataManagementTypes.DataCatalogResponse` — one combinator per field or case, read off the type's own shape.
    let dataCatalogResponse: Decoder<DataManagementTypes.DataCatalogResponse> =
        Decode.succeed (fun types -> ({
            Types = types
        }: DataManagementTypes.DataCatalogResponse))
        |> Decode.apply (Decode.field "Types" 0 (Decode.list dataTypeCatalogEntry))

    /// Generated decoder for `DataSubjectRequestKind` — one combinator per field or case, read off the type's own shape.
    let dataSubjectRequestKind: Decoder<DataSubjectRequestKind> =
        Decode.union "DataSubjectRequestKind" (function
            | 0 -> Some(Decode.case0 DataSubjectRequestKind.Export)
            | 1 -> Some(Decode.case0 DataSubjectRequestKind.Erase)
            | 2 -> Some(Decode.case0 DataSubjectRequestKind.Rectify)
            | _ -> None)

    /// Generated decoder for `ErasurePolicy` — one combinator per field or case, read off the type's own shape.
    let erasurePolicy: Decoder<ErasurePolicy> =
        Decode.union "ErasurePolicy" (function
            | 0 -> Some(Decode.case0 ErasurePolicy.HardDelete)
            | 1 -> Some(Decode.case0 ErasurePolicy.Tombstone)
            | 2 -> Some(Decode.case0 ErasurePolicy.RetainPerCompliance)
            | _ -> None)

    /// Generated decoder for `DataSubjectRequest` — one combinator per field or case, read off the type's own shape.
    let dataSubjectRequest: Decoder<DataSubjectRequest> =
        Decode.succeed (fun id kind subjectUserId teamId requestedBy requestedAt reason policy -> ({
            Id = id
            Kind = kind
            SubjectUserId = subjectUserId
            TeamId = teamId
            RequestedBy = requestedBy
            RequestedAt = requestedAt
            Reason = reason
            Policy = policy
        }: DataSubjectRequest))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 1 dataSubjectRequestKind)
        |> Decode.apply (Decode.field "SubjectUserId" 2 Decode.asString)
        |> Decode.apply (Decode.field "TeamId" 3 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "RequestedBy" 4 Decode.asString)
        |> Decode.apply (Decode.field "RequestedAt" 5 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "Reason" 6 Decode.asString)
        |> Decode.apply (Decode.field "Policy" 7 erasurePolicy)

    /// Generated decoder for `ErasureSummary` — one combinator per field or case, read off the type's own shape.
    let erasureSummary: Decoder<ErasureSummary> =
        Decode.succeed (fun handlerName recordsAffected note -> ({
            HandlerName = handlerName
            RecordsAffected = recordsAffected
            Note = note
        }: ErasureSummary))
        |> Decode.apply (Decode.field "HandlerName" 0 Decode.asString)
        |> Decode.apply (Decode.field "RecordsAffected" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "Note" 2 (Decode.option Decode.asString))

    /// Generated decoder for `DataSubjectRequestApi.ErasurePreview` — one combinator per field or case, read off the type's own shape.
    let erasurePreview: Decoder<DataSubjectRequestApi.ErasurePreview> =
        Decode.succeed (fun request perHandlerCounts -> ({
            Request = request
            PerHandlerCounts = perHandlerCounts
        }: DataSubjectRequestApi.ErasurePreview))
        |> Decode.apply (Decode.field "Request" 0 dataSubjectRequest)
        |> Decode.apply (Decode.field "PerHandlerCounts" 1 (Decode.asMap Decode.asString erasureSummary))

    /// Generated decoder for `ErasureError` — one combinator per field or case, read off the type's own shape.
    let erasureError: Decoder<ErasureError> =
        Decode.union "ErasureError" (function
            | 0 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun handlerName detail -> ErasureError.StoreUnreachable(handlerName, detail))
                         |> Decode.apply (Decode.field "handlerName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "detail" 1 Decode.asString))
                )
            | 1 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun handlerName reason -> ErasureError.HandlerRefused(handlerName, reason))
                         |> Decode.apply (Decode.field "handlerName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "reason" 1 Decode.asString))
                )
            | 2 ->
                Some(
                    Decode.fields
                        3
                        (Decode.succeed (fun handlerName partial detail -> ErasureError.HandlerPartialFailure(handlerName, partial, detail))
                         |> Decode.apply (Decode.field "handlerName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "partial" 1 erasureSummary)
                         |> Decode.apply (Decode.field "detail" 2 Decode.asString))
                )
            | _ -> None)

    /// Generated decoder for `DataSubjectRequestApi.ErasureRunSummary` — one combinator per field or case, read off the type's own shape.
    let erasureRunSummary: Decoder<DataSubjectRequestApi.ErasureRunSummary> =
        Decode.succeed (fun request startedAt completedAt perHandler overallSuccess -> ({
            Request = request
            StartedAt = startedAt
            CompletedAt = completedAt
            PerHandler = perHandler
            OverallSuccess = overallSuccess
        }: DataSubjectRequestApi.ErasureRunSummary))
        |> Decode.apply (Decode.field "Request" 0 dataSubjectRequest)
        |> Decode.apply (Decode.field "StartedAt" 1 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "CompletedAt" 2 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "PerHandler" 3 (Decode.asMap Decode.asString (Decode.result erasureSummary erasureError)))
        |> Decode.apply (Decode.field "OverallSuccess" 4 Decode.asBool)

    /// Generated decoder for `DataSubjectRequestApi.ErasureRunResult` — one combinator per field or case, read off the type's own shape.
    let erasureRunResult: Decoder<DataSubjectRequestApi.ErasureRunResult> =
        Decode.union "DataSubjectRequestApi.ErasureRunResult" (function
            | 0 -> Some(Decode.payload (erasureRunSummary |> Decode.map DataSubjectRequestApi.ErasureRunResult.Completed))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map DataSubjectRequestApi.ErasureRunResult.NotImplemented))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map DataSubjectRequestApi.ErasureRunResult.Refused))
            | _ -> None)

    /// Generated decoder for `ExportStatus` — one combinator per field or case, read off the type's own shape.
    let exportStatus: Decoder<ExportStatus> =
        Decode.union "ExportStatus" (function
            | 0 -> Some(Decode.case0 ExportStatus.Preparing)
            | 1 -> Some(Decode.payload (Decode.asInt64 |> Decode.map ExportStatus.Ready))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map ExportStatus.Failed))
            | 3 -> Some(Decode.case0 ExportStatus.Cancelled)
            | 4 -> Some(Decode.case0 ExportStatus.Expired)
            | 5 -> Some(Decode.case0 ExportStatus.Unknown)
            | _ -> None)

    /// Generated decoder for `ExportSignature` — one combinator per field or case, read off the type's own shape.
    let exportSignature: Decoder<ExportSignature> =
        Decode.succeed (fun detachedJws signingKeyId signingKeyUrl -> ({
            DetachedJws = detachedJws
            SigningKeyId = signingKeyId
            SigningKeyUrl = signingKeyUrl
        }: ExportSignature))
        |> Decode.apply (Decode.field "DetachedJws" 0 Decode.asString)
        |> Decode.apply (Decode.field "SigningKeyId" 1 Decode.asString)
        |> Decode.apply (Decode.field "SigningKeyUrl" 2 Decode.asString)

    /// Generated decoder for `SignedExportEnvelope` — one combinator per field or case, read off the type's own shape.
    let signedExportEnvelope: Decoder<SignedExportEnvelope> =
        Decode.succeed (fun envelope signature -> ({
            Envelope = envelope
            Signature = signature
        }: SignedExportEnvelope))
        |> Decode.apply (Decode.field "Envelope" 0 Decode.asBytes)
        |> Decode.apply (Decode.field "Signature" 1 exportSignature)

    /// Generated decoder for `AuditEventView` — one combinator per field or case, read off the type's own shape.
    let auditEventView: Decoder<AuditEventView> =
        Decode.succeed (fun id occurredAt eventType actor summary payload -> ({
            Id = id
            OccurredAt = occurredAt
            EventType = eventType
            Actor = actor
            Summary = summary
            Payload = payload
        }: AuditEventView))
        |> Decode.apply (Decode.field "Id" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "OccurredAt" 1 Decode.asDateTime)
        |> Decode.apply (Decode.field "EventType" 2 Decode.asString)
        |> Decode.apply (Decode.field "Actor" 3 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Summary" 4 Decode.asString)
        |> Decode.apply (Decode.field "Payload" 5 Decode.asString)

    /// Generated decoder for `AuditEventPage` — one combinator per field or case, read off the type's own shape.
    let auditEventPage: Decoder<AuditEventPage> =
        Decode.succeed (fun events nextCursor matchedCount -> ({
            Events = events
            NextCursor = nextCursor
            MatchedCount = matchedCount
        }: AuditEventPage))
        |> Decode.apply (Decode.field "Events" 0 (Decode.list auditEventView))
        |> Decode.apply (Decode.field "NextCursor" 1 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "MatchedCount" 2 Decode.asInt32)

    /// Generated decoder for `InspectedComponent` — one combinator per field or case, read off the type's own shape.
    let inspectedComponent: Decoder<InspectedComponent> =
        Decode.succeed (fun id kind label impl -> ({
            Id = id
            Kind = kind
            Label = label
            Impl = impl
        }: InspectedComponent))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 1 Decode.asString)
        |> Decode.apply (Decode.field "Label" 2 Decode.asString)
        |> Decode.apply (Decode.field "Impl" 3 (Decode.option Decode.asString))

    /// Generated decoder for `InspectedKnob` — one combinator per field or case, read off the type's own shape.
    let inspectedKnob: Decoder<InspectedKnob> =
        Decode.succeed (fun name value -> ({
            Name = name
            Value = value
        }: InspectedKnob))
        |> Decode.apply (Decode.field "Name" 0 Decode.asString)
        |> Decode.apply (Decode.field "Value" 1 Decode.asString)

    /// Generated decoder for `InspectedCanonicalMethod` — one combinator per field or case, read off the type's own shape.
    let inspectedCanonicalMethod: Decoder<InspectedCanonicalMethod> =
        Decode.succeed (fun metricId selector -> ({
            MetricId = metricId
            Selector = selector
        }: InspectedCanonicalMethod))
        |> Decode.apply (Decode.field "MetricId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Selector" 1 Decode.asString)

    /// Generated decoder for `CompositionView` — one combinator per field or case, read off the type's own shape.
    let compositionView: Decoder<CompositionView> =
        Decode.succeed (fun schemaVersion modules companionSlots dataTypes tools metrics subjects purposes configKnobs canonicalMethods -> ({
            SchemaVersion = schemaVersion
            Modules = modules
            CompanionSlots = companionSlots
            DataTypes = dataTypes
            Tools = tools
            Metrics = metrics
            Subjects = subjects
            Purposes = purposes
            ConfigKnobs = configKnobs
            CanonicalMethods = canonicalMethods
        }: CompositionView))
        |> Decode.apply (Decode.field "SchemaVersion" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "Modules" 1 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "CompanionSlots" 2 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "DataTypes" 3 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "Tools" 4 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "Metrics" 5 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "Subjects" 6 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "Purposes" 7 (Decode.list inspectedComponent))
        |> Decode.apply (Decode.field "ConfigKnobs" 8 (Decode.list inspectedKnob))
        |> Decode.apply (Decode.field "CanonicalMethods" 9 (Decode.list inspectedCanonicalMethod))

    /// Generated decoder for `InspectedSurface` — one combinator per field or case, read off the type's own shape.
    let inspectedSurface: Decoder<InspectedSurface> =
        Decode.succeed (fun family subject json -> ({
            Family = family
            Subject = subject
            Json = json
        }: InspectedSurface))
        |> Decode.apply (Decode.field "Family" 0 Decode.asString)
        |> Decode.apply (Decode.field "Subject" 1 Decode.asString)
        |> Decode.apply (Decode.field "Json" 2 Decode.asString)

    /// Generated decoder for `SurfacesView` — one combinator per field or case, read off the type's own shape.
    let surfacesView: Decoder<SurfacesView> =
        Decode.succeed (fun descriptors note -> ({
            Descriptors = descriptors
            Note = note
        }: SurfacesView))
        |> Decode.apply (Decode.field "Descriptors" 0 (Decode.list inspectedSurface))
        |> Decode.apply (Decode.field "Note" 1 (Decode.option Decode.asString))

    /// Generated decoder for `InspectedRule` — one combinator per field or case, read off the type's own shape.
    let inspectedRule: Decoder<InspectedRule> =
        Decode.succeed (fun code severity ruleClass description -> ({
            Code = code
            Severity = severity
            RuleClass = ruleClass
            Description = description
        }: InspectedRule))
        |> Decode.apply (Decode.field "Code" 0 Decode.asString)
        |> Decode.apply (Decode.field "Severity" 1 Decode.asString)
        |> Decode.apply (Decode.field "RuleClass" 2 Decode.asString)
        |> Decode.apply (Decode.field "Description" 3 Decode.asString)

    /// Generated decoder for `InspectedDefect` — one combinator per field or case, read off the type's own shape.
    let inspectedDefect: Decoder<InspectedDefect> =
        Decode.succeed (fun code severity message -> ({
            Code = code
            Severity = severity
            Message = message
        }: InspectedDefect))
        |> Decode.apply (Decode.field "Code" 0 Decode.asString)
        |> Decode.apply (Decode.field "Severity" 1 Decode.asString)
        |> Decode.apply (Decode.field "Message" 2 Decode.asString)

    /// Generated decoder for `RulesView` — one combinator per field or case, read off the type's own shape.
    let rulesView: Decoder<RulesView> =
        Decode.succeed (fun rules defects capturedAtUtc -> ({
            Rules = rules
            Defects = defects
            CapturedAtUtc = capturedAtUtc
        }: RulesView))
        |> Decode.apply (Decode.field "Rules" 0 (Decode.list inspectedRule))
        |> Decode.apply (Decode.field "Defects" 1 (Decode.list inspectedDefect))
        |> Decode.apply (Decode.field "CapturedAtUtc" 2 Decode.asDateTime)

    /// Generated decoder for `InspectedDeclaration` — one combinator per field or case, read off the type's own shape.
    let inspectedDeclaration: Decoder<InspectedDeclaration> =
        Decode.succeed (fun facet subject value -> ({
            Facet = facet
            Subject = subject
            Value = value
        }: InspectedDeclaration))
        |> Decode.apply (Decode.field "Facet" 0 Decode.asString)
        |> Decode.apply (Decode.field "Subject" 1 Decode.asString)
        |> Decode.apply (Decode.field "Value" 2 Decode.asString)

    /// Generated decoder for `DisclosureView` — one combinator per field or case, read off the type's own shape.
    let disclosureView: Decoder<DisclosureView> =
        Decode.succeed (fun schemaVersion declarations digest -> ({
            SchemaVersion = schemaVersion
            Declarations = declarations
            Digest = digest
        }: DisclosureView))
        |> Decode.apply (Decode.field "SchemaVersion" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "Declarations" 1 (Decode.list inspectedDeclaration))
        |> Decode.apply (Decode.field "Digest" 2 Decode.asString)

    /// Generated decoder for `ProvenanceView` — one combinator per field or case, read off the type's own shape.
    let provenanceView: Decoder<ProvenanceView> =
        Decode.succeed (fun graphComposed factEvidenceComposed artifactProvenanceComposed -> ({
            GraphComposed = graphComposed
            FactEvidenceComposed = factEvidenceComposed
            ArtifactProvenanceComposed = artifactProvenanceComposed
        }: ProvenanceView))
        |> Decode.apply (Decode.field "GraphComposed" 0 Decode.asBool)
        |> Decode.apply (Decode.field "FactEvidenceComposed" 1 Decode.asBool)
        |> Decode.apply (Decode.field "ArtifactProvenanceComposed" 2 Decode.asBool)

    /// Generated decoder for `ConfigFieldKind` — one combinator per field or case, read off the type's own shape.
    let configFieldKind: Decoder<ConfigFieldKind> =
        Decode.union "ConfigFieldKind" (function
            | 0 -> Some(Decode.case0 ConfigFieldKind.Bool)
            | 1 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun min max -> ConfigFieldKind.Int(min, max))
                         |> Decode.apply (Decode.field "min" 0 (Decode.option Decode.asInt32))
                         |> Decode.apply (Decode.field "max" 1 (Decode.option Decode.asInt32)))
                )
            | 2 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun min max -> ConfigFieldKind.Float(min, max))
                         |> Decode.apply (Decode.field "min" 0 (Decode.option Decode.asFloat))
                         |> Decode.apply (Decode.field "max" 1 (Decode.option Decode.asFloat)))
                )
            | 3 -> Some(Decode.payload ((Decode.option Decode.asInt32) |> Decode.map ConfigFieldKind.String))
            | 4 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map ConfigFieldKind.Choice))
            | _ -> None)

    /// Generated decoder for `ConfigFieldSchema` — one combinator per field or case, read off the type's own shape.
    let configFieldSchema: Decoder<ConfigFieldSchema> =
        Decode.succeed (fun key displayName description kind required defaultJson -> ({
            Key = key
            DisplayName = displayName
            Description = description
            Kind = kind
            Required = required
            DefaultJson = defaultJson
        }: ConfigFieldSchema))
        |> Decode.apply (Decode.field "Key" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Description" 2 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Kind" 3 configFieldKind)
        |> Decode.apply (Decode.field "Required" 4 Decode.asBool)
        |> Decode.apply (Decode.field "DefaultJson" 5 Decode.asString)

    /// Generated decoder for `ModuleConfigSchema` — one combinator per field or case, read off the type's own shape.
    let moduleConfigSchema: Decoder<ModuleConfigSchema> =
        Decode.succeed (fun fields schemaVersion -> ({
            Fields = fields
            SchemaVersion = schemaVersion
        }: ModuleConfigSchema))
        |> Decode.apply (Decode.field "Fields" 0 (Decode.list configFieldSchema))
        |> Decode.apply (Decode.field "SchemaVersion" 1 Decode.asInt32)

    /// Generated decoder for `ModuleConfigEntry` — one combinator per field or case, read off the type's own shape.
    let moduleConfigEntry: Decoder<ModuleConfigEntry> =
        Decode.succeed (fun moduleKey displayName schema -> ({
            ModuleKey = moduleKey
            DisplayName = displayName
            Schema = schema
        }: ModuleConfigEntry))
        |> Decode.apply (Decode.field "ModuleKey" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Schema" 2 moduleConfigSchema)

    /// Generated decoder for `ModuleConfigView` — one combinator per field or case, read off the type's own shape.
    let moduleConfigView: Decoder<ModuleConfigView> =
        Decode.succeed (fun moduleKey schema values -> ({
            ModuleKey = moduleKey
            Schema = schema
            Values = values
        }: ModuleConfigView))
        |> Decode.apply (Decode.field "ModuleKey" 0 Decode.asString)
        |> Decode.apply (Decode.field "Schema" 1 moduleConfigSchema)
        |> Decode.apply (Decode.field "Values" 2 (Decode.asMap Decode.asString Decode.asString))

    /// Generated decoder for `DataSourceConfig` — one combinator per field or case, read off the type's own shape.
    let dataSourceConfig: Decoder<DataSourceConfig> =
        Decode.succeed (fun id name kind connectionScope credentialKey tables tags -> ({
            Id = id
            Name = name
            Kind = kind
            ConnectionScope = connectionScope
            CredentialKey = credentialKey
            Tables = tables
            Tags = tags
        }: DataSourceConfig))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Name" 1 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 2 Decode.asString)
        |> Decode.apply (Decode.field "ConnectionScope" 3 (Decode.asMap Decode.asString Decode.asString))
        |> Decode.apply (Decode.field "CredentialKey" 4 Decode.asString)
        |> Decode.apply (Decode.field "Tables" 5 (Decode.option (Decode.list Decode.asString)))
        |> Decode.apply (Decode.field "Tags" 6 (Decode.asMap Decode.asString Decode.asString))

    /// Generated decoder for `IngestionStatus` — one combinator per field or case, read off the type's own shape.
    let ingestionStatus: Decoder<IngestionStatus> =
        Decode.union "IngestionStatus" (function
            | 0 -> Some(Decode.case0 IngestionStatus.Pending)
            | 1 -> Some(Decode.case0 IngestionStatus.Running)
            | 2 -> Some(Decode.case0 IngestionStatus.Succeeded)
            | 3 -> Some(Decode.case0 IngestionStatus.Failed)
            | _ -> None)

    /// Generated decoder for `IngestionError` — one combinator per field or case, read off the type's own shape.
    let ingestionError: Decoder<IngestionError> =
        Decode.union "IngestionError" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map IngestionError.CredentialMissing))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map IngestionError.SourceUnreachable))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map IngestionError.SchemaMismatch))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map IngestionError.StorageFailure))
            | 4 -> Some(Decode.payload (Decode.asString |> Decode.map IngestionError.UnexpectedFailure))
            | _ -> None)

    /// Generated decoder for `IngestionRun` — one combinator per field or case, read off the type's own shape.
    let ingestionRun: Decoder<IngestionRun> =
        Decode.succeed (fun runId dataSourceId scopeId table startedAt completedAt status rowsIngested resultObjectId error jobId -> ({
            RunId = runId
            DataSourceId = dataSourceId
            ScopeId = scopeId
            Table = table
            StartedAt = startedAt
            CompletedAt = completedAt
            Status = status
            RowsIngested = rowsIngested
            ResultObjectId = resultObjectId
            Error = error
            JobId = jobId
        }: IngestionRun))
        |> Decode.apply (Decode.field "RunId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "DataSourceId" 1 Decode.asString)
        |> Decode.apply (Decode.field "ScopeId" 2 Decode.asString)
        |> Decode.apply (Decode.field "Table" 3 Decode.asString)
        |> Decode.apply (Decode.field "StartedAt" 4 Decode.asDateTime)
        |> Decode.apply (Decode.field "CompletedAt" 5 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "Status" 6 ingestionStatus)
        |> Decode.apply (Decode.field "RowsIngested" 7 (Decode.option Decode.asInt64))
        |> Decode.apply (Decode.field "ResultObjectId" 8 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Error" 9 (Decode.option ingestionError))
        |> Decode.apply (Decode.field "JobId" 10 (Decode.option Decode.asGuid))

    /// Generated decoder for `CredentialStatus` — one combinator per field or case, read off the type's own shape.
    let credentialStatus: Decoder<CredentialStatus> =
        Decode.union "CredentialStatus" (function
            | 0 -> Some(Decode.case0 CredentialStatus.NotConfigured)
            | 1 -> Some(Decode.case0 CredentialStatus.NeedsAuthorization)
            | 2 -> Some(Decode.payload (Decode.asDateTime |> Decode.map CredentialStatus.Connected))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map CredentialStatus.NeedsReauthorization))
            | _ -> None)

    /// Generated decoder for `OAuthRefreshOutcome` — one combinator per field or case, read off the type's own shape.
    let oAuthRefreshOutcome: Decoder<OAuthRefreshOutcome> =
        Decode.union "OAuthRefreshOutcome" (function
            | 0 -> Some(Decode.payload (Decode.asDateTime |> Decode.map OAuthRefreshOutcome.RefreshedOutcome))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map OAuthRefreshOutcome.TransientErrorOutcome))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map OAuthRefreshOutcome.RequiresReauthOutcome))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map OAuthRefreshOutcome.DeadLetteredOutcome))
            | _ -> None)

    /// Generated decoder for `TokenStatus` — one combinator per field or case, read off the type's own shape.
    let tokenStatus: Decoder<TokenStatus> =
        Decode.succeed (fun provider lastOutcome lastOutcomeAt nextScheduledRefresh -> ({
            Provider = provider
            LastOutcome = lastOutcome
            LastOutcomeAt = lastOutcomeAt
            NextScheduledRefresh = nextScheduledRefresh
        }: TokenStatus))
        |> Decode.apply (Decode.field "Provider" 0 Decode.asString)
        |> Decode.apply (Decode.field "LastOutcome" 1 (Decode.option oAuthRefreshOutcome))
        |> Decode.apply (Decode.field "LastOutcomeAt" 2 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "NextScheduledRefresh" 3 (Decode.option Decode.asDateTime))

    /// Generated decoder for `MigrationDataTypeInfo` — one combinator per field or case, read off the type's own shape.
    let migrationDataTypeInfo: Decoder<MigrationDataTypeInfo> =
        Decode.succeed (fun dataTypeId displayName currentVersion chainProblem -> ({
            DataTypeId = dataTypeId
            DisplayName = displayName
            CurrentVersion = currentVersion
            ChainProblem = chainProblem
        }: MigrationDataTypeInfo))
        |> Decode.apply (Decode.field "DataTypeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "CurrentVersion" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "ChainProblem" 3 (Decode.option Decode.asString))

    /// Generated decoder for `MigrationRunState` — one combinator per field or case, read off the type's own shape.
    let migrationRunState: Decoder<MigrationRunState> =
        Decode.union "MigrationRunState" (function
            | 0 -> Some(Decode.case0 MigrationRunState.MigrationIdle)
            | 1 -> Some(Decode.case0 MigrationRunState.MigrationInProgress)
            | 2 -> Some(Decode.case0 MigrationRunState.MigrationComplete)
            | 3 -> Some(Decode.case0 MigrationRunState.MigrationCompleteWithFailures)
            | 4 -> Some(Decode.payload (Decode.asString |> Decode.map MigrationRunState.MigrationChainBlocked))
            | _ -> None)

    /// Generated decoder for `MigrationFailure` — one combinator per field or case, read off the type's own shape.
    let migrationFailure: Decoder<MigrationFailure> =
        Decode.succeed (fun objectId atVersion error occurredAt -> ({
            ObjectId = objectId
            AtVersion = atVersion
            Error = error
            OccurredAt = occurredAt
        }: MigrationFailure))
        |> Decode.apply (Decode.field "ObjectId" 0 Decode.asString)
        |> Decode.apply (Decode.field "AtVersion" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "Error" 2 Decode.asString)
        |> Decode.apply (Decode.field "OccurredAt" 3 Decode.asDateTime)

    /// Generated decoder for `MigrationStatus` — one combinator per field or case, read off the type's own shape.
    let migrationStatus: Decoder<MigrationStatus> =
        Decode.succeed (fun teamId dataTypeId targetVersion totalObjects migratedObjects alreadyCurrentObjects failedObjects state startedAt completedAt failures -> ({
            TeamId = teamId
            DataTypeId = dataTypeId
            TargetVersion = targetVersion
            TotalObjects = totalObjects
            MigratedObjects = migratedObjects
            AlreadyCurrentObjects = alreadyCurrentObjects
            FailedObjects = failedObjects
            State = state
            StartedAt = startedAt
            CompletedAt = completedAt
            Failures = failures
        }: MigrationStatus))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DataTypeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "TargetVersion" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "TotalObjects" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "MigratedObjects" 4 Decode.asInt32)
        |> Decode.apply (Decode.field "AlreadyCurrentObjects" 5 Decode.asInt32)
        |> Decode.apply (Decode.field "FailedObjects" 6 Decode.asInt32)
        |> Decode.apply (Decode.field "State" 7 migrationRunState)
        |> Decode.apply (Decode.field "StartedAt" 8 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "CompletedAt" 9 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "Failures" 10 (Decode.list migrationFailure))

    /// Generated decoder for `ReadinessVerdict` — one combinator per field or case, read off the type's own shape.
    let readinessVerdict: Decoder<ReadinessVerdict> =
        Decode.union "ReadinessVerdict" (function
            | 0 -> Some(Decode.case0 ReadinessVerdict.Ready)
            | 1 -> Some(Decode.case0 ReadinessVerdict.DegradedReady)
            | 2 -> Some(Decode.case0 ReadinessVerdict.NotReady)
            | _ -> None)

    /// Generated decoder for `SourceStatus` — one combinator per field or case, read off the type's own shape.
    let sourceStatus: Decoder<SourceStatus> =
        Decode.union "SourceStatus" (function
            | 0 -> Some(Decode.case0 SourceStatus.NotComposed)
            | 1 -> Some(Decode.case0 SourceStatus.Clean)
            | 2 -> Some(Decode.case0 SourceStatus.Warned)
            | 3 -> Some(Decode.case0 SourceStatus.Failed)
            | _ -> None)

    /// Generated decoder for `DeploymentReadiness.PreflightSummary` — one combinator per field or case, read off the type's own shape.
    let preflightSummary: Decoder<DeploymentReadiness.PreflightSummary> =
        Decode.succeed (fun status total errors warnings -> ({
            Status = status
            Total = total
            Errors = errors
            Warnings = warnings
        }: DeploymentReadiness.PreflightSummary))
        |> Decode.apply (Decode.field "Status" 0 sourceStatus)
        |> Decode.apply (Decode.field "Total" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "Errors" 2 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Warnings" 3 (Decode.list Decode.asString))

    /// Generated decoder for `DeploymentReadiness.SmokeSummary` — one combinator per field or case, read off the type's own shape.
    let smokeSummary: Decoder<DeploymentReadiness.SmokeSummary> =
        Decode.succeed (fun status total failures -> ({
            Status = status
            Total = total
            Failures = failures
        }: DeploymentReadiness.SmokeSummary))
        |> Decode.apply (Decode.field "Status" 0 sourceStatus)
        |> Decode.apply (Decode.field "Total" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "Failures" 2 (Decode.list Decode.asString))

    /// Generated decoder for `DeploymentReadiness.DriftSummary` — one combinator per field or case, read off the type's own shape.
    let driftSummary: Decoder<DeploymentReadiness.DriftSummary> =
        Decode.succeed (fun status driftEventCount -> ({
            Status = status
            DriftEventCount = driftEventCount
        }: DeploymentReadiness.DriftSummary))
        |> Decode.apply (Decode.field "Status" 0 sourceStatus)
        |> Decode.apply (Decode.field "DriftEventCount" 1 Decode.asInt32)

    /// Generated decoder for `DeploymentReadiness.HealthSummary` — one combinator per field or case, read off the type's own shape.
    let healthSummary: Decoder<DeploymentReadiness.HealthSummary> =
        Decode.succeed (fun status total unhealthy degraded -> ({
            Status = status
            Total = total
            Unhealthy = unhealthy
            Degraded = degraded
        }: DeploymentReadiness.HealthSummary))
        |> Decode.apply (Decode.field "Status" 0 sourceStatus)
        |> Decode.apply (Decode.field "Total" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "Unhealthy" 2 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Degraded" 3 (Decode.list Decode.asString))

    /// Generated decoder for `DeploymentReadiness.DeploymentReadinessReport` — one combinator per field or case, read off the type's own shape.
    let deploymentReadinessReport: Decoder<DeploymentReadiness.DeploymentReadinessReport> =
        Decode.succeed (fun verdict preflight smokeTests drift health generatedAt -> ({
            Verdict = verdict
            Preflight = preflight
            SmokeTests = smokeTests
            Drift = drift
            Health = health
            GeneratedAt = generatedAt
        }: DeploymentReadiness.DeploymentReadinessReport))
        |> Decode.apply (Decode.field "Verdict" 0 readinessVerdict)
        |> Decode.apply (Decode.field "Preflight" 1 preflightSummary)
        |> Decode.apply (Decode.field "SmokeTests" 2 smokeSummary)
        |> Decode.apply (Decode.field "Drift" 3 driftSummary)
        |> Decode.apply (Decode.field "Health" 4 healthSummary)
        |> Decode.apply (Decode.field "GeneratedAt" 5 Decode.asDateTime)

    /// Generated decoder for `VerificationSectionVerdict` — one combinator per field or case, read off the type's own shape.
    let verificationSectionVerdict: Decoder<VerificationSectionVerdict> =
        Decode.union "VerificationSectionVerdict" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map VerificationSectionVerdict.NotComposed))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map VerificationSectionVerdict.Verified))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map VerificationSectionVerdict.Observed))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map VerificationSectionVerdict.Failed))
            | 4 -> Some(Decode.payload (Decode.asString |> Decode.map VerificationSectionVerdict.Unreadable))
            | _ -> None)

    /// Generated decoder for `DeploymentVerification.ReportSection` — one combinator per field or case, read off the type's own shape.
    let reportSection: Decoder<DeploymentVerification.ReportSection> =
        Decode.succeed (fun id title verdict findings -> ({
            Id = id
            Title = title
            Verdict = verdict
            Findings = findings
        }: DeploymentVerification.ReportSection))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Title" 1 Decode.asString)
        |> Decode.apply (Decode.field "Verdict" 2 verificationSectionVerdict)
        |> Decode.apply (Decode.field "Findings" 3 (Decode.list Decode.asString))

    /// Generated decoder for `DeploymentVerification.NotProvedStatement` — one combinator per field or case, read off the type's own shape.
    let notProvedStatement: Decoder<DeploymentVerification.NotProvedStatement> =
        Decode.succeed (fun id statement narrowing -> ({
            Id = id
            Statement = statement
            Narrowing = narrowing
        }: DeploymentVerification.NotProvedStatement))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Statement" 1 Decode.asString)
        |> Decode.apply (Decode.field "Narrowing" 2 (Decode.option Decode.asString))

    /// Generated decoder for `DeploymentVerificationOutcome` — one combinator per field or case, read off the type's own shape.
    let deploymentVerificationOutcome: Decoder<DeploymentVerificationOutcome> =
        Decode.union "DeploymentVerificationOutcome" (function
            | 0 -> Some(Decode.case0 DeploymentVerificationOutcome.NothingComposed)
            | 1 -> Some(Decode.case0 DeploymentVerificationOutcome.AllComposedVerified)
            | 2 -> Some(Decode.case0 DeploymentVerificationOutcome.PartiallyVerified)
            | 3 -> Some(Decode.case0 DeploymentVerificationOutcome.FailuresPresent)
            | _ -> None)

    /// Generated decoder for `DeploymentVerification.DeploymentVerificationReport` — one combinator per field or case, read off the type's own shape.
    let deploymentVerificationReport: Decoder<DeploymentVerification.DeploymentVerificationReport> =
        Decode.succeed (fun schemaVersion actor generatedAt sections notProved outcome verdictDigest -> ({
            SchemaVersion = schemaVersion
            Actor = actor
            GeneratedAt = generatedAt
            Sections = sections
            NotProved = notProved
            Outcome = outcome
            VerdictDigest = verdictDigest
        }: DeploymentVerification.DeploymentVerificationReport))
        |> Decode.apply (Decode.field "SchemaVersion" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "Actor" 1 Decode.asString)
        |> Decode.apply (Decode.field "GeneratedAt" 2 Decode.asDateTime)
        |> Decode.apply (Decode.field "Sections" 3 (Decode.list reportSection))
        |> Decode.apply (Decode.field "NotProved" 4 (Decode.list notProvedStatement))
        |> Decode.apply (Decode.field "Outcome" 5 deploymentVerificationOutcome)
        |> Decode.apply (Decode.field "VerdictDigest" 6 Decode.asString)

    /// Generated decoder for `ContactOwner` — one combinator per field or case, read off the type's own shape.
    let contactOwner: Decoder<ContactOwner> =
        Decode.union "ContactOwner" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map ContactOwner.User))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map ContactOwner.Team))
            | _ -> None)

    /// Generated decoder for `NotificationKind.PushVariant` — one combinator per field or case, read off the type's own shape.
    let pushVariant: Decoder<NotificationKind.PushVariant> =
        Decode.union "NotificationKind.PushVariant" (function
            | 0 -> Some(Decode.case0 NotificationKind.PushVariant.WebPush)
            | 1 -> Some(Decode.case0 NotificationKind.PushVariant.Fcm)
            | 2 -> Some(Decode.case0 NotificationKind.PushVariant.Apns)
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map NotificationKind.PushVariant.Other))
            | _ -> None)

    /// Generated decoder for `NotificationKind.SinkKind` — one combinator per field or case, read off the type's own shape.
    let sinkKind: Decoder<NotificationKind.SinkKind> =
        Decode.union "NotificationKind.SinkKind" (function
            | 0 -> Some(Decode.case0 NotificationKind.SinkKind.Email)
            | 1 -> Some(Decode.case0 NotificationKind.SinkKind.Sms)
            | 2 -> Some(Decode.payload (pushVariant |> Decode.map NotificationKind.SinkKind.Push))
            | 3 -> Some(Decode.case0 NotificationKind.SinkKind.WhatsApp)
            | _ -> None)

    /// Generated decoder for `OptInRecord` — one combinator per field or case, read off the type's own shape.
    let optInRecord: Decoder<OptInRecord> =
        Decode.succeed (fun grantedAt source expiresAt -> ({
            GrantedAt = grantedAt
            Source = source
            ExpiresAt = expiresAt
        }: OptInRecord))
        |> Decode.apply (Decode.field "GrantedAt" 0 Decode.asDateTime)
        |> Decode.apply (Decode.field "Source" 1 Decode.asString)
        |> Decode.apply (Decode.field "ExpiresAt" 2 (Decode.option Decode.asDateTime))

    /// Generated decoder for `ExternalContact` — one combinator per field or case, read off the type's own shape.
    let externalContact: Decoder<ExternalContact> =
        Decode.succeed (fun id type' version displayName optionalEmailAddress optionalPhoneNumber optionalWhatsAppNumber owner optIns tags createdAt lastInboundUtc notes -> ({
            Id = id
            Type = type'
            Version = version
            DisplayName = displayName
            OptionalEmailAddress = optionalEmailAddress
            OptionalPhoneNumber = optionalPhoneNumber
            OptionalWhatsAppNumber = optionalWhatsAppNumber
            Owner = owner
            OptIns = optIns
            Tags = tags
            CreatedAt = createdAt
            LastInboundUtc = lastInboundUtc
            Notes = notes
        }: ExternalContact))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Type" 1 Decode.asString)
        |> Decode.apply (Decode.field "Version" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "DisplayName" 3 Decode.asString)
        |> Decode.apply (Decode.field "OptionalEmailAddress" 4 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "OptionalPhoneNumber" 5 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "OptionalWhatsAppNumber" 6 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Owner" 7 contactOwner)
        |> Decode.apply (Decode.field "OptIns" 8 (Decode.asMap sinkKind optInRecord))
        |> Decode.apply (Decode.field "Tags" 9 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "CreatedAt" 10 Decode.asDateTime)
        |> Decode.apply (Decode.field "LastInboundUtc" 11 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "Notes" 12 (Decode.option Decode.asString))

    /// Generated decoder for `FlagValue` — one combinator per field or case, read off the type's own shape.
    let flagValue: Decoder<FlagValue> =
        Decode.union "FlagValue" (function
            | 0 -> Some(Decode.payload (Decode.asBool |> Decode.map FlagValue.Bool))
            | 1 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun options value -> FlagValue.Variant(options, value))
                         |> Decode.apply (Decode.field "options" 0 (Decode.list Decode.asString))
                         |> Decode.apply (Decode.field "value" 1 Decode.asString))
                )
            | _ -> None)

    /// Generated decoder for `FeatureFlag` — one combinator per field or case, read off the type's own shape.
    let featureFlag: Decoder<FeatureFlag> =
        Decode.succeed (fun key defaultValue description owner -> ({
            Key = key
            DefaultValue = defaultValue
            Description = description
            Owner = owner
        }: FeatureFlag))
        |> Decode.apply (Decode.field "Key" 0 Decode.asString)
        |> Decode.apply (Decode.field "DefaultValue" 1 flagValue)
        |> Decode.apply (Decode.field "Description" 2 Decode.asString)
        |> Decode.apply (Decode.field "Owner" 3 (Decode.option Decode.asString))

    /// Generated decoder for `HealthProbeView` — one combinator per field or case, read off the type's own shape.
    let healthProbeView: Decoder<HealthProbeView> =
        Decode.succeed (fun name kind timeoutMs status message elapsedMs -> ({
            Name = name
            Kind = kind
            TimeoutMs = timeoutMs
            Status = status
            Message = message
            ElapsedMs = elapsedMs
        }: HealthProbeView))
        |> Decode.apply (Decode.field "Name" 0 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 1 Decode.asString)
        |> Decode.apply (Decode.field "TimeoutMs" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "Status" 3 Decode.asString)
        |> Decode.apply (Decode.field "Message" 4 Decode.asString)
        |> Decode.apply (Decode.field "ElapsedMs" 5 Decode.asInt64)

    /// Generated decoder for `HealthSnapshot` — one combinator per field or case, read off the type's own shape.
    let healthSnapshot: Decoder<HealthSnapshot> =
        Decode.succeed (fun generatedAt probes -> ({
            GeneratedAt = generatedAt
            Probes = probes
        }: HealthSnapshot))
        |> Decode.apply (Decode.field "GeneratedAt" 0 Decode.asDateTime)
        |> Decode.apply (Decode.field "Probes" 1 (Decode.list healthProbeView))

    /// Generated decoder for `PreflightOutcomeView` — one combinator per field or case, read off the type's own shape.
    let preflightOutcomeView: Decoder<PreflightOutcomeView> =
        Decode.succeed (fun name status message elapsedMs -> ({
            Name = name
            Status = status
            Message = message
            ElapsedMs = elapsedMs
        }: PreflightOutcomeView))
        |> Decode.apply (Decode.field "Name" 0 Decode.asString)
        |> Decode.apply (Decode.field "Status" 1 Decode.asString)
        |> Decode.apply (Decode.field "Message" 2 Decode.asString)
        |> Decode.apply (Decode.field "ElapsedMs" 3 Decode.asInt64)

    /// Generated decoder for `PreflightSnapshotView` — one combinator per field or case, read off the type's own shape.
    let preflightSnapshotView: Decoder<PreflightSnapshotView> =
        Decode.succeed (fun hasSnapshot outcomes -> ({
            HasSnapshot = hasSnapshot
            Outcomes = outcomes
        }: PreflightSnapshotView))
        |> Decode.apply (Decode.field "HasSnapshot" 0 Decode.asBool)
        |> Decode.apply (Decode.field "Outcomes" 1 (Decode.list preflightOutcomeView))

    /// Generated decoder for `JobSchedulerTelemetryView` — one combinator per field or case, read off the type's own shape.
    let jobSchedulerTelemetryView: Decoder<JobSchedulerTelemetryView> =
        Decode.succeed (fun hasScheduler tickMissedCount60Min lastDriftMs lastTickMissedAt generatedAt -> ({
            HasScheduler = hasScheduler
            TickMissedCount60Min = tickMissedCount60Min
            LastDriftMs = lastDriftMs
            LastTickMissedAt = lastTickMissedAt
            GeneratedAt = generatedAt
        }: JobSchedulerTelemetryView))
        |> Decode.apply (Decode.field "HasScheduler" 0 Decode.asBool)
        |> Decode.apply (Decode.field "TickMissedCount60Min" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "LastDriftMs" 2 (Decode.option Decode.asInt64))
        |> Decode.apply (Decode.field "LastTickMissedAt" 3 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "GeneratedAt" 4 Decode.asDateTime)

    /// Generated decoder for `DegradedCapability` — one combinator per field or case, read off the type's own shape.
    let degradedCapability: Decoder<DegradedCapability> =
        Decode.succeed (fun capability degradedSince reason impact remediation -> ({
            Capability = capability
            DegradedSince = degradedSince
            Reason = reason
            Impact = impact
            Remediation = remediation
        }: DegradedCapability))
        |> Decode.apply (Decode.field "Capability" 0 Decode.asString)
        |> Decode.apply (Decode.field "DegradedSince" 1 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "Reason" 2 Decode.asString)
        |> Decode.apply (Decode.field "Impact" 3 Decode.asString)
        |> Decode.apply (Decode.field "Remediation" 4 Decode.asString)

    /// Generated decoder for `AIDenialGroupCount` — one combinator per field or case, read off the type's own shape.
    let aiDenialGroupCount: Decoder<AIDenialGroupCount> =
        Decode.succeed (fun key count -> ({
            Key = key
            Count = count
        }: AIDenialGroupCount))
        |> Decode.apply (Decode.field "Key" 0 Decode.asString)
        |> Decode.apply (Decode.field "Count" 1 Decode.asInt32)

    /// Generated decoder for `AIDenialToolModulePair` — one combinator per field or case, read off the type's own shape.
    let aiDenialToolModulePair: Decoder<AIDenialToolModulePair> =
        Decode.succeed (fun toolName activeModule count -> ({
            ToolName = toolName
            ActiveModule = activeModule
            Count = count
        }: AIDenialToolModulePair))
        |> Decode.apply (Decode.field "ToolName" 0 Decode.asString)
        |> Decode.apply (Decode.field "ActiveModule" 1 Decode.asString)
        |> Decode.apply (Decode.field "Count" 2 Decode.asInt32)

    /// Generated decoder for `RecentAIDenial` — one combinator per field or case, read off the type's own shape.
    let recentAIDenial: Decoder<RecentAIDenial> =
        Decode.succeed (fun toolName activeModule reason occurredAt -> ({
            ToolName = toolName
            ActiveModule = activeModule
            Reason = reason
            OccurredAt = occurredAt
        }: RecentAIDenial))
        |> Decode.apply (Decode.field "ToolName" 0 Decode.asString)
        |> Decode.apply (Decode.field "ActiveModule" 1 Decode.asString)
        |> Decode.apply (Decode.field "Reason" 2 Decode.asString)
        |> Decode.apply (Decode.field "OccurredAt" 3 Decode.asDateTime)

    /// Generated decoder for `AIDenialRollup` — one combinator per field or case, read off the type's own shape.
    let aiDenialRollup: Decoder<AIDenialRollup> =
        Decode.succeed (fun generatedAt scopeId windowMinutes totalDenialsAllTime totalDenialsInWindow denialsPerMinute byToolName byActiveModule byScopeId topToolModulePairs recentDenials -> ({
            GeneratedAt = generatedAt
            ScopeId = scopeId
            WindowMinutes = windowMinutes
            TotalDenialsAllTime = totalDenialsAllTime
            TotalDenialsInWindow = totalDenialsInWindow
            DenialsPerMinute = denialsPerMinute
            ByToolName = byToolName
            ByActiveModule = byActiveModule
            ByScopeId = byScopeId
            TopToolModulePairs = topToolModulePairs
            RecentDenials = recentDenials
        }: AIDenialRollup))
        |> Decode.apply (Decode.field "GeneratedAt" 0 Decode.asDateTime)
        |> Decode.apply (Decode.field "ScopeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "WindowMinutes" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "TotalDenialsAllTime" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "TotalDenialsInWindow" 4 Decode.asInt32)
        |> Decode.apply (Decode.field "DenialsPerMinute" 5 Decode.asFloat)
        |> Decode.apply (Decode.field "ByToolName" 6 (Decode.list aiDenialGroupCount))
        |> Decode.apply (Decode.field "ByActiveModule" 7 (Decode.list aiDenialGroupCount))
        |> Decode.apply (Decode.field "ByScopeId" 8 (Decode.list aiDenialGroupCount))
        |> Decode.apply (Decode.field "TopToolModulePairs" 9 (Decode.list aiDenialToolModulePair))
        |> Decode.apply (Decode.field "RecentDenials" 10 (Decode.list recentAIDenial))

    /// Generated decoder for `ToolDataCount` — one combinator per field or case, read off the type's own shape.
    let toolDataCount: Decoder<ToolDataCount> =
        Decode.succeed (fun typeId displayName count -> ({
            TypeId = typeId
            DisplayName = displayName
            Count = count
        }: ToolDataCount))
        |> Decode.apply (Decode.field "TypeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Count" 2 Decode.asInt32)

    /// Generated decoder for `ToolSummary` — one combinator per field or case, read off the type's own shape.
    let toolSummary: Decoder<ToolSummary> =
        Decode.succeed (fun moduleId name dataCounts totalRecords -> ({
            ModuleId = moduleId
            Name = name
            DataCounts = dataCounts
            TotalRecords = totalRecords
        }: ToolSummary))
        |> Decode.apply (Decode.field "ModuleId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Name" 1 Decode.asString)
        |> Decode.apply (Decode.field "DataCounts" 2 (Decode.list toolDataCount))
        |> Decode.apply (Decode.field "TotalRecords" 3 Decode.asInt32)

    /// Generated decoder for `ActiveAiSummary` — one combinator per field or case, read off the type's own shape.
    let activeAiSummary: Decoder<ActiveAiSummary> =
        Decode.succeed (fun providerLabel model -> ({
            ProviderLabel = providerLabel
            Model = model
        }: ActiveAiSummary))
        |> Decode.apply (Decode.field "ProviderLabel" 0 Decode.asString)
        |> Decode.apply (Decode.field "Model" 1 (Decode.option Decode.asString))

    /// Generated decoder for `DeploymentContext` — one combinator per field or case, read off the type's own shape.
    let deploymentContext: Decoder<DeploymentContext> =
        Decode.succeed (fun mode health -> ({
            Mode = mode
            Health = health
        }: DeploymentContext))
        |> Decode.apply (Decode.field "Mode" 0 Decode.asString)
        |> Decode.apply (Decode.field "Health" 1 (Decode.option Decode.asString))

    /// Generated decoder for `HomeOverview` — one combinator per field or case, read off the type's own shape.
    let homeOverview: Decoder<HomeOverview> =
        Decode.succeed (fun tools activeAi deployment generatedAt widgetData -> ({
            Tools = tools
            ActiveAi = activeAi
            Deployment = deployment
            GeneratedAt = generatedAt
            WidgetData = widgetData
        }: HomeOverview))
        |> Decode.apply (Decode.field "Tools" 0 (Decode.list toolSummary))
        |> Decode.apply (Decode.field "ActiveAi" 1 (Decode.option activeAiSummary))
        |> Decode.apply (Decode.field "Deployment" 2 deploymentContext)
        |> Decode.apply (Decode.field "GeneratedAt" 3 Decode.asDateTime)
        |> Decode.apply (Decode.field "WidgetData" 4 (Decode.asMap Decode.asString Decode.asString))

    /// Generated decoder for `HomePinningState` — one combinator per field or case, read off the type's own shape.
    let homePinningState: Decoder<HomePinningState> =
        Decode.succeed (fun pinned recent -> ({
            Pinned = pinned
            Recent = recent
        }: HomePinningState))
        |> Decode.apply (Decode.field "Pinned" 0 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Recent" 1 (Decode.list Decode.asString))

    /// Generated decoder for `ModuleQueryResponse` — one combinator per field or case, read off the type's own shape.
    let moduleQueryResponse: Decoder<ModuleQueryResponse> =
        Decode.succeed (fun payload -> ({
            Payload = payload
        }: ModuleQueryResponse))
        |> Decode.apply (Decode.field "Payload" 0 Decode.asString)

    /// Generated decoder for `ModuleQueryError` — one combinator per field or case, read off the type's own shape.
    let moduleQueryError: Decoder<ModuleQueryError> =
        Decode.union "ModuleQueryError" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map ModuleQueryError.PermissionDenied))
            | 1 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun moduleName queryKey -> ModuleQueryError.NoHandler(moduleName, queryKey))
                         |> Decode.apply (Decode.field "moduleName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "queryKey" 1 Decode.asString))
                )
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map ModuleQueryError.HandlerFailed))
            | _ -> None)

    /// Generated decoder for `ModuleVisibilityRule` — one combinator per field or case, read off the type's own shape.
    let moduleVisibilityRule: Decoder<ModuleVisibilityRule> =
        Decode.union "ModuleVisibilityRule" (function
            | 0 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map ModuleVisibilityRule.Allow))
            | 1 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map ModuleVisibilityRule.Deny))
            | _ -> None)

    /// Generated decoder for `ModuleVisibilityProfile` — one combinator per field or case, read off the type's own shape.
    let moduleVisibilityProfile: Decoder<ModuleVisibilityProfile> =
        Decode.succeed (fun scope rule excludedEntryIds note -> ({
            Scope = scope
            Rule = rule
            ExcludedEntryIds = excludedEntryIds
            Note = note
        }: ModuleVisibilityProfile))
        |> Decode.apply (Decode.field "Scope" 0 flagScope)
        |> Decode.apply (Decode.field "Rule" 1 moduleVisibilityRule)
        |> Decode.apply (Decode.field "ExcludedEntryIds" 2 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Note" 3 (Decode.option Decode.asString))

    /// Generated decoder for `NotificationCategory` — one combinator per field or case, read off the type's own shape.
    let notificationCategory: Decoder<NotificationCategory> =
        Decode.succeed (fun id displayName description suppressible -> ({
            Id = id
            DisplayName = displayName
            Description = description
            Suppressible = suppressible
        }: NotificationCategory))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Description" 2 Decode.asString)
        |> Decode.apply (Decode.field "Suppressible" 3 Decode.asBool)

    /// Generated decoder for `PreferenceChannel` — one combinator per field or case, read off the type's own shape.
    let preferenceChannel: Decoder<PreferenceChannel> =
        Decode.union "PreferenceChannel" (function
            | 0 -> Some(Decode.case0 PreferenceChannel.Email)
            | 1 -> Some(Decode.case0 PreferenceChannel.Sms)
            | 2 -> Some(Decode.case0 PreferenceChannel.Push)
            | _ -> None)

    /// Generated decoder for `DigestFrequency` — one combinator per field or case, read off the type's own shape.
    let digestFrequency: Decoder<DigestFrequency> =
        Decode.union "DigestFrequency" (function
            | 0 -> Some(Decode.case0 DigestFrequency.Hourly)
            | 1 -> Some(Decode.case0 DigestFrequency.Daily)
            | 2 -> Some(Decode.case0 DigestFrequency.Weekly)
            | _ -> None)

    /// Generated decoder for `DeliveryPreference` — one combinator per field or case, read off the type's own shape.
    let deliveryPreference: Decoder<DeliveryPreference> =
        Decode.union "DeliveryPreference" (function
            | 0 -> Some(Decode.case0 DeliveryPreference.Immediate)
            | 1 -> Some(Decode.payload (digestFrequency |> Decode.map DeliveryPreference.Digest))
            | 2 -> Some(Decode.case0 DeliveryPreference.Muted)
            | _ -> None)

    /// Generated decoder for `CategoryChannelPreference` — one combinator per field or case, read off the type's own shape.
    let categoryChannelPreference: Decoder<CategoryChannelPreference> =
        Decode.succeed (fun category channel delivery -> ({
            Category = category
            Channel = channel
            Delivery = delivery
        }: CategoryChannelPreference))
        |> Decode.apply (Decode.field "Category" 0 Decode.asString)
        |> Decode.apply (Decode.field "Channel" 1 preferenceChannel)
        |> Decode.apply (Decode.field "Delivery" 2 deliveryPreference)

    /// Generated decoder for `QuietHours` — one combinator per field or case, read off the type's own shape.
    let quietHours: Decoder<QuietHours> =
        Decode.succeed (fun startMinute endMinute timeZoneId -> ({
            StartMinute = startMinute
            EndMinute = endMinute
            TimeZoneId = timeZoneId
        }: QuietHours))
        |> Decode.apply (Decode.field "StartMinute" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "EndMinute" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "TimeZoneId" 2 Decode.asString)

    /// Generated decoder for `UserNotificationPreferences` — one combinator per field or case, read off the type's own shape.
    let userNotificationPreferences: Decoder<UserNotificationPreferences> =
        Decode.succeed (fun channels quietHours -> ({
            Channels = channels
            QuietHours = quietHours
        }: UserNotificationPreferences))
        |> Decode.apply (Decode.field "Channels" 0 (Decode.list categoryChannelPreference))
        |> Decode.apply (Decode.field "QuietHours" 1 (Decode.option quietHours))

    /// Generated decoder for `NotificationPreferenceView` — one combinator per field or case, read off the type's own shape.
    let notificationPreferenceView: Decoder<NotificationPreferenceView> =
        Decode.succeed (fun categories channels preferences -> ({
            Categories = categories
            Channels = channels
            Preferences = preferences
        }: NotificationPreferenceView))
        |> Decode.apply (Decode.field "Categories" 0 (Decode.list notificationCategory))
        |> Decode.apply (Decode.field "Channels" 1 (Decode.list preferenceChannel))
        |> Decode.apply (Decode.field "Preferences" 2 userNotificationPreferences)

    /// Generated decoder for `TenantLifecyclePhase` — one combinator per field or case, read off the type's own shape.
    let tenantLifecyclePhase: Decoder<TenantLifecyclePhase> =
        Decode.union "TenantLifecyclePhase" (function
            | 0 -> Some(Decode.case0 TenantLifecyclePhase.Provisioning)
            | 1 -> Some(Decode.case0 TenantLifecyclePhase.Deprovisioning)
            | _ -> None)

    /// Generated decoder for `LifecycleHookResult` — one combinator per field or case, read off the type's own shape.
    let lifecycleHookResult: Decoder<LifecycleHookResult> =
        Decode.union "LifecycleHookResult" (function
            | 0 -> Some(Decode.case0 LifecycleHookResult.Completed)
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map LifecycleHookResult.Skipped))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map LifecycleHookResult.Failed))
            | _ -> None)

    /// Generated decoder for `LifecycleHookOutcome` — one combinator per field or case, read off the type's own shape.
    let lifecycleHookOutcome: Decoder<LifecycleHookOutcome> =
        Decode.succeed (fun hookName result elapsedMs -> ({
            HookName = hookName
            Result = result
            ElapsedMs = elapsedMs
        }: LifecycleHookOutcome))
        |> Decode.apply (Decode.field "HookName" 0 Decode.asString)
        |> Decode.apply (Decode.field "Result" 1 lifecycleHookResult)
        |> Decode.apply (Decode.field "ElapsedMs" 2 Decode.asInt64)

    /// Generated decoder for `LifecycleSummary` — one combinator per field or case, read off the type's own shape.
    let lifecycleSummary: Decoder<LifecycleSummary> =
        Decode.succeed (fun scopeId phase outcomes totalElapsedMs -> ({
            ScopeId = scopeId
            Phase = phase
            Outcomes = outcomes
            TotalElapsedMs = totalElapsedMs
        }: LifecycleSummary))
        |> Decode.apply (Decode.field "ScopeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Phase" 1 tenantLifecyclePhase)
        |> Decode.apply (Decode.field "Outcomes" 2 (Decode.list lifecycleHookOutcome))
        |> Decode.apply (Decode.field "TotalElapsedMs" 3 Decode.asInt64)

    /// Generated decoder for `LifecycleJobHandle` — one combinator per field or case, read off the type's own shape.
    let lifecycleJobHandle: Decoder<LifecycleJobHandle> =
        Decode.succeed (fun jobId scopeId phase -> ({
            JobId = jobId
            ScopeId = scopeId
            Phase = phase
        }: LifecycleJobHandle))
        |> Decode.apply (Decode.field "JobId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "ScopeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Phase" 2 tenantLifecyclePhase)

    /// Generated decoder for `LifecyclePreviewItem` — one combinator per field or case, read off the type's own shape.
    let lifecyclePreviewItem: Decoder<LifecyclePreviewItem> =
        Decode.succeed (fun hookName hasPreview wouldAffect detail -> ({
            HookName = hookName
            HasPreview = hasPreview
            WouldAffect = wouldAffect
            Detail = detail
        }: LifecyclePreviewItem))
        |> Decode.apply (Decode.field "HookName" 0 Decode.asString)
        |> Decode.apply (Decode.field "HasPreview" 1 Decode.asBool)
        |> Decode.apply (Decode.field "WouldAffect" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "Detail" 3 Decode.asString)

    /// Generated decoder for `LifecyclePreview` — one combinator per field or case, read off the type's own shape.
    let lifecyclePreview: Decoder<LifecyclePreview> =
        Decode.succeed (fun scopeId items totalWouldAffect -> ({
            ScopeId = scopeId
            Items = items
            TotalWouldAffect = totalWouldAffect
        }: LifecyclePreview))
        |> Decode.apply (Decode.field "ScopeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Items" 1 (Decode.list lifecyclePreviewItem))
        |> Decode.apply (Decode.field "TotalWouldAffect" 2 Decode.asInt32)

    /// Generated decoder for `LifecycleExportArchive` — one combinator per field or case, read off the type's own shape.
    let lifecycleExportArchive: Decoder<LifecycleExportArchive> =
        Decode.succeed (fun container blobPath contentHash segmentCount -> ({
            Container = container
            BlobPath = blobPath
            ContentHash = contentHash
            SegmentCount = segmentCount
        }: LifecycleExportArchive))
        |> Decode.apply (Decode.field "Container" 0 Decode.asString)
        |> Decode.apply (Decode.field "BlobPath" 1 Decode.asString)
        |> Decode.apply (Decode.field "ContentHash" 2 Decode.asString)
        |> Decode.apply (Decode.field "SegmentCount" 3 Decode.asInt32)

    /// Generated decoder for `ExportThenDeprovisionResult` — one combinator per field or case, read off the type's own shape.
    let exportThenDeprovisionResult: Decoder<ExportThenDeprovisionResult> =
        Decode.succeed (fun summary archive -> ({
            Summary = summary
            Archive = archive
        }: ExportThenDeprovisionResult))
        |> Decode.apply (Decode.field "Summary" 0 lifecycleSummary)
        |> Decode.apply (Decode.field "Archive" 1 lifecycleExportArchive)

    /// Generated decoder for `OffboardConfirmation` — one combinator per field or case, read off the type's own shape.
    let offboardConfirmation: Decoder<OffboardConfirmation> =
        Decode.succeed (fun token scopeId reason requestedBy expiresAt -> ({
            Token = token
            ScopeId = scopeId
            Reason = reason
            RequestedBy = requestedBy
            ExpiresAt = expiresAt
        }: OffboardConfirmation))
        |> Decode.apply (Decode.field "Token" 0 Decode.asString)
        |> Decode.apply (Decode.field "ScopeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Reason" 2 Decode.asString)
        |> Decode.apply (Decode.field "RequestedBy" 3 Decode.asString)
        |> Decode.apply (Decode.field "ExpiresAt" 4 Decode.asDateTimeOffset)

    /// Generated decoder for `ScheduledDeprovision` — one combinator per field or case, read off the type's own shape.
    let scheduledDeprovision: Decoder<ScheduledDeprovision> =
        Decode.succeed (fun scopeId requestedBy reason dueAt jobId -> ({
            ScopeId = scopeId
            RequestedBy = requestedBy
            Reason = reason
            DueAt = dueAt
            JobId = jobId
        }: ScheduledDeprovision))
        |> Decode.apply (Decode.field "ScopeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "RequestedBy" 1 Decode.asString)
        |> Decode.apply (Decode.field "Reason" 2 Decode.asString)
        |> Decode.apply (Decode.field "DueAt" 3 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "JobId" 4 Decode.asString)

    /// Generated decoder for `TeamRole` — one combinator per field or case, read off the type's own shape.
    let teamRole: Decoder<TeamRole> =
        Decode.union "TeamRole" (function
            | 0 -> Some(Decode.case0 TeamRole.Owner)
            | 1 -> Some(Decode.case0 TeamRole.Admin)
            | 2 -> Some(Decode.case0 TeamRole.Member)
            | _ -> None)

    /// Generated decoder for `PrincipalSummary` — one combinator per field or case, read off the type's own shape.
    let principalSummary: Decoder<PrincipalSummary> =
        Decode.succeed (fun userId memberships lastSeenAt hasUserScopeData -> ({
            UserId = userId
            Memberships = memberships
            LastSeenAt = lastSeenAt
            HasUserScopeData = hasUserScopeData
        }: PrincipalSummary))
        |> Decode.apply (Decode.field "UserId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Memberships" 1 (Decode.list (Decode.tuple2 Decode.asString teamRole)))
        |> Decode.apply (Decode.field "LastSeenAt" 2 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "HasUserScopeData" 3 Decode.asBool)

    /// Generated decoder for `PresenceLocation` — one combinator per field or case, read off the type's own shape.
    let presenceLocation: Decoder<PresenceLocation> =
        Decode.succeed (fun module' page -> ({
            Module = module'
            Page = page
        }: PresenceLocation))
        |> Decode.apply (Decode.field "Module" 0 Decode.asString)
        |> Decode.apply (Decode.field "Page" 1 (Decode.option Decode.asString))

    /// Generated decoder for `PresencePeer` — one combinator per field or case, read off the type's own shape.
    let presencePeer: Decoder<PresencePeer> =
        Decode.succeed (fun userId displayName location lastSeen -> ({
            UserId = userId
            DisplayName = displayName
            Location = location
            LastSeen = lastSeen
        }: PresencePeer))
        |> Decode.apply (Decode.field "UserId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Location" 2 presenceLocation)
        |> Decode.apply (Decode.field "LastSeen" 3 Decode.asDateTime)

    /// Generated decoder for `EntityLockRef` — one combinator per field or case, read off the type's own shape.
    let entityLockRef: Decoder<EntityLockRef> =
        Decode.succeed (fun entityType entityId -> ({
            EntityType = entityType
            EntityId = entityId
        }: EntityLockRef))
        |> Decode.apply (Decode.field "EntityType" 0 Decode.asString)
        |> Decode.apply (Decode.field "EntityId" 1 Decode.asString)

    /// Generated decoder for `LockLease` — one combinator per field or case, read off the type's own shape.
    let lockLease: Decoder<LockLease> =
        Decode.succeed (fun ref holder acquiredAt expiresAt -> ({
            Ref = ref
            Holder = holder
            AcquiredAt = acquiredAt
            ExpiresAt = expiresAt
        }: LockLease))
        |> Decode.apply (Decode.field "Ref" 0 entityLockRef)
        |> Decode.apply (Decode.field "Holder" 1 Decode.asString)
        |> Decode.apply (Decode.field "AcquiredAt" 2 Decode.asDateTime)
        |> Decode.apply (Decode.field "ExpiresAt" 3 Decode.asDateTime)

    /// Generated decoder for `LockOutcome` — one combinator per field or case, read off the type's own shape.
    let lockOutcome: Decoder<LockOutcome> =
        Decode.union "LockOutcome" (function
            | 0 -> Some(Decode.payload (lockLease |> Decode.map LockOutcome.Acquired))
            | 1 -> Some(Decode.payload (lockLease |> Decode.map LockOutcome.HeldByOther))
            | _ -> None)

    /// Generated decoder for `WireProvenanceCaps` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceCaps: Decoder<WireProvenanceCaps> =
        Decode.succeed (fun maxDepth maxNodes -> ({
            MaxDepth = maxDepth
            MaxNodes = maxNodes
        }: WireProvenanceCaps))
        |> Decode.apply (Decode.field "MaxDepth" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "MaxNodes" 1 Decode.asInt32)

    /// Generated decoder for `WireProvenanceNodeKind` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceNodeKind: Decoder<WireProvenanceNodeKind> =
        Decode.union "WireProvenanceNodeKind" (function
            | 0 -> Some(Decode.case0 WireProvenanceNodeKind.DataObjectVersion)
            | 1 -> Some(Decode.case0 WireProvenanceNodeKind.AnalysisResult)
            | 2 -> Some(Decode.case0 WireProvenanceNodeKind.FactNode)
            | 3 -> Some(Decode.case0 WireProvenanceNodeKind.NarrativeDocument)
            | 4 -> Some(Decode.case0 WireProvenanceNodeKind.ConversationMessage)
            | 5 -> Some(Decode.case0 WireProvenanceNodeKind.AnswerPlanNode)
            | 6 -> Some(Decode.case0 WireProvenanceNodeKind.ModelArtifactNode)
            | 7 -> Some(Decode.case0 WireProvenanceNodeKind.ProvenanceAttachmentNode)
            | _ -> None)

    /// Generated decoder for `WireProvenanceNode` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceNode: Decoder<WireProvenanceNode> =
        Decode.succeed (fun id kind disclosure label -> ({
            Id = id
            Kind = kind
            Disclosure = disclosure
            Label = label
        }: WireProvenanceNode))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 1 wireProvenanceNodeKind)
        |> Decode.apply (Decode.field "Disclosure" 2 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Label" 3 Decode.asString)

    /// Generated decoder for `WireWithheldNode` — one combinator per field or case, read off the type's own shape.
    let wireWithheldNode: Decoder<WireWithheldNode> =
        Decode.succeed (fun id kind policyRef -> ({
            Id = id
            Kind = kind
            PolicyRef = policyRef
        }: WireWithheldNode))
        |> Decode.apply (Decode.field "Id" 0 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 1 wireProvenanceNodeKind)
        |> Decode.apply (Decode.field "PolicyRef" 2 Decode.asString)

    /// Generated decoder for `WireProvenanceNodeAnswer` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceNodeAnswer: Decoder<WireProvenanceNodeAnswer> =
        Decode.union "WireProvenanceNodeAnswer" (function
            | 0 -> Some(Decode.payload (wireProvenanceNode |> Decode.map WireProvenanceNodeAnswer.Found))
            | 1 -> Some(Decode.payload (wireWithheldNode |> Decode.map WireProvenanceNodeAnswer.Withheld))
            | 2 -> Some(Decode.case0 WireProvenanceNodeAnswer.Absent)
            | _ -> None)

    /// Generated decoder for `WireProvenanceEdgeKind` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceEdgeKind: Decoder<WireProvenanceEdgeKind> =
        Decode.union "WireProvenanceEdgeKind" (function
            | 0 -> Some(Decode.case0 WireProvenanceEdgeKind.DerivedFrom)
            | 1 -> Some(Decode.case0 WireProvenanceEdgeKind.EvidenceFor)
            | 2 -> Some(Decode.case0 WireProvenanceEdgeKind.CitesFact)
            | 3 -> Some(Decode.case0 WireProvenanceEdgeKind.Supersedes)
            | 4 -> Some(Decode.case0 WireProvenanceEdgeKind.PlannedBy)
            | 5 -> Some(Decode.case0 WireProvenanceEdgeKind.HasAttachment)
            | _ -> None)

    /// Generated decoder for `WireProvenanceEdge` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceEdge: Decoder<WireProvenanceEdge> =
        Decode.succeed (fun from to' kind -> ({
            From = from
            To = to'
            Kind = kind
        }: WireProvenanceEdge))
        |> Decode.apply (Decode.field "From" 0 Decode.asString)
        |> Decode.apply (Decode.field "To" 1 Decode.asString)
        |> Decode.apply (Decode.field "Kind" 2 wireProvenanceEdgeKind)

    /// Generated decoder for `WireProvenanceEdgeSet` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceEdgeSet: Decoder<WireProvenanceEdgeSet> =
        Decode.succeed (fun ref outgoing incoming -> ({
            Ref = ref
            Outgoing = outgoing
            Incoming = incoming
        }: WireProvenanceEdgeSet))
        |> Decode.apply (Decode.field "Ref" 0 Decode.asString)
        |> Decode.apply (Decode.field "Outgoing" 1 (Decode.list wireProvenanceEdge))
        |> Decode.apply (Decode.field "Incoming" 2 (Decode.list wireProvenanceEdge))

    /// Generated decoder for `WireProvenanceChainPage` — one combinator per field or case, read off the type's own shape.
    let wireProvenanceChainPage: Decoder<WireProvenanceChainPage> =
        Decode.succeed (fun root nodes edges withheld depth -> ({
            Root = root
            Nodes = nodes
            Edges = edges
            Withheld = withheld
            Depth = depth
        }: WireProvenanceChainPage))
        |> Decode.apply (Decode.field "Root" 0 Decode.asString)
        |> Decode.apply (Decode.field "Nodes" 1 (Decode.list wireProvenanceNode))
        |> Decode.apply (Decode.field "Edges" 2 (Decode.list wireProvenanceEdge))
        |> Decode.apply (Decode.field "Withheld" 3 (Decode.list wireWithheldNode))
        |> Decode.apply (Decode.field "Depth" 4 Decode.asInt32)

    /// Generated decoder for `ProvenanceQueryError` — one combinator per field or case, read off the type's own shape.
    let provenanceQueryError: Decoder<ProvenanceQueryError> =
        Decode.union "ProvenanceQueryError" (function
            | 0 -> Some(Decode.payload (Decode.asInt32 |> Decode.map ProvenanceQueryError.ProvenanceDepthInvalid))
            | 1 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun requested cap -> ProvenanceQueryError.ProvenanceDepthExceedsCap(requested, cap))
                         |> Decode.apply (Decode.field "requested" 0 Decode.asInt32)
                         |> Decode.apply (Decode.field "cap" 1 Decode.asInt32))
                )
            | 2 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun nodes cap -> ProvenanceQueryError.ProvenanceChainExceedsNodeCap(nodes, cap))
                         |> Decode.apply (Decode.field "nodes" 0 Decode.asInt32)
                         |> Decode.apply (Decode.field "cap" 1 Decode.asInt32))
                )
            | _ -> None)

    /// Generated decoder for `CredentialOrigin` — one combinator per field or case, read off the type's own shape.
    let credentialOrigin: Decoder<CredentialOrigin> =
        Decode.union "CredentialOrigin" (function
            | 0 -> Some(Decode.case0 CredentialOrigin.PastedKey)
            | 1 -> Some(Decode.case0 CredentialOrigin.OAuthConnected)
            | _ -> None)

    /// Generated decoder for `ProviderHealthStatus` — one combinator per field or case, read off the type's own shape.
    let providerHealthStatus: Decoder<ProviderHealthStatus> =
        Decode.union "ProviderHealthStatus" (function
            | 0 -> Some(Decode.case0 ProviderHealthStatus.Healthy)
            | 1 -> Some(Decode.case0 ProviderHealthStatus.Degraded)
            | 2 -> Some(Decode.case0 ProviderHealthStatus.Unhealthy)
            | 3 -> Some(Decode.case0 ProviderHealthStatus.NeedsReauthorization)
            | 4 -> Some(Decode.case0 ProviderHealthStatus.Unknown)
            | _ -> None)

    /// Generated decoder for `ProviderHealth` — one combinator per field or case, read off the type's own shape.
    let providerHealth: Decoder<ProviderHealth> =
        Decode.succeed (fun lastVerifiedAt recentErrorCount rateLimitHeadroom status -> ({
            LastVerifiedAt = lastVerifiedAt
            RecentErrorCount = recentErrorCount
            RateLimitHeadroom = rateLimitHeadroom
            Status = status
        }: ProviderHealth))
        |> Decode.apply (Decode.field "LastVerifiedAt" 0 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "RecentErrorCount" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "RateLimitHeadroom" 2 (Decode.option Decode.asFloat))
        |> Decode.apply (Decode.field "Status" 3 providerHealthStatus)

    /// Generated decoder for `ProviderEntryView` — one combinator per field or case, read off the type's own shape.
    let providerEntryView: Decoder<ProviderEntryView> =
        Decode.succeed (fun label providerId model tags origin hasCredential connectedAt health updatedAt -> ({
            Label = label
            ProviderId = providerId
            Model = model
            Tags = tags
            Origin = origin
            HasCredential = hasCredential
            ConnectedAt = connectedAt
            Health = health
            UpdatedAt = updatedAt
        }: ProviderEntryView))
        |> Decode.apply (Decode.field "Label" 0 Decode.asString)
        |> Decode.apply (Decode.field "ProviderId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Model" 2 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Tags" 3 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Origin" 4 credentialOrigin)
        |> Decode.apply (Decode.field "HasCredential" 5 Decode.asBool)
        |> Decode.apply (Decode.field "ConnectedAt" 6 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "Health" 7 providerHealth)
        |> Decode.apply (Decode.field "UpdatedAt" 8 Decode.asDateTime)

    /// Generated decoder for `RoutingRule` — one combinator per field or case, read off the type's own shape.
    let routingRule: Decoder<RoutingRule> =
        Decode.succeed (fun surface context entryLabel -> ({
            Surface = surface
            Context = context
            EntryLabel = entryLabel
        }: RoutingRule))
        |> Decode.apply (Decode.field "Surface" 0 Decode.asString)
        |> Decode.apply (Decode.field "Context" 1 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "EntryLabel" 2 Decode.asString)

    /// Generated decoder for `FallbackChain` — one combinator per field or case, read off the type's own shape.
    let fallbackChain: Decoder<FallbackChain> =
        Decode.succeed (fun ordered -> ({
            Ordered = ordered
        }: FallbackChain))
        |> Decode.apply (Decode.field "Ordered" 0 (Decode.list Decode.asString))

    /// Generated decoder for `ProviderProfileView` — one combinator per field or case, read off the type's own shape.
    let providerProfileView: Decoder<ProviderProfileView> =
        Decode.succeed (fun entries routing fallback -> ({
            Entries = entries
            Routing = routing
            Fallback = fallback
        }: ProviderProfileView))
        |> Decode.apply (Decode.field "Entries" 0 (Decode.list providerEntryView))
        |> Decode.apply (Decode.field "Routing" 1 (Decode.list routingRule))
        |> Decode.apply (Decode.field "Fallback" 2 fallbackChain)

    /// Generated decoder for `ModulePermission` — one combinator per field or case, read off the type's own shape.
    let modulePermission: Decoder<ModulePermission> =
        Decode.union "ModulePermission" (function
            | 0 -> Some(Decode.case0 ModulePermission.Read)
            | 1 -> Some(Decode.case0 ModulePermission.Write)
            | 2 -> Some(Decode.case0 ModulePermission.Admin)
            | 3 -> Some(Decode.case0 ModulePermission.SchemaOnly)
            | _ -> None)

    /// Generated decoder for `ServiceAccountStatus` — one combinator per field or case, read off the type's own shape.
    let serviceAccountStatus: Decoder<ServiceAccountStatus> =
        Decode.union "ServiceAccountStatus" (function
            | 0 -> Some(Decode.case0 ServiceAccountStatus.Active)
            | 1 -> Some(Decode.case0 ServiceAccountStatus.Disabled)
            | _ -> None)

    /// Generated decoder for `ServiceAccount` — one combinator per field or case, read off the type's own shape.
    let serviceAccount: Decoder<ServiceAccount> =
        Decode.succeed (fun accountId displayName scopeId permissions createdBy createdAt status -> ({
            AccountId = accountId
            DisplayName = displayName
            ScopeId = scopeId
            Permissions = permissions
            CreatedBy = createdBy
            CreatedAt = createdAt
            Status = status
        }: ServiceAccount))
        |> Decode.apply (Decode.field "AccountId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "ScopeId" 2 Decode.asString)
        |> Decode.apply (Decode.field "Permissions" 3 (Decode.asMap Decode.asString (Decode.list modulePermission)))
        |> Decode.apply (Decode.field "CreatedBy" 4 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 5 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "Status" 6 serviceAccountStatus)

    /// Generated decoder for `ServiceAccountTokenView` — one combinator per field or case, read off the type's own shape.
    let serviceAccountTokenView: Decoder<ServiceAccountTokenView> =
        Decode.succeed (fun tokenId accountId displayName issuedBy issuedAt expiresAt revoked -> ({
            TokenId = tokenId
            AccountId = accountId
            DisplayName = displayName
            IssuedBy = issuedBy
            IssuedAt = issuedAt
            ExpiresAt = expiresAt
            Revoked = revoked
        }: ServiceAccountTokenView))
        |> Decode.apply (Decode.field "TokenId" 0 Decode.asString)
        |> Decode.apply (Decode.field "AccountId" 1 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 2 Decode.asString)
        |> Decode.apply (Decode.field "IssuedBy" 3 Decode.asString)
        |> Decode.apply (Decode.field "IssuedAt" 4 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "ExpiresAt" 5 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "Revoked" 6 Decode.asBool)

    /// Generated decoder for `MintedServiceAccountTokenView` — one combinator per field or case, read off the type's own shape.
    let mintedServiceAccountTokenView: Decoder<MintedServiceAccountTokenView> =
        Decode.succeed (fun secret token -> ({
            Secret = secret
            Token = token
        }: MintedServiceAccountTokenView))
        |> Decode.apply (Decode.field "Secret" 0 Decode.asString)
        |> Decode.apply (Decode.field "Token" 1 serviceAccountTokenView)

    /// Generated decoder for `OverallStatus` — one combinator per field or case, read off the type's own shape.
    let overallStatus: Decoder<OverallStatus> =
        Decode.union "OverallStatus" (function
            | 0 -> Some(Decode.case0 OverallStatus.AllOk)
            | 1 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map OverallStatus.DegradedBy))
            | 2 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map OverallStatus.UnhealthyBy))
            | _ -> None)

    /// Generated decoder for `StatusSeverity` — one combinator per field or case, read off the type's own shape.
    let statusSeverity: Decoder<StatusSeverity> =
        Decode.union "StatusSeverity" (function
            | 0 -> Some(Decode.case0 StatusSeverity.Ok)
            | 1 -> Some(Decode.case0 StatusSeverity.Warn)
            | 2 -> Some(Decode.case0 StatusSeverity.Error)
            | _ -> None)

    /// Generated decoder for `SectionSummary` — one combinator per field or case, read off the type's own shape.
    let sectionSummary: Decoder<SectionSummary> =
        Decode.succeed (fun disabled disabledReason severity headline details -> ({
            Disabled = disabled
            DisabledReason = disabledReason
            Severity = severity
            Headline = headline
            Details = details
        }: SectionSummary))
        |> Decode.apply (Decode.field "Disabled" 0 Decode.asBool)
        |> Decode.apply (Decode.field "DisabledReason" 1 Decode.asString)
        |> Decode.apply (Decode.field "Severity" 2 statusSeverity)
        |> Decode.apply (Decode.field "Headline" 3 Decode.asString)
        |> Decode.apply (Decode.field "Details" 4 (Decode.list Decode.asString))

    /// Generated decoder for `ServiceStatusSnapshot` — one combinator per field or case, read off the type's own shape.
    let serviceStatusSnapshot: Decoder<ServiceStatusSnapshot> =
        Decode.succeed (fun generatedAt overall health preflight drift rateLimit jobQueue smokeTest -> ({
            GeneratedAt = generatedAt
            Overall = overall
            Health = health
            Preflight = preflight
            Drift = drift
            RateLimit = rateLimit
            JobQueue = jobQueue
            SmokeTest = smokeTest
        }: ServiceStatusSnapshot))
        |> Decode.apply (Decode.field "GeneratedAt" 0 Decode.asDateTime)
        |> Decode.apply (Decode.field "Overall" 1 overallStatus)
        |> Decode.apply (Decode.field "Health" 2 sectionSummary)
        |> Decode.apply (Decode.field "Preflight" 3 sectionSummary)
        |> Decode.apply (Decode.field "Drift" 4 sectionSummary)
        |> Decode.apply (Decode.field "RateLimit" 5 sectionSummary)
        |> Decode.apply (Decode.field "JobQueue" 6 sectionSummary)
        |> Decode.apply (Decode.field "SmokeTest" 7 sectionSummary)

    /// Generated decoder for `SessionStatus` — one combinator per field or case, read off the type's own shape.
    let sessionStatus: Decoder<SessionStatus> =
        Decode.union "SessionStatus" (function
            | 0 -> Some(Decode.case0 SessionStatus.ActiveSession)
            | 1 -> Some(Decode.case0 SessionStatus.RevokedSession)
            | _ -> None)

    /// Generated decoder for `SessionRecord` — one combinator per field or case, read off the type's own shape.
    let sessionRecord: Decoder<SessionRecord> =
        Decode.succeed (fun sessionId userId scopeId deviceDescriptor authProvider createdAt lastSeenAt status revokedAt revokedBy -> ({
            SessionId = sessionId
            UserId = userId
            ScopeId = scopeId
            DeviceDescriptor = deviceDescriptor
            AuthProvider = authProvider
            CreatedAt = createdAt
            LastSeenAt = lastSeenAt
            Status = status
            RevokedAt = revokedAt
            RevokedBy = revokedBy
        }: SessionRecord))
        |> Decode.apply (Decode.field "SessionId" 0 Decode.asString)
        |> Decode.apply (Decode.field "UserId" 1 Decode.asString)
        |> Decode.apply (Decode.field "ScopeId" 2 Decode.asString)
        |> Decode.apply (Decode.field "DeviceDescriptor" 3 Decode.asString)
        |> Decode.apply (Decode.field "AuthProvider" 4 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 5 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "LastSeenAt" 6 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "Status" 7 sessionStatus)
        |> Decode.apply (Decode.field "RevokedAt" 8 (Decode.option Decode.asDateTimeOffset))
        |> Decode.apply (Decode.field "RevokedBy" 9 (Decode.option Decode.asString))

    /// Generated decoder for `TeamInviteIssueResult` — one combinator per field or case, read off the type's own shape.
    let teamInviteIssueResult: Decoder<TeamInviteIssueResult> =
        Decode.succeed (fun inviteUrl tokenId -> ({
            InviteUrl = inviteUrl
            TokenId = tokenId
        }: TeamInviteIssueResult))
        |> Decode.apply (Decode.field "InviteUrl" 0 Decode.asString)
        |> Decode.apply (Decode.field "TokenId" 1 Decode.asString)

    /// Generated decoder for `TeamInviteAcceptResult` — one combinator per field or case, read off the type's own shape.
    let teamInviteAcceptResult: Decoder<TeamInviteAcceptResult> =
        Decode.succeed (fun teamId teamName role -> ({
            TeamId = teamId
            TeamName = teamName
            Role = role
        }: TeamInviteAcceptResult))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "TeamName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Role" 2 teamRole)

    /// Generated decoder for `TeamInviteSummary` — one combinator per field or case, read off the type's own shape.
    let teamInviteSummary: Decoder<TeamInviteSummary> =
        Decode.succeed (fun tokenId teamId role inviterUserId emailHint expiresAt remainingUses revoked -> ({
            TokenId = tokenId
            TeamId = teamId
            Role = role
            InviterUserId = inviterUserId
            EmailHint = emailHint
            ExpiresAt = expiresAt
            RemainingUses = remainingUses
            Revoked = revoked
        }: TeamInviteSummary))
        |> Decode.apply (Decode.field "TokenId" 0 Decode.asString)
        |> Decode.apply (Decode.field "TeamId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Role" 2 teamRole)
        |> Decode.apply (Decode.field "InviterUserId" 3 Decode.asString)
        |> Decode.apply (Decode.field "EmailHint" 4 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "ExpiresAt" 5 Decode.asDateTime)
        |> Decode.apply (Decode.field "RemainingUses" 6 Decode.asInt32)
        |> Decode.apply (Decode.field "Revoked" 7 Decode.asBool)

    /// Generated decoder for `PendingInviteByEmail` — one combinator per field or case, read off the type's own shape.
    let pendingInviteByEmail: Decoder<PendingInviteByEmail> =
        Decode.succeed (fun teamId role expiresAt inviterUserId issuedAt -> ({
            TeamId = teamId
            Role = role
            ExpiresAt = expiresAt
            InviterUserId = inviterUserId
            IssuedAt = issuedAt
        }: PendingInviteByEmail))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Role" 1 teamRole)
        |> Decode.apply (Decode.field "ExpiresAt" 2 Decode.asDateTime)
        |> Decode.apply (Decode.field "InviterUserId" 3 Decode.asString)
        |> Decode.apply (Decode.field "IssuedAt" 4 Decode.asDateTime)

    /// Generated decoder for `TeamInviteExpiredPayload` — one combinator per field or case, read off the type's own shape.
    let teamInviteExpiredPayload: Decoder<TeamInviteExpiredPayload> =
        Decode.succeed (fun teamId inviteeEmail inviterUserId role issuedAt expiredAt -> ({
            TeamId = teamId
            InviteeEmail = inviteeEmail
            InviterUserId = inviterUserId
            Role = role
            IssuedAt = issuedAt
            ExpiredAt = expiredAt
        }: TeamInviteExpiredPayload))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "InviteeEmail" 1 Decode.asString)
        |> Decode.apply (Decode.field "InviterUserId" 2 Decode.asString)
        |> Decode.apply (Decode.field "Role" 3 teamRole)
        |> Decode.apply (Decode.field "IssuedAt" 4 Decode.asDateTime)
        |> Decode.apply (Decode.field "ExpiredAt" 5 Decode.asDateTime)

    /// Generated decoder for `TeamInfo` — one combinator per field or case, read off the type's own shape.
    let teamInfo: Decoder<TeamInfo> =
        Decode.succeed (fun teamId name createdAt archived -> ({
            TeamId = teamId
            Name = name
            CreatedAt = createdAt
            Archived = archived
        }: TeamInfo))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Name" 1 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 2 Decode.asDateTime)
        |> Decode.apply (Decode.field "Archived" 3 Decode.asBool)

    /// Generated decoder for `UserSummary` — one combinator per field or case, read off the type's own shape.
    let userSummary: Decoder<UserSummary> =
        Decode.succeed (fun userId displayName email -> ({
            UserId = userId
            DisplayName = displayName
            Email = email
        }: UserSummary))
        |> Decode.apply (Decode.field "UserId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Email" 2 (Decode.option Decode.asString))

    /// Generated decoder for `AuthoredBy` — one combinator per field or case, read off the type's own shape.
    let authoredBy: Decoder<AuthoredBy> =
        Decode.union "AuthoredBy" (function
            | 0 -> Some(Decode.case0 AuthoredBy.Human)
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map AuthoredBy.AIWithApproval))
            | _ -> None)

    /// Generated decoder for `BIFriendlyType` — one combinator per field or case, read off the type's own shape.
    let biFriendlyType: Decoder<BIFriendlyType> =
        Decode.union "BIFriendlyType" (function
            | 0 -> Some(Decode.case0 BIFriendlyType.String)
            | 1 -> Some(Decode.case0 BIFriendlyType.Number)
            | 2 -> Some(Decode.case0 BIFriendlyType.Boolean)
            | 3 -> Some(Decode.case0 BIFriendlyType.Date)
            | 4 -> Some(Decode.case0 BIFriendlyType.DateTime)
            | 5 -> Some(Decode.payload (Decode.asString |> Decode.map BIFriendlyType.Currency))
            | 6 -> Some(Decode.payload (Decode.asInt32 |> Decode.map BIFriendlyType.Percentage))
            | 7 -> Some(Decode.case0 BIFriendlyType.CountryCode)
            | 8 -> Some(Decode.payload ((Decode.list Decode.asString) |> Decode.map BIFriendlyType.Enum))
            | 9 -> Some(Decode.payload (Decode.asString |> Decode.map BIFriendlyType.Ref))
            | 10 -> Some(Decode.case0 BIFriendlyType.Id)
            | 11 -> Some(Decode.case0 BIFriendlyType.Email)
            | 12 -> Some(Decode.case0 BIFriendlyType.Url)
            | _ -> None)

    /// Generated decoder for `FieldSensitivity` — one combinator per field or case, read off the type's own shape.
    let fieldSensitivity: Decoder<FieldSensitivity> =
        Decode.union "FieldSensitivity" (function
            | 0 -> Some(Decode.case0 FieldSensitivity.Public)
            | 1 -> Some(Decode.case0 FieldSensitivity.Internal)
            | 2 -> Some(Decode.case0 FieldSensitivity.PII)
            | 3 -> Some(Decode.case0 FieldSensitivity.PHI)
            | 4 -> Some(Decode.case0 FieldSensitivity.Financial)
            | 5 -> Some(Decode.case0 FieldSensitivity.Secret)
            | _ -> None)

    /// Generated decoder for `UserSchemaField` — one combinator per field or case, read off the type's own shape.
    let userSchemaField: Decoder<UserSchemaField> =
        Decode.succeed (fun name type' required description sensitivity -> ({
            Name = name
            Type = type'
            Required = required
            Description = description
            Sensitivity = sensitivity
        }: UserSchemaField))
        |> Decode.apply (Decode.field "Name" 0 Decode.asString)
        |> Decode.apply (Decode.field "Type" 1 biFriendlyType)
        |> Decode.apply (Decode.field "Required" 2 Decode.asBool)
        |> Decode.apply (Decode.field "Description" 3 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Sensitivity" 4 fieldSensitivity)

    /// Generated decoder for `SchemaMigration` — one combinator per field or case, read off the type's own shape.
    let schemaMigration: Decoder<SchemaMigration> =
        Decode.union "SchemaMigration" (function
            | 0 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun field defaultValue -> SchemaMigration.AddField(field, defaultValue))
                         |> Decode.apply (Decode.field "field" 0 userSchemaField)
                         |> Decode.apply (Decode.field "defaultValue" 1 Decode.asString))
                )
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map SchemaMigration.RemoveField))
            | 2 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun fromName toName -> SchemaMigration.RenameField(fromName, toName))
                         |> Decode.apply (Decode.field "fromName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "toName" 1 Decode.asString))
                )
            | 3 ->
                Some(
                    Decode.fields
                        3
                        (Decode.succeed (fun fieldName fromType toType -> SchemaMigration.TightenType(fieldName, fromType, toType))
                         |> Decode.apply (Decode.field "fieldName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "fromType" 1 biFriendlyType)
                         |> Decode.apply (Decode.field "toType" 2 biFriendlyType))
                )
            | 4 ->
                Some(
                    Decode.fields
                        3
                        (Decode.succeed (fun fieldName fromType toType -> SchemaMigration.LooseType(fieldName, fromType, toType))
                         |> Decode.apply (Decode.field "fieldName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "fromType" 1 biFriendlyType)
                         |> Decode.apply (Decode.field "toType" 2 biFriendlyType))
                )
            | 5 ->
                Some(
                    Decode.fields
                        3
                        (Decode.succeed (fun fieldName fromTypeId toTypeId -> SchemaMigration.ChangeRef(fieldName, fromTypeId, toTypeId))
                         |> Decode.apply (Decode.field "fieldName" 0 Decode.asString)
                         |> Decode.apply (Decode.field "fromTypeId" 1 Decode.asString)
                         |> Decode.apply (Decode.field "toTypeId" 2 Decode.asString))
                )
            | _ -> None)

    /// Generated decoder for `UserAuthoredSchema` — one combinator per field or case, read off the type's own shape.
    let userAuthoredSchema: Decoder<UserAuthoredSchema> =
        Decode.succeed (fun schemaId displayName description owner version versionLabel proposedBy evolvedFrom migrationPlan fields -> ({
            SchemaId = schemaId
            DisplayName = displayName
            Description = description
            Owner = owner
            Version = version
            VersionLabel = versionLabel
            ProposedBy = proposedBy
            EvolvedFrom = evolvedFrom
            MigrationPlan = migrationPlan
            Fields = fields
        }: UserAuthoredSchema))
        |> Decode.apply (Decode.field "SchemaId" 0 Decode.asString)
        |> Decode.apply (Decode.field "DisplayName" 1 Decode.asString)
        |> Decode.apply (Decode.field "Description" 2 Decode.asString)
        |> Decode.apply (Decode.field "Owner" 3 Decode.asString)
        |> Decode.apply (Decode.field "Version" 4 Decode.asInt32)
        |> Decode.apply (Decode.field "VersionLabel" 5 Decode.asString)
        |> Decode.apply (Decode.field "ProposedBy" 6 authoredBy)
        |> Decode.apply (Decode.field "EvolvedFrom" 7 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "MigrationPlan" 8 (Decode.list schemaMigration))
        |> Decode.apply (Decode.field "Fields" 9 (Decode.list userSchemaField))

    /// Generated decoder for `UserSchemaError` — one combinator per field or case, read off the type's own shape.
    let userSchemaError: Decoder<UserSchemaError> =
        Decode.union "UserSchemaError" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map UserSchemaError.SchemaNotFound))
            | 1 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun schemaId version -> UserSchemaError.SchemaVersionNotFound(schemaId, version))
                         |> Decode.apply (Decode.field "schemaId" 0 Decode.asString)
                         |> Decode.apply (Decode.field "version" 1 Decode.asInt32))
                )
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map UserSchemaError.InvalidSchema))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map UserSchemaError.MigrationFailed))
            | 4 -> Some(Decode.payload (Decode.asString |> Decode.map UserSchemaError.UserSchemaUnauthorized))
            | 5 -> Some(Decode.payload (Decode.asString |> Decode.map UserSchemaError.UserSchemaStorageFailure))
            | _ -> None)

    /// Generated decoder for `MigrationOutcome` — one combinator per field or case, read off the type's own shape.
    let migrationOutcome: Decoder<MigrationOutcome> =
        Decode.succeed (fun schemaId fromVersion toVersion instancesMigrated appliedMigrations -> ({
            SchemaId = schemaId
            FromVersion = fromVersion
            ToVersion = toVersion
            InstancesMigrated = instancesMigrated
            AppliedMigrations = appliedMigrations
        }: MigrationOutcome))
        |> Decode.apply (Decode.field "SchemaId" 0 Decode.asString)
        |> Decode.apply (Decode.field "FromVersion" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "ToVersion" 2 Decode.asInt32)
        |> Decode.apply (Decode.field "InstancesMigrated" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "AppliedMigrations" 4 Decode.asInt32)

    /// Generated decoder for `WebhookStatus` — one combinator per field or case, read off the type's own shape.
    let webhookStatus: Decoder<WebhookStatus> =
        Decode.union "WebhookStatus" (function
            | 0 -> Some(Decode.case0 WebhookStatus.Active)
            | 1 -> Some(Decode.case0 WebhookStatus.Paused)
            | 2 -> Some(Decode.case0 WebhookStatus.Disabled)
            | _ -> None)

    /// Generated decoder for `WebhookSubscription` — one combinator per field or case, read off the type's own shape.
    let webhookSubscription: Decoder<WebhookSubscription> =
        Decode.succeed (fun subscriptionId scopeId targetUrl secretRef secret eventTypes status createdBy createdAt consecutiveFailures previousSecretRef previousSecret previousSecretExpiresAt -> ({
            SubscriptionId = subscriptionId
            ScopeId = scopeId
            TargetUrl = targetUrl
            SecretRef = secretRef
            Secret = secret
            EventTypes = eventTypes
            Status = status
            CreatedBy = createdBy
            CreatedAt = createdAt
            ConsecutiveFailures = consecutiveFailures
            PreviousSecretRef = previousSecretRef
            PreviousSecret = previousSecret
            PreviousSecretExpiresAt = previousSecretExpiresAt
        }: WebhookSubscription))
        |> Decode.apply (Decode.field "SubscriptionId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "ScopeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "TargetUrl" 2 Decode.asString)
        |> Decode.apply (Decode.field "SecretRef" 3 Decode.asString)
        |> Decode.apply (Decode.field "Secret" 4 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "EventTypes" 5 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Status" 6 webhookStatus)
        |> Decode.apply (Decode.field "CreatedBy" 7 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 8 Decode.asDateTime)
        |> Decode.apply (Decode.field "ConsecutiveFailures" 9 Decode.asInt32)
        |> Decode.apply (Decode.field "PreviousSecretRef" 10 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "PreviousSecret" 11 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "PreviousSecretExpiresAt" 12 (Decode.option Decode.asDateTime))

    /// Generated decoder for `WebhookTestResult` — one combinator per field or case, read off the type's own shape.
    let webhookTestResult: Decoder<WebhookTestResult> =
        Decode.succeed (fun deliveryId statusCode latencyMs error -> ({
            DeliveryId = deliveryId
            StatusCode = statusCode
            LatencyMs = latencyMs
            Error = error
        }: WebhookTestResult))
        |> Decode.apply (Decode.field "DeliveryId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "StatusCode" 1 (Decode.option Decode.asInt32))
        |> Decode.apply (Decode.field "LatencyMs" 2 Decode.asInt64)
        |> Decode.apply (Decode.field "Error" 3 (Decode.option Decode.asString))

    /// Generated decoder for `WebhookDeliveryOutcome` — one combinator per field or case, read off the type's own shape.
    let webhookDeliveryOutcome: Decoder<WebhookDeliveryOutcome> =
        Decode.union "WebhookDeliveryOutcome" (function
            | 0 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun statusCode latencyMs -> WebhookDeliveryOutcome.Success(statusCode, latencyMs))
                         |> Decode.apply (Decode.field "statusCode" 0 Decode.asInt32)
                         |> Decode.apply (Decode.field "latencyMs" 1 Decode.asInt64))
                )
            | 1 ->
                Some(
                    Decode.fields
                        3
                        (Decode.succeed (fun statusCode error latencyMs -> WebhookDeliveryOutcome.Failure(statusCode, error, latencyMs))
                         |> Decode.apply (Decode.field "statusCode" 0 (Decode.option Decode.asInt32))
                         |> Decode.apply (Decode.field "error" 1 Decode.asString)
                         |> Decode.apply (Decode.field "latencyMs" 2 Decode.asInt64))
                )
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map WebhookDeliveryOutcome.DeadLettered))
            | _ -> None)

    /// Generated decoder for `WebhookDelivery` — one combinator per field or case, read off the type's own shape.
    let webhookDelivery: Decoder<WebhookDelivery> =
        Decode.succeed (fun deliveryId subscriptionId eventId attempt attemptedAt outcome -> ({
            DeliveryId = deliveryId
            SubscriptionId = subscriptionId
            EventId = eventId
            Attempt = attempt
            AttemptedAt = attemptedAt
            Outcome = outcome
        }: WebhookDelivery))
        |> Decode.apply (Decode.field "DeliveryId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "SubscriptionId" 1 Decode.asGuid)
        |> Decode.apply (Decode.field "EventId" 2 (Decode.option Decode.asGuid))
        |> Decode.apply (Decode.field "Attempt" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "AttemptedAt" 4 Decode.asDateTime)
        |> Decode.apply (Decode.field "Outcome" 5 webhookDeliveryOutcome)

    /// Generated decoder for `Trigger` — one combinator per field or case, read off the type's own shape.
    let trigger: Decoder<Trigger> =
        Decode.union "Trigger" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map Trigger.CronTrigger))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map Trigger.OnEvent))
            | 2 -> Some(Decode.case0 Trigger.Manual)
            | _ -> None)

    /// Generated decoder for `IdempotencyKey` — one combinator per field or case, read off the type's own shape.
    let idempotencyKey: Decoder<IdempotencyKey> =
        Decode.succeed (fun key ttlSeconds -> ({
            Key = key
            TtlSeconds = ttlSeconds
        }: IdempotencyKey))
        |> Decode.apply (Decode.field "Key" 0 Decode.asString)
        |> Decode.apply (Decode.field "TtlSeconds" 1 Decode.asInt32)

    /// Generated decoder for `JobRetryPolicy` — one combinator per field or case, read off the type's own shape.
    let jobRetryPolicy: Decoder<JobRetryPolicy> =
        Decode.succeed (fun maxAttempts initialBackoff maxBackoff deadLetterDestination -> ({
            MaxAttempts = maxAttempts
            InitialBackoff = initialBackoff
            MaxBackoff = maxBackoff
            DeadLetterDestination = deadLetterDestination
        }: JobRetryPolicy))
        |> Decode.apply (Decode.field "MaxAttempts" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "InitialBackoff" 1 Decode.asTimeSpan)
        |> Decode.apply (Decode.field "MaxBackoff" 2 Decode.asTimeSpan)
        |> Decode.apply (Decode.field "DeadLetterDestination" 3 (Decode.option Decode.asString))

    /// Generated decoder for `JobPrecision` — one combinator per field or case, read off the type's own shape.
    let jobPrecision: Decoder<JobPrecision> =
        Decode.union "JobPrecision" (function
            | 0 -> Some(Decode.case0 JobPrecision.Second)
            | 1 -> Some(Decode.case0 JobPrecision.Minute)
            | _ -> None)

    /// Generated decoder for `JobStatus` — one combinator per field or case, read off the type's own shape.
    let jobStatus: Decoder<JobStatus> =
        Decode.union "JobStatus" (function
            | 0 -> Some(Decode.case0 JobStatus.Active)
            | 1 -> Some(Decode.case0 JobStatus.Disabled)
            | 2 -> Some(Decode.case0 JobStatus.Cancelled)
            | _ -> None)

    /// Generated decoder for `JobRunStatus` — one combinator per field or case, read off the type's own shape.
    let jobRunStatus: Decoder<JobRunStatus> =
        Decode.union "JobRunStatus" (function
            | 0 -> Some(Decode.case0 JobRunStatus.Pending)
            | 1 -> Some(Decode.case0 JobRunStatus.Running)
            | 2 -> Some(Decode.case0 JobRunStatus.Succeeded)
            | 3 -> Some(Decode.case0 JobRunStatus.Failed)
            | 4 -> Some(Decode.case0 JobRunStatus.DeadLettered)
            | 5 -> Some(Decode.case0 JobRunStatus.AwaitingExternal)
            | 6 -> Some(Decode.case0 JobRunStatus.ExternallyCancelled)
            | _ -> None)

    /// Generated decoder for `JobDefinition` — one combinator per field or case, read off the type's own shape.
    let jobDefinition: Decoder<JobDefinition> =
        Decode.succeed (fun jobId scopeId handler payload trigger idempotency retryPolicy shardKey precision status createdAt createdBy nextRunAt lastRunAt lastRunStatus lastRunError consecutiveFailures tags -> ({
            JobId = jobId
            ScopeId = scopeId
            Handler = handler
            Payload = payload
            Trigger = trigger
            Idempotency = idempotency
            RetryPolicy = retryPolicy
            ShardKey = shardKey
            Precision = precision
            Status = status
            CreatedAt = createdAt
            CreatedBy = createdBy
            NextRunAt = nextRunAt
            LastRunAt = lastRunAt
            LastRunStatus = lastRunStatus
            LastRunError = lastRunError
            ConsecutiveFailures = consecutiveFailures
            Tags = tags
        }: JobDefinition))
        |> Decode.apply (Decode.field "JobId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "ScopeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Handler" 2 Decode.asString)
        |> Decode.apply (Decode.field "Payload" 3 Decode.asString)
        |> Decode.apply (Decode.field "Trigger" 4 trigger)
        |> Decode.apply (Decode.field "Idempotency" 5 (Decode.option idempotencyKey))
        |> Decode.apply (Decode.field "RetryPolicy" 6 jobRetryPolicy)
        |> Decode.apply (Decode.field "ShardKey" 7 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "Precision" 8 jobPrecision)
        |> Decode.apply (Decode.field "Status" 9 jobStatus)
        |> Decode.apply (Decode.field "CreatedAt" 10 Decode.asDateTime)
        |> Decode.apply (Decode.field "CreatedBy" 11 Decode.asString)
        |> Decode.apply (Decode.field "NextRunAt" 12 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "LastRunAt" 13 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "LastRunStatus" 14 (Decode.option jobRunStatus))
        |> Decode.apply (Decode.field "LastRunError" 15 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "ConsecutiveFailures" 16 Decode.asInt32)
        |> Decode.apply (Decode.field "Tags" 17 (Decode.asMap Decode.asString Decode.asString))

    /// Generated decoder for `ExternalHandle` — one combinator per field or case, read off the type's own shape.
    let externalHandle: Decoder<ExternalHandle> =
        Decode.succeed (fun handleId backend scopeId nativeRef submittedAt -> ({
            HandleId = handleId
            Backend = backend
            ScopeId = scopeId
            NativeRef = nativeRef
            SubmittedAt = submittedAt
        }: ExternalHandle))
        |> Decode.apply (Decode.field "HandleId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "Backend" 1 Decode.asString)
        |> Decode.apply (Decode.field "ScopeId" 2 Decode.asString)
        |> Decode.apply (Decode.field "NativeRef" 3 Decode.asString)
        |> Decode.apply (Decode.field "SubmittedAt" 4 Decode.asDateTime)

    /// Generated decoder for `JobRun` — one combinator per field or case, read off the type's own shape.
    let jobRun: Decoder<JobRun> =
        Decode.succeed (fun runId jobId scopeId attempt startedAt completedAt status error durationMs externalHandle -> ({
            RunId = runId
            JobId = jobId
            ScopeId = scopeId
            Attempt = attempt
            StartedAt = startedAt
            CompletedAt = completedAt
            Status = status
            Error = error
            DurationMs = durationMs
            ExternalHandle = externalHandle
        }: JobRun))
        |> Decode.apply (Decode.field "RunId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "JobId" 1 Decode.asGuid)
        |> Decode.apply (Decode.field "ScopeId" 2 Decode.asString)
        |> Decode.apply (Decode.field "Attempt" 3 Decode.asInt32)
        |> Decode.apply (Decode.field "StartedAt" 4 Decode.asDateTime)
        |> Decode.apply (Decode.field "CompletedAt" 5 (Decode.option Decode.asDateTime))
        |> Decode.apply (Decode.field "Status" 6 jobRunStatus)
        |> Decode.apply (Decode.field "Error" 7 (Decode.option Decode.asString))
        |> Decode.apply (Decode.field "DurationMs" 8 (Decode.option Decode.asInt64))
        |> Decode.apply (Decode.field "ExternalHandle" 9 (Decode.option externalHandle))

    /// Generated decoder for `ScheduleError` — one combinator per field or case, read off the type's own shape.
    let scheduleError: Decoder<ScheduleError> =
        Decode.union "ScheduleError" (function
            | 0 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun expression reason -> ScheduleError.InvalidCron(expression, reason))
                         |> Decode.apply (Decode.field "expression" 0 Decode.asString)
                         |> Decode.apply (Decode.field "reason" 1 Decode.asString))
                )
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map ScheduleError.HandlerNotRegistered))
            | 2 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun supplied supported -> ScheduleError.PrecisionUnsupported(supplied, supported))
                         |> Decode.apply (Decode.field "supplied" 0 jobPrecision)
                         |> Decode.apply (Decode.field "supported" 1 (Decode.list jobPrecision)))
                )
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map ScheduleError.StorageFailure))
            | _ -> None)

    /// Generated decoder for `ModelExecutionJobRef` — one combinator per field or case, read off the type's own shape.
    let modelExecutionJobRef: Decoder<ModelExecutionJobRef> =
        Decode.succeed (fun index jobId -> ({
            Index = index
            JobId = jobId
        }: ModelExecutionJobRef))
        |> Decode.apply (Decode.field "Index" 0 Decode.asInt32)
        |> Decode.apply (Decode.field "JobId" 1 Decode.asString)

    /// Generated decoder for `ModelExecutionScoreRefusal` — one combinator per field or case, read off the type's own shape.
    let modelExecutionScoreRefusal: Decoder<ModelExecutionScoreRefusal> =
        Decode.union "ModelExecutionScoreRefusal" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionScoreRefusal.ProviderNotFound))
            | 1 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionScoreRefusal.NotApproved))
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionScoreRefusal.InputSchemaMismatch))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionScoreRefusal.InputUnavailable))
            | 4 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun kind message -> ModelExecutionScoreRefusal.ProviderFailed(kind, message))
                         |> Decode.apply (Decode.field "kind" 0 Decode.asString)
                         |> Decode.apply (Decode.field "message" 1 Decode.asString))
                )
            | 5 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionScoreRefusal.StorageFailure))
            | _ -> None)

    /// Generated decoder for `ComputeBudgetDenial` — one combinator per field or case, read off the type's own shape.
    let computeBudgetDenial: Decoder<ComputeBudgetDenial> =
        Decode.succeed (fun scopeId submitterClass dimension quota spent requested periodKey -> ({
            ScopeId = scopeId
            SubmitterClass = submitterClass
            Dimension = dimension
            Quota = quota
            Spent = spent
            Requested = requested
            PeriodKey = periodKey
        }: ComputeBudgetDenial))
        |> Decode.apply (Decode.field "ScopeId" 0 Decode.asString)
        |> Decode.apply (Decode.field "SubmitterClass" 1 Decode.asString)
        |> Decode.apply (Decode.field "Dimension" 2 Decode.asString)
        |> Decode.apply (Decode.field "Quota" 3 Decode.asDecimal)
        |> Decode.apply (Decode.field "Spent" 4 Decode.asDecimal)
        |> Decode.apply (Decode.field "Requested" 5 Decode.asDecimal)
        |> Decode.apply (Decode.field "PeriodKey" 6 Decode.asString)

    /// Generated decoder for `ModelExecutionGateVerdict` — one combinator per field or case, read off the type's own shape.
    let modelExecutionGateVerdict: Decoder<ModelExecutionGateVerdict> =
        Decode.succeed (fun name threshold direction observed passed -> ({
            Name = name
            Threshold = threshold
            Direction = direction
            Observed = observed
            Passed = passed
        }: ModelExecutionGateVerdict))
        |> Decode.apply (Decode.field "Name" 0 Decode.asString)
        |> Decode.apply (Decode.field "Threshold" 1 Decode.asFloat)
        |> Decode.apply (Decode.field "Direction" 2 Decode.asString)
        |> Decode.apply (Decode.field "Observed" 3 Decode.asFloat)
        |> Decode.apply (Decode.field "Passed" 4 Decode.asBool)

    /// Generated decoder for `ModelExecutionRefusal` — one combinator per field or case, read off the type's own shape.
    let modelExecutionRefusal: Decoder<ModelExecutionRefusal> =
        Decode.union "ModelExecutionRefusal" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.SubstrateDisabled))
            | 1 -> Some(Decode.case0 ModelExecutionRefusal.ScopeUnavailable)
            | 2 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.Forbidden))
            | 3 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.UnknownProvider))
            | 4 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.InvalidSubmission))
            | 5 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.NotFound))
            | 6 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.InvalidQuery))
            | 7 -> Some(Decode.payload (modelExecutionScoreRefusal |> Decode.map ModelExecutionRefusal.ScoreRefused))
            | 8 -> Some(Decode.payload (computeBudgetDenial |> Decode.map ModelExecutionRefusal.BudgetDenied))
            | 9 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun received accepted -> ModelExecutionRefusal.EnvelopeVersionMismatch(received, accepted))
                         |> Decode.apply (Decode.field "received" 0 Decode.asInt32)
                         |> Decode.apply (Decode.field "accepted" 1 (Decode.list Decode.asInt32)))
                )
            | 10 ->
                Some(
                    Decode.fields
                        2
                        (Decode.succeed (fun kind known -> ModelExecutionRefusal.UnknownDocumentKind(kind, known))
                         |> Decode.apply (Decode.field "kind" 0 Decode.asString)
                         |> Decode.apply (Decode.field "known" 1 (Decode.list Decode.asString)))
                )
            | 11 -> Some(Decode.payload ((Decode.list modelExecutionGateVerdict) |> Decode.map ModelExecutionRefusal.GateFailed))
            | 12 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.PolicyRefused))
            | 13 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.StorageFailure))
            | 14 -> Some(Decode.payload (Decode.asString |> Decode.map ModelExecutionRefusal.Unexpected))
            | _ -> None)

    /// Generated decoder for `ModelExecutionReceipt` — one combinator per field or case, read off the type's own shape.
    let modelExecutionReceipt: Decoder<ModelExecutionReceipt> =
        Decode.succeed (fun batchId itemCount jobs enqueueFailures -> ({
            BatchId = batchId
            ItemCount = itemCount
            Jobs = jobs
            EnqueueFailures = enqueueFailures
        }: ModelExecutionReceipt))
        |> Decode.apply (Decode.field "BatchId" 0 Decode.asString)
        |> Decode.apply (Decode.field "ItemCount" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "Jobs" 2 (Decode.list modelExecutionJobRef))
        |> Decode.apply (Decode.field "EnqueueFailures" 3 (Decode.list (Decode.tuple2 Decode.asInt32 modelExecutionRefusal)))

    /// Generated decoder for `ModelExecutionArtifactRef` — one combinator per field or case, read off the type's own shape.
    let modelExecutionArtifactRef: Decoder<ModelExecutionArtifactRef> =
        Decode.succeed (fun artifactId contentHash format -> ({
            ArtifactId = artifactId
            ContentHash = contentHash
            Format = format
        }: ModelExecutionArtifactRef))
        |> Decode.apply (Decode.field "ArtifactId" 0 Decode.asString)
        |> Decode.apply (Decode.field "ContentHash" 1 Decode.asString)
        |> Decode.apply (Decode.field "Format" 2 (Decode.option Decode.asString))

    /// Generated decoder for `ModelExecutionTiming` — one combinator per field or case, read off the type's own shape.
    let modelExecutionTiming: Decoder<ModelExecutionTiming> =
        Decode.succeed (fun submittedAt startedAt completedAt durationMs -> ({
            SubmittedAt = submittedAt
            StartedAt = startedAt
            CompletedAt = completedAt
            DurationMs = durationMs
        }: ModelExecutionTiming))
        |> Decode.apply (Decode.field "SubmittedAt" 0 Decode.asDateTimeOffset)
        |> Decode.apply (Decode.field "StartedAt" 1 (Decode.option Decode.asDateTimeOffset))
        |> Decode.apply (Decode.field "CompletedAt" 2 (Decode.option Decode.asDateTimeOffset))
        |> Decode.apply (Decode.field "DurationMs" 3 (Decode.option Decode.asInt64))

    /// Generated decoder for `ModelExecutionCost` — one combinator per field or case, read off the type's own shape.
    let modelExecutionCost: Decoder<ModelExecutionCost> =
        Decode.succeed (fun unit amount -> ({
            Unit = unit
            Amount = amount
        }: ModelExecutionCost))
        |> Decode.apply (Decode.field "Unit" 0 Decode.asString)
        |> Decode.apply (Decode.field "Amount" 1 Decode.asFloat)

    /// Generated decoder for `ModelExecutionOutcome` — one combinator per field or case, read off the type's own shape.
    let modelExecutionOutcome: Decoder<ModelExecutionOutcome> =
        Decode.succeed (fun compositeKeyHash specHash datasetVersion seed providerId providerVersion artifact diagnostics gateVerdicts status timing cost annotations registeredAt -> ({
            CompositeKeyHash = compositeKeyHash
            SpecHash = specHash
            DatasetVersion = datasetVersion
            Seed = seed
            ProviderId = providerId
            ProviderVersion = providerVersion
            Artifact = artifact
            Diagnostics = diagnostics
            GateVerdicts = gateVerdicts
            Status = status
            Timing = timing
            Cost = cost
            Annotations = annotations
            RegisteredAt = registeredAt
        }: ModelExecutionOutcome))
        |> Decode.apply (Decode.field "CompositeKeyHash" 0 Decode.asString)
        |> Decode.apply (Decode.field "SpecHash" 1 Decode.asString)
        |> Decode.apply (Decode.field "DatasetVersion" 2 Decode.asString)
        |> Decode.apply (Decode.field "Seed" 3 Decode.asInt64)
        |> Decode.apply (Decode.field "ProviderId" 4 Decode.asString)
        |> Decode.apply (Decode.field "ProviderVersion" 5 Decode.asString)
        |> Decode.apply (Decode.field "Artifact" 6 (Decode.option modelExecutionArtifactRef))
        |> Decode.apply (Decode.field "Diagnostics" 7 (Decode.asMap Decode.asString Decode.asFloat))
        |> Decode.apply (Decode.field "GateVerdicts" 8 (Decode.list modelExecutionGateVerdict))
        |> Decode.apply (Decode.field "Status" 9 Decode.asString)
        |> Decode.apply (Decode.field "Timing" 10 modelExecutionTiming)
        |> Decode.apply (Decode.field "Cost" 11 (Decode.option modelExecutionCost))
        |> Decode.apply (Decode.field "Annotations" 12 (Decode.asMap Decode.asString Decode.asString))
        |> Decode.apply (Decode.field "RegisteredAt" 13 Decode.asDateTimeOffset)

    /// Generated decoder for `ModelExecutionOutcomePage` — one combinator per field or case, read off the type's own shape.
    let modelExecutionOutcomePage: Decoder<ModelExecutionOutcomePage> =
        Decode.succeed (fun outcomes nextCursor -> ({
            Outcomes = outcomes
            NextCursor = nextCursor
        }: ModelExecutionOutcomePage))
        |> Decode.apply (Decode.field "Outcomes" 0 (Decode.list modelExecutionOutcome))
        |> Decode.apply (Decode.field "NextCursor" 1 (Decode.option Decode.asString))

    /// Generated decoder for `ModelExecutionDatasetVersion` — one combinator per field or case, read off the type's own shape.
    let modelExecutionDatasetVersion: Decoder<ModelExecutionDatasetVersion> =
        Decode.succeed (fun datasetId version rowCount format contentHash createdAt -> ({
            DatasetId = datasetId
            Version = version
            RowCount = rowCount
            Format = format
            ContentHash = contentHash
            CreatedAt = createdAt
        }: ModelExecutionDatasetVersion))
        |> Decode.apply (Decode.field "DatasetId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Version" 1 Decode.asInt32)
        |> Decode.apply (Decode.field "RowCount" 2 Decode.asInt64)
        |> Decode.apply (Decode.field "Format" 3 Decode.asString)
        |> Decode.apply (Decode.field "ContentHash" 4 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 5 Decode.asDateTimeOffset)

    /// Generated decoder for `ModuleExposure` — one combinator per field or case, read off the type's own shape.
    let moduleExposure: Decoder<ModuleExposure> =
        Decode.union "ModuleExposure" (function
            | 0 -> Some(Decode.case0 ModuleExposure.Available)
            | 1 -> Some(Decode.case0 ModuleExposure.Hidden)
            | 2 -> Some(Decode.case0 ModuleExposure.Unavailable)
            | _ -> None)

    /// Generated decoder for `GrantState` — one combinator per field or case, read off the type's own shape.
    let grantState: Decoder<GrantState> =
        Decode.union "GrantState" (function
            | 0 -> Some(Decode.case0 GrantState.Active)
            | 1 -> Some(Decode.case0 GrantState.PendingConsent)
            | _ -> None)

    /// Generated decoder for `PartyRef` — one combinator per field or case, read off the type's own shape.
    let partyRef: Decoder<PartyRef> =
        Decode.union "PartyRef" (function
            | 0 -> Some(Decode.payload (Decode.asString |> Decode.map PartyRef.PartyRef))
            | _ -> None)

    /// Generated decoder for `GrantPolicy` — one combinator per field or case, read off the type's own shape.
    let grantPolicy: Decoder<GrantPolicy> =
        Decode.union "GrantPolicy" (function
            | 0 -> Some(Decode.case0 GrantPolicy.AdminDiscretion)
            | 1 -> Some(Decode.case0 GrantPolicy.RequiresAcknowledgement)
            | 2 -> Some(Decode.case0 GrantPolicy.RequiresSubjectConsent)
            | 3 -> Some(Decode.payload (partyRef |> Decode.map GrantPolicy.RequiresCounterpartyApproval))
            | _ -> None)

    /// Generated decoder for `ModuleGrantRecord` — one combinator per field or case, read off the type's own shape.
    let moduleGrantRecord: Decoder<ModuleGrantRecord> =
        Decode.succeed (fun state satisfiedPolicy justification consentedBy -> ({
            State = state
            SatisfiedPolicy = satisfiedPolicy
            Justification = justification
            ConsentedBy = consentedBy
        }: ModuleGrantRecord))
        |> Decode.apply (Decode.field "State" 0 grantState)
        |> Decode.apply (Decode.field "SatisfiedPolicy" 1 grantPolicy)
        |> Decode.apply (Decode.field "Justification" 2 Decode.asString)
        |> Decode.apply (Decode.field "ConsentedBy" 3 (Decode.option Decode.asString))

    /// Generated decoder for `TeamPermissions` — one combinator per field or case, read off the type's own shape.
    let teamPermissions: Decoder<TeamPermissions> =
        Decode.succeed (fun defaults members exposure grants -> ({
            Defaults = defaults
            Members = members
            Exposure = exposure
            Grants = grants
        }: TeamPermissions))
        |> Decode.apply (Decode.field "Defaults" 0 (Decode.asMap Decode.asString (Decode.list modulePermission)))
        |> Decode.apply (Decode.field "Members" 1 (Decode.asMap Decode.asString (Decode.asMap Decode.asString (Decode.list modulePermission))))
        |> Decode.apply (Decode.field "Exposure" 2 (Decode.asMap Decode.asString moduleExposure))
        |> Decode.apply (Decode.field "Grants" 3 (Decode.asMap Decode.asString (Decode.asMap Decode.asString moduleGrantRecord)))

    /// Generated decoder for `PlatformKnowledgeBaseMode` — one combinator per field or case, read off the type's own shape.
    let platformKnowledgeBaseMode: Decoder<PlatformKnowledgeBaseMode> =
        Decode.union "PlatformKnowledgeBaseMode" (function
            | 0 -> Some(Decode.case0 PlatformKnowledgeBaseMode.NoPlatformKnowledgeBase)
            | 1 -> Some(Decode.case0 PlatformKnowledgeBaseMode.EnabledPlatformKnowledgeBase)
            | _ -> None)

    /// Generated decoder for `PlatformInfo` — one combinator per field or case, read off the type's own shape.
    let platformInfo: Decoder<PlatformInfo> =
        Decode.succeed (fun requiresAuth -> ({
            RequiresAuth = requiresAuth
        }: PlatformInfo))
        |> Decode.apply (Decode.field "RequiresAuth" 0 Decode.asBool)

    /// Generated decoder for `TeamMembership` — one combinator per field or case, read off the type's own shape.
    let teamMembership: Decoder<TeamMembership> =
        Decode.succeed (fun teamId userId role joinedAt -> ({
            TeamId = teamId
            UserId = userId
            Role = role
            JoinedAt = joinedAt
        }: TeamMembership))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "UserId" 1 Decode.asString)
        |> Decode.apply (Decode.field "Role" 2 teamRole)
        |> Decode.apply (Decode.field "JoinedAt" 3 Decode.asDateTime)

    /// Generated decoder for `TeamCreationPolicy` — one combinator per field or case, read off the type's own shape.
    let teamCreationPolicy: Decoder<TeamCreationPolicy> =
        Decode.union "TeamCreationPolicy" (function
            | 0 -> Some(Decode.case0 TeamCreationPolicy.PlatformAdminOnly)
            | 1 -> Some(Decode.case0 TeamCreationPolicy.AnyAuthenticatedUser)
            | _ -> None)

    /// Generated decoder for `TeamSummary` — one combinator per field or case, read off the type's own shape.
    let teamSummary: Decoder<TeamSummary> =
        Decode.succeed (fun teamId name createdAt archived memberCount owners admins -> ({
            TeamId = teamId
            Name = name
            CreatedAt = createdAt
            Archived = archived
            MemberCount = memberCount
            Owners = owners
            Admins = admins
        }: TeamSummary))
        |> Decode.apply (Decode.field "TeamId" 0 Decode.asString)
        |> Decode.apply (Decode.field "Name" 1 Decode.asString)
        |> Decode.apply (Decode.field "CreatedAt" 2 Decode.asDateTime)
        |> Decode.apply (Decode.field "Archived" 3 Decode.asBool)
        |> Decode.apply (Decode.field "MemberCount" 4 Decode.asInt32)
        |> Decode.apply (Decode.field "Owners" 5 (Decode.list Decode.asString))
        |> Decode.apply (Decode.field "Admins" 6 (Decode.list Decode.asString))

    /// Generated decoder for `ProviderOrigin` — one combinator per field or case, read off the type's own shape.
    let providerOrigin: Decoder<ProviderOrigin> =
        Decode.union "ProviderOrigin" (function
            | 0 -> Some(Decode.case0 ProviderOrigin.TenantBYOK)
            | 1 -> Some(Decode.case0 ProviderOrigin.PlatformManaged)
            | _ -> None)

    /// Generated decoder for `UsageRecord` — one combinator per field or case, read off the type's own shape.
    let usageRecord: Decoder<UsageRecord> =
        Decode.succeed (fun recordId scopeId resourceKind quantity unit origin metadata timestamp -> ({
            RecordId = recordId
            ScopeId = scopeId
            ResourceKind = resourceKind
            Quantity = quantity
            Unit = unit
            Origin = origin
            Metadata = metadata
            Timestamp = timestamp
        }: UsageRecord))
        |> Decode.apply (Decode.field "RecordId" 0 Decode.asGuid)
        |> Decode.apply (Decode.field "ScopeId" 1 Decode.asString)
        |> Decode.apply (Decode.field "ResourceKind" 2 Decode.asString)
        |> Decode.apply (Decode.field "Quantity" 3 Decode.asDecimal)
        |> Decode.apply (Decode.field "Unit" 4 Decode.asString)
        |> Decode.apply (Decode.field "Origin" 5 (Decode.option providerOrigin))
        |> Decode.apply (Decode.field "Metadata" 6 (Decode.asMap Decode.asString Decode.asString))
        |> Decode.apply (Decode.field "Timestamp" 7 Decode.asDateTime)

    /// Generated decoder for `UsageAggregateRow` — one combinator per field or case, read off the type's own shape.
    let usageAggregateRow: Decoder<UsageAggregateRow> =
        Decode.succeed (fun bucket quantity -> ({
            Bucket = bucket
            Quantity = quantity
        }: UsageAggregateRow))
        |> Decode.apply (Decode.field "Bucket" 0 Decode.asString)
        |> Decode.apply (Decode.field "Quantity" 1 Decode.asDecimal)

    /// The wire types this module covers, in registration order.
    let covered: string list = [
        typeof<ColumnMappingTypes.DateOrder>.FullName
        typeof<ColumnMappingTypes.CellTransform>.FullName
        typeof<ColumnMappingTypes.ColumnExpr>.FullName
        typeof<ColumnMappingTypes.DerivedColumn>.FullName
        typeof<ColumnMappingTypes.Conversion>.FullName
        typeof<ColumnMappingTypes.ConversionRecord>.FullName
        typeof<ColumnMappingTypes.DryRunCellIssue>.FullName
        typeof<ColumnMappingTypes.DryRunRowIssue>.FullName
        typeof<ColumnMappingTypes.DryRunReport>.FullName
        typeof<DataManagementTypes.UploadedFileInfo>.FullName
        typeof<ProcessedDataTypes.ProcessedData>.FullName
        typeof<ProcessedDataTypes.ProcessedFileEntry>.FullName
        typeof<ProcessedDataTypes.FileUploadResponse>.FullName
        typeof<DataManagementTypes.FileIngestionStatus>.FullName
        typeof<ProcessedDataTypes.FileListSnapshot>.FullName
        typeof<DataManagementTypes.DataFileUpload>.FullName
        typeof<ProcessedDataTypes.SessionStoreInfo>.FullName
        typeof<FlagScope>.FullName
        typeof<ModuleVisibilityResolution>.FullName
        typeof<AccessibleModulesResponse>.FullName
        typeof<DataManagementTypes.ColumnType>.FullName
        typeof<DataManagementTypes.DataTypeColumn>.FullName
        typeof<DataManagementTypes.DataTypeSchema>.FullName
        typeof<DataManagementTypes.DataTypeInfo>.FullName
        typeof<DataManagementTypes.DataTypeCatalogEntry>.FullName
        typeof<DataManagementTypes.DataCatalogResponse>.FullName
        typeof<DataSubjectRequestKind>.FullName
        typeof<ErasurePolicy>.FullName
        typeof<DataSubjectRequest>.FullName
        typeof<ErasureSummary>.FullName
        typeof<DataSubjectRequestApi.ErasurePreview>.FullName
        typeof<ErasureError>.FullName
        typeof<DataSubjectRequestApi.ErasureRunSummary>.FullName
        typeof<DataSubjectRequestApi.ErasureRunResult>.FullName
        typeof<ExportStatus>.FullName
        typeof<ExportSignature>.FullName
        typeof<SignedExportEnvelope>.FullName
        typeof<AuditEventView>.FullName
        typeof<AuditEventPage>.FullName
        typeof<InspectedComponent>.FullName
        typeof<InspectedKnob>.FullName
        typeof<InspectedCanonicalMethod>.FullName
        typeof<CompositionView>.FullName
        typeof<InspectedSurface>.FullName
        typeof<SurfacesView>.FullName
        typeof<InspectedRule>.FullName
        typeof<InspectedDefect>.FullName
        typeof<RulesView>.FullName
        typeof<InspectedDeclaration>.FullName
        typeof<DisclosureView>.FullName
        typeof<ProvenanceView>.FullName
        typeof<ConfigFieldKind>.FullName
        typeof<ConfigFieldSchema>.FullName
        typeof<ModuleConfigSchema>.FullName
        typeof<ModuleConfigEntry>.FullName
        typeof<ModuleConfigView>.FullName
        typeof<DataSourceConfig>.FullName
        typeof<IngestionStatus>.FullName
        typeof<IngestionError>.FullName
        typeof<IngestionRun>.FullName
        typeof<CredentialStatus>.FullName
        typeof<OAuthRefreshOutcome>.FullName
        typeof<TokenStatus>.FullName
        typeof<MigrationDataTypeInfo>.FullName
        typeof<MigrationRunState>.FullName
        typeof<MigrationFailure>.FullName
        typeof<MigrationStatus>.FullName
        typeof<ReadinessVerdict>.FullName
        typeof<SourceStatus>.FullName
        typeof<DeploymentReadiness.PreflightSummary>.FullName
        typeof<DeploymentReadiness.SmokeSummary>.FullName
        typeof<DeploymentReadiness.DriftSummary>.FullName
        typeof<DeploymentReadiness.HealthSummary>.FullName
        typeof<DeploymentReadiness.DeploymentReadinessReport>.FullName
        typeof<VerificationSectionVerdict>.FullName
        typeof<DeploymentVerification.ReportSection>.FullName
        typeof<DeploymentVerification.NotProvedStatement>.FullName
        typeof<DeploymentVerificationOutcome>.FullName
        typeof<DeploymentVerification.DeploymentVerificationReport>.FullName
        typeof<ContactOwner>.FullName
        typeof<NotificationKind.PushVariant>.FullName
        typeof<NotificationKind.SinkKind>.FullName
        typeof<OptInRecord>.FullName
        typeof<ExternalContact>.FullName
        typeof<FlagValue>.FullName
        typeof<FeatureFlag>.FullName
        typeof<HealthProbeView>.FullName
        typeof<HealthSnapshot>.FullName
        typeof<PreflightOutcomeView>.FullName
        typeof<PreflightSnapshotView>.FullName
        typeof<JobSchedulerTelemetryView>.FullName
        typeof<DegradedCapability>.FullName
        typeof<AIDenialGroupCount>.FullName
        typeof<AIDenialToolModulePair>.FullName
        typeof<RecentAIDenial>.FullName
        typeof<AIDenialRollup>.FullName
        typeof<ToolDataCount>.FullName
        typeof<ToolSummary>.FullName
        typeof<ActiveAiSummary>.FullName
        typeof<DeploymentContext>.FullName
        typeof<HomeOverview>.FullName
        typeof<HomePinningState>.FullName
        typeof<ModuleQueryResponse>.FullName
        typeof<ModuleQueryError>.FullName
        typeof<ModuleVisibilityRule>.FullName
        typeof<ModuleVisibilityProfile>.FullName
        typeof<NotificationCategory>.FullName
        typeof<PreferenceChannel>.FullName
        typeof<DigestFrequency>.FullName
        typeof<DeliveryPreference>.FullName
        typeof<CategoryChannelPreference>.FullName
        typeof<QuietHours>.FullName
        typeof<UserNotificationPreferences>.FullName
        typeof<NotificationPreferenceView>.FullName
        typeof<TenantLifecyclePhase>.FullName
        typeof<LifecycleHookResult>.FullName
        typeof<LifecycleHookOutcome>.FullName
        typeof<LifecycleSummary>.FullName
        typeof<LifecycleJobHandle>.FullName
        typeof<LifecyclePreviewItem>.FullName
        typeof<LifecyclePreview>.FullName
        typeof<LifecycleExportArchive>.FullName
        typeof<ExportThenDeprovisionResult>.FullName
        typeof<OffboardConfirmation>.FullName
        typeof<ScheduledDeprovision>.FullName
        typeof<TeamRole>.FullName
        typeof<PrincipalSummary>.FullName
        typeof<PresenceLocation>.FullName
        typeof<PresencePeer>.FullName
        typeof<EntityLockRef>.FullName
        typeof<LockLease>.FullName
        typeof<LockOutcome>.FullName
        typeof<WireProvenanceCaps>.FullName
        typeof<WireProvenanceNodeKind>.FullName
        typeof<WireProvenanceNode>.FullName
        typeof<WireWithheldNode>.FullName
        typeof<WireProvenanceNodeAnswer>.FullName
        typeof<WireProvenanceEdgeKind>.FullName
        typeof<WireProvenanceEdge>.FullName
        typeof<WireProvenanceEdgeSet>.FullName
        typeof<WireProvenanceChainPage>.FullName
        typeof<ProvenanceQueryError>.FullName
        typeof<CredentialOrigin>.FullName
        typeof<ProviderHealthStatus>.FullName
        typeof<ProviderHealth>.FullName
        typeof<ProviderEntryView>.FullName
        typeof<RoutingRule>.FullName
        typeof<FallbackChain>.FullName
        typeof<ProviderProfileView>.FullName
        typeof<ModulePermission>.FullName
        typeof<ServiceAccountStatus>.FullName
        typeof<ServiceAccount>.FullName
        typeof<ServiceAccountTokenView>.FullName
        typeof<MintedServiceAccountTokenView>.FullName
        typeof<OverallStatus>.FullName
        typeof<StatusSeverity>.FullName
        typeof<SectionSummary>.FullName
        typeof<ServiceStatusSnapshot>.FullName
        typeof<SessionStatus>.FullName
        typeof<SessionRecord>.FullName
        typeof<TeamInviteIssueResult>.FullName
        typeof<TeamInviteAcceptResult>.FullName
        typeof<TeamInviteSummary>.FullName
        typeof<PendingInviteByEmail>.FullName
        typeof<TeamInviteExpiredPayload>.FullName
        typeof<TeamInfo>.FullName
        typeof<UserSummary>.FullName
        typeof<AuthoredBy>.FullName
        typeof<BIFriendlyType>.FullName
        typeof<FieldSensitivity>.FullName
        typeof<UserSchemaField>.FullName
        typeof<SchemaMigration>.FullName
        typeof<UserAuthoredSchema>.FullName
        typeof<UserSchemaError>.FullName
        typeof<MigrationOutcome>.FullName
        typeof<WebhookStatus>.FullName
        typeof<WebhookSubscription>.FullName
        typeof<WebhookTestResult>.FullName
        typeof<WebhookDeliveryOutcome>.FullName
        typeof<WebhookDelivery>.FullName
        typeof<Trigger>.FullName
        typeof<IdempotencyKey>.FullName
        typeof<JobRetryPolicy>.FullName
        typeof<JobPrecision>.FullName
        typeof<JobStatus>.FullName
        typeof<JobRunStatus>.FullName
        typeof<JobDefinition>.FullName
        typeof<ExternalHandle>.FullName
        typeof<JobRun>.FullName
        typeof<ScheduleError>.FullName
        typeof<ModelExecutionJobRef>.FullName
        typeof<ModelExecutionScoreRefusal>.FullName
        typeof<ComputeBudgetDenial>.FullName
        typeof<ModelExecutionGateVerdict>.FullName
        typeof<ModelExecutionRefusal>.FullName
        typeof<ModelExecutionReceipt>.FullName
        typeof<ModelExecutionArtifactRef>.FullName
        typeof<ModelExecutionTiming>.FullName
        typeof<ModelExecutionCost>.FullName
        typeof<ModelExecutionOutcome>.FullName
        typeof<ModelExecutionOutcomePage>.FullName
        typeof<ModelExecutionDatasetVersion>.FullName
        typeof<ModuleExposure>.FullName
        typeof<GrantState>.FullName
        typeof<PartyRef>.FullName
        typeof<GrantPolicy>.FullName
        typeof<ModuleGrantRecord>.FullName
        typeof<TeamPermissions>.FullName
        typeof<PlatformKnowledgeBaseMode>.FullName
        typeof<PlatformInfo>.FullName
        typeof<TeamMembership>.FullName
        typeof<TeamCreationPolicy>.FullName
        typeof<TeamSummary>.FullName
        typeof<ProviderOrigin>.FullName
        typeof<UsageRecord>.FullName
        typeof<UsageAggregateRow>.FullName
        typeof<ColumnMappingTypes.Conversion list>.FullName
        typeof<Result<unit, string>>.FullName
        typeof<ColumnMappingTypes.ConversionRecord list>.FullName
        typeof<Result<ColumnMappingTypes.DryRunReport, string>>.FullName
        typeof<Result<ProcessedDataTypes.FileUploadResponse, string>>.FullName
        typeof<Result<DataManagementTypes.DataFileUpload, string>>.FullName
        typeof<Result<ProcessedDataTypes.ProcessedFileEntry, string>>.FullName
        typeof<Result<int, string>>.FullName
        typeof<unit>.FullName
        typeof<Result<byte[], string>>.FullName
        typeof<Result<DataSubjectRequestApi.ErasurePreview, string>>.FullName
        typeof<Result<DataSubjectRequestApi.ErasureRunResult, string>>.FullName
        typeof<Result<string, string>>.FullName
        typeof<Result<ExportStatus, string>>.FullName
        typeof<Result<SignedExportEnvelope, string>>.FullName
        typeof<Result<AuditEventPage, string>>.FullName
        typeof<Result<string list, string>>.FullName
        typeof<Result<CompositionView, string>>.FullName
        typeof<Result<SurfacesView, string>>.FullName
        typeof<Result<RulesView, string>>.FullName
        typeof<Result<DisclosureView, string>>.FullName
        typeof<Result<ProvenanceView, string>>.FullName
        typeof<ModuleConfigEntry list>.FullName
        typeof<Result<ModuleConfigView, string>>.FullName
        typeof<Map<string, Map<string, string>>>.FullName
        typeof<DataSourceConfig list>.FullName
        typeof<DataSourceConfig option>.FullName
        typeof<Result<Guid, string>>.FullName
        typeof<IngestionRun list>.FullName
        typeof<TokenStatus option>.FullName
        typeof<MigrationDataTypeInfo list>.FullName
        typeof<MigrationStatus list>.FullName
        typeof<Result<MigrationStatus, string>>.FullName
        typeof<Result<DeploymentReadiness.DeploymentReadinessReport, string>>.FullName
        typeof<Result<DeploymentVerification.DeploymentVerificationReport, string>>.FullName
        typeof<Result<ExternalContact list, string>>.FullName
        typeof<Result<ExternalContact, string>>.FullName
        typeof<Map<string, FlagValue>>.FullName
        typeof<FeatureFlag list>.FullName
        typeof<Result<Map<string, FlagValue>, string>>.FullName
        typeof<Result<HealthSnapshot, string>>.FullName
        typeof<Result<PreflightSnapshotView, string>>.FullName
        typeof<Result<JobSchedulerTelemetryView, string>>.FullName
        typeof<Result<DegradedCapability list, string>>.FullName
        typeof<Result<AIDenialRollup option, string>>.FullName
        typeof<(Result<ModuleQueryResponse, ModuleQueryError>) option>.FullName
        typeof<ModuleVisibilityResolution option>.FullName
        typeof<Result<ModuleVisibilityProfile option, string>>.FullName
        typeof<string list>.FullName
        typeof<Result<NotificationPreferenceView, string>>.FullName
        typeof<Result<LifecycleSummary, string>>.FullName
        typeof<Result<LifecycleSummary option, string>>.FullName
        typeof<Result<LifecycleJobHandle, string>>.FullName
        typeof<Result<LifecyclePreview, string>>.FullName
        typeof<Result<ExportThenDeprovisionResult, string>>.FullName
        typeof<Result<OffboardConfirmation, string>>.FullName
        typeof<Result<ScheduledDeprovision, string>>.FullName
        typeof<Result<ScheduledDeprovision option, string>>.FullName
        typeof<Result<PrincipalSummary list, string>>.FullName
        typeof<PresencePeer list>.FullName
        typeof<LockLease option>.FullName
        typeof<Result<WireProvenanceChainPage, ProvenanceQueryError>>.FullName
        typeof<Result<ProviderProfileView, string>>.FullName
        typeof<Result<(Tuple<string, ProviderHealth>) list, string>>.FullName
        typeof<Result<ServiceAccount list, string>>.FullName
        typeof<Result<ServiceAccount, string>>.FullName
        typeof<Result<ServiceAccountTokenView list, string>>.FullName
        typeof<Result<MintedServiceAccountTokenView, string>>.FullName
        typeof<Result<ServiceStatusSnapshot, string>>.FullName
        typeof<Result<SectionSummary, string>>.FullName
        typeof<Result<SessionRecord list, string>>.FullName
        typeof<Result<TeamInviteIssueResult, string>>.FullName
        typeof<Result<TeamInviteAcceptResult, string>>.FullName
        typeof<Result<TeamInviteSummary list, string>>.FullName
        typeof<Result<(Tuple<string, PendingInviteByEmail>) list, string>>.FullName
        typeof<Result<TeamInviteExpiredPayload list, string>>.FullName
        typeof<Result<TeamInfo option, string>>.FullName
        typeof<Result<UserSummary list, string>>.FullName
        typeof<Result<UserAuthoredSchema list, UserSchemaError>>.FullName
        typeof<Result<UserAuthoredSchema, UserSchemaError>>.FullName
        typeof<Result<MigrationOutcome, UserSchemaError>>.FullName
        typeof<Result<unit, UserSchemaError>>.FullName
        typeof<Result<WebhookSubscription, string>>.FullName
        typeof<Result<WebhookSubscription list, string>>.FullName
        typeof<Result<WebhookTestResult, string>>.FullName
        typeof<Result<WebhookDelivery list, string>>.FullName
        typeof<JobDefinition list>.FullName
        typeof<JobDefinition option>.FullName
        typeof<JobRun list>.FullName
        typeof<Result<Guid, ScheduleError>>.FullName
        typeof<Result<ModelExecutionReceipt, ModelExecutionRefusal>>.FullName
        typeof<Result<ModelExecutionOutcome, ModelExecutionRefusal>>.FullName
        typeof<Result<ModelExecutionOutcomePage, ModelExecutionRefusal>>.FullName
        typeof<Result<ModelExecutionDatasetVersion, ModelExecutionRefusal>>.FullName
        typeof<Result<TeamPermissions, string>>.FullName
        typeof<bool>.FullName
        typeof<Result<TeamInfo, string>>.FullName
        typeof<TeamInfo list>.FullName
        typeof<TeamMembership list>.FullName
        typeof<string option>.FullName
        typeof<Result<TeamSummary list, string>>.FullName
        typeof<UsageRecord list>.FullName
        typeof<UsageAggregateRow list>.FullName
        typeof<byte[]>.FullName
    ]

    /// Register every decoder above. Idempotent, and explicit —
    /// a static initialiser would fire at a moment nothing states,
    /// so a deployment could not say whether the algebra path was live.
    let registerAll () : unit =
        RemotingDecoders.register<ColumnMappingTypes.DateOrder> dateOrder
        RemotingDecoders.register<ColumnMappingTypes.CellTransform> cellTransform
        RemotingDecoders.register<ColumnMappingTypes.ColumnExpr> columnExpr
        RemotingDecoders.register<ColumnMappingTypes.DerivedColumn> derivedColumn
        RemotingDecoders.register<ColumnMappingTypes.Conversion> conversion
        RemotingDecoders.register<ColumnMappingTypes.ConversionRecord> conversionRecord
        RemotingDecoders.register<ColumnMappingTypes.DryRunCellIssue> dryRunCellIssue
        RemotingDecoders.register<ColumnMappingTypes.DryRunRowIssue> dryRunRowIssue
        RemotingDecoders.register<ColumnMappingTypes.DryRunReport> dryRunReport
        RemotingDecoders.register<DataManagementTypes.UploadedFileInfo> uploadedFileInfo
        RemotingDecoders.register<ProcessedDataTypes.ProcessedData> processedData
        RemotingDecoders.register<ProcessedDataTypes.ProcessedFileEntry> processedFileEntry
        RemotingDecoders.register<ProcessedDataTypes.FileUploadResponse> fileUploadResponse
        RemotingDecoders.register<DataManagementTypes.FileIngestionStatus> fileIngestionStatus
        RemotingDecoders.register<ProcessedDataTypes.FileListSnapshot> fileListSnapshot
        RemotingDecoders.register<DataManagementTypes.DataFileUpload> dataFileUpload
        RemotingDecoders.register<ProcessedDataTypes.SessionStoreInfo> sessionStoreInfo
        RemotingDecoders.register<FlagScope> flagScope
        RemotingDecoders.register<ModuleVisibilityResolution> moduleVisibilityResolution
        RemotingDecoders.register<AccessibleModulesResponse> accessibleModulesResponse
        RemotingDecoders.register<DataManagementTypes.ColumnType> columnType
        RemotingDecoders.register<DataManagementTypes.DataTypeColumn> dataTypeColumn
        RemotingDecoders.register<DataManagementTypes.DataTypeSchema> dataTypeSchema
        RemotingDecoders.register<DataManagementTypes.DataTypeInfo> dataTypeInfo
        RemotingDecoders.register<DataManagementTypes.DataTypeCatalogEntry> dataTypeCatalogEntry
        RemotingDecoders.register<DataManagementTypes.DataCatalogResponse> dataCatalogResponse
        RemotingDecoders.register<DataSubjectRequestKind> dataSubjectRequestKind
        RemotingDecoders.register<ErasurePolicy> erasurePolicy
        RemotingDecoders.register<DataSubjectRequest> dataSubjectRequest
        RemotingDecoders.register<ErasureSummary> erasureSummary
        RemotingDecoders.register<DataSubjectRequestApi.ErasurePreview> erasurePreview
        RemotingDecoders.register<ErasureError> erasureError
        RemotingDecoders.register<DataSubjectRequestApi.ErasureRunSummary> erasureRunSummary
        RemotingDecoders.register<DataSubjectRequestApi.ErasureRunResult> erasureRunResult
        RemotingDecoders.register<ExportStatus> exportStatus
        RemotingDecoders.register<ExportSignature> exportSignature
        RemotingDecoders.register<SignedExportEnvelope> signedExportEnvelope
        RemotingDecoders.register<AuditEventView> auditEventView
        RemotingDecoders.register<AuditEventPage> auditEventPage
        RemotingDecoders.register<InspectedComponent> inspectedComponent
        RemotingDecoders.register<InspectedKnob> inspectedKnob
        RemotingDecoders.register<InspectedCanonicalMethod> inspectedCanonicalMethod
        RemotingDecoders.register<CompositionView> compositionView
        RemotingDecoders.register<InspectedSurface> inspectedSurface
        RemotingDecoders.register<SurfacesView> surfacesView
        RemotingDecoders.register<InspectedRule> inspectedRule
        RemotingDecoders.register<InspectedDefect> inspectedDefect
        RemotingDecoders.register<RulesView> rulesView
        RemotingDecoders.register<InspectedDeclaration> inspectedDeclaration
        RemotingDecoders.register<DisclosureView> disclosureView
        RemotingDecoders.register<ProvenanceView> provenanceView
        RemotingDecoders.register<ConfigFieldKind> configFieldKind
        RemotingDecoders.register<ConfigFieldSchema> configFieldSchema
        RemotingDecoders.register<ModuleConfigSchema> moduleConfigSchema
        RemotingDecoders.register<ModuleConfigEntry> moduleConfigEntry
        RemotingDecoders.register<ModuleConfigView> moduleConfigView
        RemotingDecoders.register<DataSourceConfig> dataSourceConfig
        RemotingDecoders.register<IngestionStatus> ingestionStatus
        RemotingDecoders.register<IngestionError> ingestionError
        RemotingDecoders.register<IngestionRun> ingestionRun
        RemotingDecoders.register<CredentialStatus> credentialStatus
        RemotingDecoders.register<OAuthRefreshOutcome> oAuthRefreshOutcome
        RemotingDecoders.register<TokenStatus> tokenStatus
        RemotingDecoders.register<MigrationDataTypeInfo> migrationDataTypeInfo
        RemotingDecoders.register<MigrationRunState> migrationRunState
        RemotingDecoders.register<MigrationFailure> migrationFailure
        RemotingDecoders.register<MigrationStatus> migrationStatus
        RemotingDecoders.register<ReadinessVerdict> readinessVerdict
        RemotingDecoders.register<SourceStatus> sourceStatus
        RemotingDecoders.register<DeploymentReadiness.PreflightSummary> preflightSummary
        RemotingDecoders.register<DeploymentReadiness.SmokeSummary> smokeSummary
        RemotingDecoders.register<DeploymentReadiness.DriftSummary> driftSummary
        RemotingDecoders.register<DeploymentReadiness.HealthSummary> healthSummary
        RemotingDecoders.register<DeploymentReadiness.DeploymentReadinessReport> deploymentReadinessReport
        RemotingDecoders.register<VerificationSectionVerdict> verificationSectionVerdict
        RemotingDecoders.register<DeploymentVerification.ReportSection> reportSection
        RemotingDecoders.register<DeploymentVerification.NotProvedStatement> notProvedStatement
        RemotingDecoders.register<DeploymentVerificationOutcome> deploymentVerificationOutcome
        RemotingDecoders.register<DeploymentVerification.DeploymentVerificationReport> deploymentVerificationReport
        RemotingDecoders.register<ContactOwner> contactOwner
        RemotingDecoders.register<NotificationKind.PushVariant> pushVariant
        RemotingDecoders.register<NotificationKind.SinkKind> sinkKind
        RemotingDecoders.register<OptInRecord> optInRecord
        RemotingDecoders.register<ExternalContact> externalContact
        RemotingDecoders.register<FlagValue> flagValue
        RemotingDecoders.register<FeatureFlag> featureFlag
        RemotingDecoders.register<HealthProbeView> healthProbeView
        RemotingDecoders.register<HealthSnapshot> healthSnapshot
        RemotingDecoders.register<PreflightOutcomeView> preflightOutcomeView
        RemotingDecoders.register<PreflightSnapshotView> preflightSnapshotView
        RemotingDecoders.register<JobSchedulerTelemetryView> jobSchedulerTelemetryView
        RemotingDecoders.register<DegradedCapability> degradedCapability
        RemotingDecoders.register<AIDenialGroupCount> aiDenialGroupCount
        RemotingDecoders.register<AIDenialToolModulePair> aiDenialToolModulePair
        RemotingDecoders.register<RecentAIDenial> recentAIDenial
        RemotingDecoders.register<AIDenialRollup> aiDenialRollup
        RemotingDecoders.register<ToolDataCount> toolDataCount
        RemotingDecoders.register<ToolSummary> toolSummary
        RemotingDecoders.register<ActiveAiSummary> activeAiSummary
        RemotingDecoders.register<DeploymentContext> deploymentContext
        RemotingDecoders.register<HomeOverview> homeOverview
        RemotingDecoders.register<HomePinningState> homePinningState
        RemotingDecoders.register<ModuleQueryResponse> moduleQueryResponse
        RemotingDecoders.register<ModuleQueryError> moduleQueryError
        RemotingDecoders.register<ModuleVisibilityRule> moduleVisibilityRule
        RemotingDecoders.register<ModuleVisibilityProfile> moduleVisibilityProfile
        RemotingDecoders.register<NotificationCategory> notificationCategory
        RemotingDecoders.register<PreferenceChannel> preferenceChannel
        RemotingDecoders.register<DigestFrequency> digestFrequency
        RemotingDecoders.register<DeliveryPreference> deliveryPreference
        RemotingDecoders.register<CategoryChannelPreference> categoryChannelPreference
        RemotingDecoders.register<QuietHours> quietHours
        RemotingDecoders.register<UserNotificationPreferences> userNotificationPreferences
        RemotingDecoders.register<NotificationPreferenceView> notificationPreferenceView
        RemotingDecoders.register<TenantLifecyclePhase> tenantLifecyclePhase
        RemotingDecoders.register<LifecycleHookResult> lifecycleHookResult
        RemotingDecoders.register<LifecycleHookOutcome> lifecycleHookOutcome
        RemotingDecoders.register<LifecycleSummary> lifecycleSummary
        RemotingDecoders.register<LifecycleJobHandle> lifecycleJobHandle
        RemotingDecoders.register<LifecyclePreviewItem> lifecyclePreviewItem
        RemotingDecoders.register<LifecyclePreview> lifecyclePreview
        RemotingDecoders.register<LifecycleExportArchive> lifecycleExportArchive
        RemotingDecoders.register<ExportThenDeprovisionResult> exportThenDeprovisionResult
        RemotingDecoders.register<OffboardConfirmation> offboardConfirmation
        RemotingDecoders.register<ScheduledDeprovision> scheduledDeprovision
        RemotingDecoders.register<TeamRole> teamRole
        RemotingDecoders.register<PrincipalSummary> principalSummary
        RemotingDecoders.register<PresenceLocation> presenceLocation
        RemotingDecoders.register<PresencePeer> presencePeer
        RemotingDecoders.register<EntityLockRef> entityLockRef
        RemotingDecoders.register<LockLease> lockLease
        RemotingDecoders.register<LockOutcome> lockOutcome
        RemotingDecoders.register<WireProvenanceCaps> wireProvenanceCaps
        RemotingDecoders.register<WireProvenanceNodeKind> wireProvenanceNodeKind
        RemotingDecoders.register<WireProvenanceNode> wireProvenanceNode
        RemotingDecoders.register<WireWithheldNode> wireWithheldNode
        RemotingDecoders.register<WireProvenanceNodeAnswer> wireProvenanceNodeAnswer
        RemotingDecoders.register<WireProvenanceEdgeKind> wireProvenanceEdgeKind
        RemotingDecoders.register<WireProvenanceEdge> wireProvenanceEdge
        RemotingDecoders.register<WireProvenanceEdgeSet> wireProvenanceEdgeSet
        RemotingDecoders.register<WireProvenanceChainPage> wireProvenanceChainPage
        RemotingDecoders.register<ProvenanceQueryError> provenanceQueryError
        RemotingDecoders.register<CredentialOrigin> credentialOrigin
        RemotingDecoders.register<ProviderHealthStatus> providerHealthStatus
        RemotingDecoders.register<ProviderHealth> providerHealth
        RemotingDecoders.register<ProviderEntryView> providerEntryView
        RemotingDecoders.register<RoutingRule> routingRule
        RemotingDecoders.register<FallbackChain> fallbackChain
        RemotingDecoders.register<ProviderProfileView> providerProfileView
        RemotingDecoders.register<ModulePermission> modulePermission
        RemotingDecoders.register<ServiceAccountStatus> serviceAccountStatus
        RemotingDecoders.register<ServiceAccount> serviceAccount
        RemotingDecoders.register<ServiceAccountTokenView> serviceAccountTokenView
        RemotingDecoders.register<MintedServiceAccountTokenView> mintedServiceAccountTokenView
        RemotingDecoders.register<OverallStatus> overallStatus
        RemotingDecoders.register<StatusSeverity> statusSeverity
        RemotingDecoders.register<SectionSummary> sectionSummary
        RemotingDecoders.register<ServiceStatusSnapshot> serviceStatusSnapshot
        RemotingDecoders.register<SessionStatus> sessionStatus
        RemotingDecoders.register<SessionRecord> sessionRecord
        RemotingDecoders.register<TeamInviteIssueResult> teamInviteIssueResult
        RemotingDecoders.register<TeamInviteAcceptResult> teamInviteAcceptResult
        RemotingDecoders.register<TeamInviteSummary> teamInviteSummary
        RemotingDecoders.register<PendingInviteByEmail> pendingInviteByEmail
        RemotingDecoders.register<TeamInviteExpiredPayload> teamInviteExpiredPayload
        RemotingDecoders.register<TeamInfo> teamInfo
        RemotingDecoders.register<UserSummary> userSummary
        RemotingDecoders.register<AuthoredBy> authoredBy
        RemotingDecoders.register<BIFriendlyType> biFriendlyType
        RemotingDecoders.register<FieldSensitivity> fieldSensitivity
        RemotingDecoders.register<UserSchemaField> userSchemaField
        RemotingDecoders.register<SchemaMigration> schemaMigration
        RemotingDecoders.register<UserAuthoredSchema> userAuthoredSchema
        RemotingDecoders.register<UserSchemaError> userSchemaError
        RemotingDecoders.register<MigrationOutcome> migrationOutcome
        RemotingDecoders.register<WebhookStatus> webhookStatus
        RemotingDecoders.register<WebhookSubscription> webhookSubscription
        RemotingDecoders.register<WebhookTestResult> webhookTestResult
        RemotingDecoders.register<WebhookDeliveryOutcome> webhookDeliveryOutcome
        RemotingDecoders.register<WebhookDelivery> webhookDelivery
        RemotingDecoders.register<Trigger> trigger
        RemotingDecoders.register<IdempotencyKey> idempotencyKey
        RemotingDecoders.register<JobRetryPolicy> jobRetryPolicy
        RemotingDecoders.register<JobPrecision> jobPrecision
        RemotingDecoders.register<JobStatus> jobStatus
        RemotingDecoders.register<JobRunStatus> jobRunStatus
        RemotingDecoders.register<JobDefinition> jobDefinition
        RemotingDecoders.register<ExternalHandle> externalHandle
        RemotingDecoders.register<JobRun> jobRun
        RemotingDecoders.register<ScheduleError> scheduleError
        RemotingDecoders.register<ModelExecutionJobRef> modelExecutionJobRef
        RemotingDecoders.register<ModelExecutionScoreRefusal> modelExecutionScoreRefusal
        RemotingDecoders.register<ComputeBudgetDenial> computeBudgetDenial
        RemotingDecoders.register<ModelExecutionGateVerdict> modelExecutionGateVerdict
        RemotingDecoders.register<ModelExecutionRefusal> modelExecutionRefusal
        RemotingDecoders.register<ModelExecutionReceipt> modelExecutionReceipt
        RemotingDecoders.register<ModelExecutionArtifactRef> modelExecutionArtifactRef
        RemotingDecoders.register<ModelExecutionTiming> modelExecutionTiming
        RemotingDecoders.register<ModelExecutionCost> modelExecutionCost
        RemotingDecoders.register<ModelExecutionOutcome> modelExecutionOutcome
        RemotingDecoders.register<ModelExecutionOutcomePage> modelExecutionOutcomePage
        RemotingDecoders.register<ModelExecutionDatasetVersion> modelExecutionDatasetVersion
        RemotingDecoders.register<ModuleExposure> moduleExposure
        RemotingDecoders.register<GrantState> grantState
        RemotingDecoders.register<PartyRef> partyRef
        RemotingDecoders.register<GrantPolicy> grantPolicy
        RemotingDecoders.register<ModuleGrantRecord> moduleGrantRecord
        RemotingDecoders.register<TeamPermissions> teamPermissions
        RemotingDecoders.register<PlatformKnowledgeBaseMode> platformKnowledgeBaseMode
        RemotingDecoders.register<PlatformInfo> platformInfo
        RemotingDecoders.register<TeamMembership> teamMembership
        RemotingDecoders.register<TeamCreationPolicy> teamCreationPolicy
        RemotingDecoders.register<TeamSummary> teamSummary
        RemotingDecoders.register<ProviderOrigin> providerOrigin
        RemotingDecoders.register<UsageRecord> usageRecord
        RemotingDecoders.register<UsageAggregateRow> usageAggregateRow
        RemotingDecoders.register<ColumnMappingTypes.Conversion list> (Decode.list conversion)
        RemotingDecoders.register<Result<unit, string>> (Decode.result Decode.asUnit Decode.asString)
        RemotingDecoders.register<ColumnMappingTypes.ConversionRecord list> (Decode.list conversionRecord)
        RemotingDecoders.register<Result<ColumnMappingTypes.DryRunReport, string>> (Decode.result dryRunReport Decode.asString)
        RemotingDecoders.register<Result<ProcessedDataTypes.FileUploadResponse, string>> (Decode.result fileUploadResponse Decode.asString)
        RemotingDecoders.register<Result<DataManagementTypes.DataFileUpload, string>> (Decode.result dataFileUpload Decode.asString)
        RemotingDecoders.register<Result<ProcessedDataTypes.ProcessedFileEntry, string>> (Decode.result processedFileEntry Decode.asString)
        RemotingDecoders.register<Result<int, string>> (Decode.result Decode.asInt32 Decode.asString)
        RemotingDecoders.register<unit> Decode.asUnit
        RemotingDecoders.register<Result<byte[], string>> (Decode.result Decode.asBytes Decode.asString)
        RemotingDecoders.register<Result<DataSubjectRequestApi.ErasurePreview, string>> (Decode.result erasurePreview Decode.asString)
        RemotingDecoders.register<Result<DataSubjectRequestApi.ErasureRunResult, string>> (Decode.result erasureRunResult Decode.asString)
        RemotingDecoders.register<Result<string, string>> (Decode.result Decode.asString Decode.asString)
        RemotingDecoders.register<Result<ExportStatus, string>> (Decode.result exportStatus Decode.asString)
        RemotingDecoders.register<Result<SignedExportEnvelope, string>> (Decode.result signedExportEnvelope Decode.asString)
        RemotingDecoders.register<Result<AuditEventPage, string>> (Decode.result auditEventPage Decode.asString)
        RemotingDecoders.register<Result<string list, string>> (Decode.result (Decode.list Decode.asString) Decode.asString)
        RemotingDecoders.register<Result<CompositionView, string>> (Decode.result compositionView Decode.asString)
        RemotingDecoders.register<Result<SurfacesView, string>> (Decode.result surfacesView Decode.asString)
        RemotingDecoders.register<Result<RulesView, string>> (Decode.result rulesView Decode.asString)
        RemotingDecoders.register<Result<DisclosureView, string>> (Decode.result disclosureView Decode.asString)
        RemotingDecoders.register<Result<ProvenanceView, string>> (Decode.result provenanceView Decode.asString)
        RemotingDecoders.register<ModuleConfigEntry list> (Decode.list moduleConfigEntry)
        RemotingDecoders.register<Result<ModuleConfigView, string>> (Decode.result moduleConfigView Decode.asString)
        RemotingDecoders.register<Map<string, Map<string, string>>> (Decode.asMap Decode.asString (Decode.asMap Decode.asString Decode.asString))
        RemotingDecoders.register<DataSourceConfig list> (Decode.list dataSourceConfig)
        RemotingDecoders.register<DataSourceConfig option> (Decode.option dataSourceConfig)
        RemotingDecoders.register<Result<Guid, string>> (Decode.result Decode.asGuid Decode.asString)
        RemotingDecoders.register<IngestionRun list> (Decode.list ingestionRun)
        RemotingDecoders.register<TokenStatus option> (Decode.option tokenStatus)
        RemotingDecoders.register<MigrationDataTypeInfo list> (Decode.list migrationDataTypeInfo)
        RemotingDecoders.register<MigrationStatus list> (Decode.list migrationStatus)
        RemotingDecoders.register<Result<MigrationStatus, string>> (Decode.result migrationStatus Decode.asString)
        RemotingDecoders.register<Result<DeploymentReadiness.DeploymentReadinessReport, string>> (Decode.result deploymentReadinessReport Decode.asString)
        RemotingDecoders.register<Result<DeploymentVerification.DeploymentVerificationReport, string>> (Decode.result deploymentVerificationReport Decode.asString)
        RemotingDecoders.register<Result<ExternalContact list, string>> (Decode.result (Decode.list externalContact) Decode.asString)
        RemotingDecoders.register<Result<ExternalContact, string>> (Decode.result externalContact Decode.asString)
        RemotingDecoders.register<Map<string, FlagValue>> (Decode.asMap Decode.asString flagValue)
        RemotingDecoders.register<FeatureFlag list> (Decode.list featureFlag)
        RemotingDecoders.register<Result<Map<string, FlagValue>, string>> (Decode.result (Decode.asMap Decode.asString flagValue) Decode.asString)
        RemotingDecoders.register<Result<HealthSnapshot, string>> (Decode.result healthSnapshot Decode.asString)
        RemotingDecoders.register<Result<PreflightSnapshotView, string>> (Decode.result preflightSnapshotView Decode.asString)
        RemotingDecoders.register<Result<JobSchedulerTelemetryView, string>> (Decode.result jobSchedulerTelemetryView Decode.asString)
        RemotingDecoders.register<Result<DegradedCapability list, string>> (Decode.result (Decode.list degradedCapability) Decode.asString)
        RemotingDecoders.register<Result<AIDenialRollup option, string>> (Decode.result (Decode.option aiDenialRollup) Decode.asString)
        RemotingDecoders.register<(Result<ModuleQueryResponse, ModuleQueryError>) option> (Decode.option (Decode.result moduleQueryResponse moduleQueryError))
        RemotingDecoders.register<ModuleVisibilityResolution option> (Decode.option moduleVisibilityResolution)
        RemotingDecoders.register<Result<ModuleVisibilityProfile option, string>> (Decode.result (Decode.option moduleVisibilityProfile) Decode.asString)
        RemotingDecoders.register<string list> (Decode.list Decode.asString)
        RemotingDecoders.register<Result<NotificationPreferenceView, string>> (Decode.result notificationPreferenceView Decode.asString)
        RemotingDecoders.register<Result<LifecycleSummary, string>> (Decode.result lifecycleSummary Decode.asString)
        RemotingDecoders.register<Result<LifecycleSummary option, string>> (Decode.result (Decode.option lifecycleSummary) Decode.asString)
        RemotingDecoders.register<Result<LifecycleJobHandle, string>> (Decode.result lifecycleJobHandle Decode.asString)
        RemotingDecoders.register<Result<LifecyclePreview, string>> (Decode.result lifecyclePreview Decode.asString)
        RemotingDecoders.register<Result<ExportThenDeprovisionResult, string>> (Decode.result exportThenDeprovisionResult Decode.asString)
        RemotingDecoders.register<Result<OffboardConfirmation, string>> (Decode.result offboardConfirmation Decode.asString)
        RemotingDecoders.register<Result<ScheduledDeprovision, string>> (Decode.result scheduledDeprovision Decode.asString)
        RemotingDecoders.register<Result<ScheduledDeprovision option, string>> (Decode.result (Decode.option scheduledDeprovision) Decode.asString)
        RemotingDecoders.register<Result<PrincipalSummary list, string>> (Decode.result (Decode.list principalSummary) Decode.asString)
        RemotingDecoders.register<PresencePeer list> (Decode.list presencePeer)
        RemotingDecoders.register<LockLease option> (Decode.option lockLease)
        RemotingDecoders.register<Result<WireProvenanceChainPage, ProvenanceQueryError>> (Decode.result wireProvenanceChainPage provenanceQueryError)
        RemotingDecoders.register<Result<ProviderProfileView, string>> (Decode.result providerProfileView Decode.asString)
        RemotingDecoders.register<Result<(Tuple<string, ProviderHealth>) list, string>> (Decode.result (Decode.list (Decode.tuple2 Decode.asString providerHealth)) Decode.asString)
        RemotingDecoders.register<Result<ServiceAccount list, string>> (Decode.result (Decode.list serviceAccount) Decode.asString)
        RemotingDecoders.register<Result<ServiceAccount, string>> (Decode.result serviceAccount Decode.asString)
        RemotingDecoders.register<Result<ServiceAccountTokenView list, string>> (Decode.result (Decode.list serviceAccountTokenView) Decode.asString)
        RemotingDecoders.register<Result<MintedServiceAccountTokenView, string>> (Decode.result mintedServiceAccountTokenView Decode.asString)
        RemotingDecoders.register<Result<ServiceStatusSnapshot, string>> (Decode.result serviceStatusSnapshot Decode.asString)
        RemotingDecoders.register<Result<SectionSummary, string>> (Decode.result sectionSummary Decode.asString)
        RemotingDecoders.register<Result<SessionRecord list, string>> (Decode.result (Decode.list sessionRecord) Decode.asString)
        RemotingDecoders.register<Result<TeamInviteIssueResult, string>> (Decode.result teamInviteIssueResult Decode.asString)
        RemotingDecoders.register<Result<TeamInviteAcceptResult, string>> (Decode.result teamInviteAcceptResult Decode.asString)
        RemotingDecoders.register<Result<TeamInviteSummary list, string>> (Decode.result (Decode.list teamInviteSummary) Decode.asString)
        RemotingDecoders.register<Result<(Tuple<string, PendingInviteByEmail>) list, string>> (Decode.result (Decode.list (Decode.tuple2 Decode.asString pendingInviteByEmail)) Decode.asString)
        RemotingDecoders.register<Result<TeamInviteExpiredPayload list, string>> (Decode.result (Decode.list teamInviteExpiredPayload) Decode.asString)
        RemotingDecoders.register<Result<TeamInfo option, string>> (Decode.result (Decode.option teamInfo) Decode.asString)
        RemotingDecoders.register<Result<UserSummary list, string>> (Decode.result (Decode.list userSummary) Decode.asString)
        RemotingDecoders.register<Result<UserAuthoredSchema list, UserSchemaError>> (Decode.result (Decode.list userAuthoredSchema) userSchemaError)
        RemotingDecoders.register<Result<UserAuthoredSchema, UserSchemaError>> (Decode.result userAuthoredSchema userSchemaError)
        RemotingDecoders.register<Result<MigrationOutcome, UserSchemaError>> (Decode.result migrationOutcome userSchemaError)
        RemotingDecoders.register<Result<unit, UserSchemaError>> (Decode.result Decode.asUnit userSchemaError)
        RemotingDecoders.register<Result<WebhookSubscription, string>> (Decode.result webhookSubscription Decode.asString)
        RemotingDecoders.register<Result<WebhookSubscription list, string>> (Decode.result (Decode.list webhookSubscription) Decode.asString)
        RemotingDecoders.register<Result<WebhookTestResult, string>> (Decode.result webhookTestResult Decode.asString)
        RemotingDecoders.register<Result<WebhookDelivery list, string>> (Decode.result (Decode.list webhookDelivery) Decode.asString)
        RemotingDecoders.register<JobDefinition list> (Decode.list jobDefinition)
        RemotingDecoders.register<JobDefinition option> (Decode.option jobDefinition)
        RemotingDecoders.register<JobRun list> (Decode.list jobRun)
        RemotingDecoders.register<Result<Guid, ScheduleError>> (Decode.result Decode.asGuid scheduleError)
        RemotingDecoders.register<Result<ModelExecutionReceipt, ModelExecutionRefusal>> (Decode.result modelExecutionReceipt modelExecutionRefusal)
        RemotingDecoders.register<Result<ModelExecutionOutcome, ModelExecutionRefusal>> (Decode.result modelExecutionOutcome modelExecutionRefusal)
        RemotingDecoders.register<Result<ModelExecutionOutcomePage, ModelExecutionRefusal>> (Decode.result modelExecutionOutcomePage modelExecutionRefusal)
        RemotingDecoders.register<Result<ModelExecutionDatasetVersion, ModelExecutionRefusal>> (Decode.result modelExecutionDatasetVersion modelExecutionRefusal)
        RemotingDecoders.register<Result<TeamPermissions, string>> (Decode.result teamPermissions Decode.asString)
        RemotingDecoders.register<bool> Decode.asBool
        RemotingDecoders.register<Result<TeamInfo, string>> (Decode.result teamInfo Decode.asString)
        RemotingDecoders.register<TeamInfo list> (Decode.list teamInfo)
        RemotingDecoders.register<TeamMembership list> (Decode.list teamMembership)
        RemotingDecoders.register<string option> (Decode.option Decode.asString)
        RemotingDecoders.register<Result<TeamSummary list, string>> (Decode.result (Decode.list teamSummary) Decode.asString)
        RemotingDecoders.register<UsageRecord list> (Decode.list usageRecord)
        RemotingDecoders.register<UsageAggregateRow list> (Decode.list usageAggregateRow)
        RemotingDecoders.register<byte[]> Decode.asBytes

#if !FABLE_COMPILER
    /// Phase 801 — every decoder above beside the reflection reader,
    /// over `draws` draws of its own type from `seed`. One outcome per
    /// registration, in registration order; a refusal names the type
    /// and the first diverging draw.
    let verifyAll (draws: int) (seed: int) : Result<DecoderVerification, DecoderRefusal> list = [
        RemotingDecoders.verify<ColumnMappingTypes.DateOrder> draws seed dateOrder
        RemotingDecoders.verify<ColumnMappingTypes.CellTransform> draws seed cellTransform
        RemotingDecoders.verify<ColumnMappingTypes.ColumnExpr> draws seed columnExpr
        RemotingDecoders.verify<ColumnMappingTypes.DerivedColumn> draws seed derivedColumn
        RemotingDecoders.verify<ColumnMappingTypes.Conversion> draws seed conversion
        RemotingDecoders.verify<ColumnMappingTypes.ConversionRecord> draws seed conversionRecord
        RemotingDecoders.verify<ColumnMappingTypes.DryRunCellIssue> draws seed dryRunCellIssue
        RemotingDecoders.verify<ColumnMappingTypes.DryRunRowIssue> draws seed dryRunRowIssue
        RemotingDecoders.verify<ColumnMappingTypes.DryRunReport> draws seed dryRunReport
        RemotingDecoders.verify<DataManagementTypes.UploadedFileInfo> draws seed uploadedFileInfo
        RemotingDecoders.verify<ProcessedDataTypes.ProcessedData> draws seed processedData
        RemotingDecoders.verify<ProcessedDataTypes.ProcessedFileEntry> draws seed processedFileEntry
        RemotingDecoders.verify<ProcessedDataTypes.FileUploadResponse> draws seed fileUploadResponse
        RemotingDecoders.verify<DataManagementTypes.FileIngestionStatus> draws seed fileIngestionStatus
        RemotingDecoders.verify<ProcessedDataTypes.FileListSnapshot> draws seed fileListSnapshot
        RemotingDecoders.verify<DataManagementTypes.DataFileUpload> draws seed dataFileUpload
        RemotingDecoders.verify<ProcessedDataTypes.SessionStoreInfo> draws seed sessionStoreInfo
        RemotingDecoders.verify<FlagScope> draws seed flagScope
        RemotingDecoders.verify<ModuleVisibilityResolution> draws seed moduleVisibilityResolution
        RemotingDecoders.verify<AccessibleModulesResponse> draws seed accessibleModulesResponse
        RemotingDecoders.verify<DataManagementTypes.ColumnType> draws seed columnType
        RemotingDecoders.verify<DataManagementTypes.DataTypeColumn> draws seed dataTypeColumn
        RemotingDecoders.verify<DataManagementTypes.DataTypeSchema> draws seed dataTypeSchema
        RemotingDecoders.verify<DataManagementTypes.DataTypeInfo> draws seed dataTypeInfo
        RemotingDecoders.verify<DataManagementTypes.DataTypeCatalogEntry> draws seed dataTypeCatalogEntry
        RemotingDecoders.verify<DataManagementTypes.DataCatalogResponse> draws seed dataCatalogResponse
        RemotingDecoders.verify<DataSubjectRequestKind> draws seed dataSubjectRequestKind
        RemotingDecoders.verify<ErasurePolicy> draws seed erasurePolicy
        RemotingDecoders.verify<DataSubjectRequest> draws seed dataSubjectRequest
        RemotingDecoders.verify<ErasureSummary> draws seed erasureSummary
        RemotingDecoders.verify<DataSubjectRequestApi.ErasurePreview> draws seed erasurePreview
        RemotingDecoders.verify<ErasureError> draws seed erasureError
        RemotingDecoders.verify<DataSubjectRequestApi.ErasureRunSummary> draws seed erasureRunSummary
        RemotingDecoders.verify<DataSubjectRequestApi.ErasureRunResult> draws seed erasureRunResult
        RemotingDecoders.verify<ExportStatus> draws seed exportStatus
        RemotingDecoders.verify<ExportSignature> draws seed exportSignature
        RemotingDecoders.verify<SignedExportEnvelope> draws seed signedExportEnvelope
        RemotingDecoders.verify<AuditEventView> draws seed auditEventView
        RemotingDecoders.verify<AuditEventPage> draws seed auditEventPage
        RemotingDecoders.verify<InspectedComponent> draws seed inspectedComponent
        RemotingDecoders.verify<InspectedKnob> draws seed inspectedKnob
        RemotingDecoders.verify<InspectedCanonicalMethod> draws seed inspectedCanonicalMethod
        RemotingDecoders.verify<CompositionView> draws seed compositionView
        RemotingDecoders.verify<InspectedSurface> draws seed inspectedSurface
        RemotingDecoders.verify<SurfacesView> draws seed surfacesView
        RemotingDecoders.verify<InspectedRule> draws seed inspectedRule
        RemotingDecoders.verify<InspectedDefect> draws seed inspectedDefect
        RemotingDecoders.verify<RulesView> draws seed rulesView
        RemotingDecoders.verify<InspectedDeclaration> draws seed inspectedDeclaration
        RemotingDecoders.verify<DisclosureView> draws seed disclosureView
        RemotingDecoders.verify<ProvenanceView> draws seed provenanceView
        RemotingDecoders.verify<ConfigFieldKind> draws seed configFieldKind
        RemotingDecoders.verify<ConfigFieldSchema> draws seed configFieldSchema
        RemotingDecoders.verify<ModuleConfigSchema> draws seed moduleConfigSchema
        RemotingDecoders.verify<ModuleConfigEntry> draws seed moduleConfigEntry
        RemotingDecoders.verify<ModuleConfigView> draws seed moduleConfigView
        RemotingDecoders.verify<DataSourceConfig> draws seed dataSourceConfig
        RemotingDecoders.verify<IngestionStatus> draws seed ingestionStatus
        RemotingDecoders.verify<IngestionError> draws seed ingestionError
        RemotingDecoders.verify<IngestionRun> draws seed ingestionRun
        RemotingDecoders.verify<CredentialStatus> draws seed credentialStatus
        RemotingDecoders.verify<OAuthRefreshOutcome> draws seed oAuthRefreshOutcome
        RemotingDecoders.verify<TokenStatus> draws seed tokenStatus
        RemotingDecoders.verify<MigrationDataTypeInfo> draws seed migrationDataTypeInfo
        RemotingDecoders.verify<MigrationRunState> draws seed migrationRunState
        RemotingDecoders.verify<MigrationFailure> draws seed migrationFailure
        RemotingDecoders.verify<MigrationStatus> draws seed migrationStatus
        RemotingDecoders.verify<ReadinessVerdict> draws seed readinessVerdict
        RemotingDecoders.verify<SourceStatus> draws seed sourceStatus
        RemotingDecoders.verify<DeploymentReadiness.PreflightSummary> draws seed preflightSummary
        RemotingDecoders.verify<DeploymentReadiness.SmokeSummary> draws seed smokeSummary
        RemotingDecoders.verify<DeploymentReadiness.DriftSummary> draws seed driftSummary
        RemotingDecoders.verify<DeploymentReadiness.HealthSummary> draws seed healthSummary
        RemotingDecoders.verify<DeploymentReadiness.DeploymentReadinessReport> draws seed deploymentReadinessReport
        RemotingDecoders.verify<VerificationSectionVerdict> draws seed verificationSectionVerdict
        RemotingDecoders.verify<DeploymentVerification.ReportSection> draws seed reportSection
        RemotingDecoders.verify<DeploymentVerification.NotProvedStatement> draws seed notProvedStatement
        RemotingDecoders.verify<DeploymentVerificationOutcome> draws seed deploymentVerificationOutcome
        RemotingDecoders.verify<DeploymentVerification.DeploymentVerificationReport> draws seed deploymentVerificationReport
        RemotingDecoders.verify<ContactOwner> draws seed contactOwner
        RemotingDecoders.verify<NotificationKind.PushVariant> draws seed pushVariant
        RemotingDecoders.verify<NotificationKind.SinkKind> draws seed sinkKind
        RemotingDecoders.verify<OptInRecord> draws seed optInRecord
        RemotingDecoders.verify<ExternalContact> draws seed externalContact
        RemotingDecoders.verify<FlagValue> draws seed flagValue
        RemotingDecoders.verify<FeatureFlag> draws seed featureFlag
        RemotingDecoders.verify<HealthProbeView> draws seed healthProbeView
        RemotingDecoders.verify<HealthSnapshot> draws seed healthSnapshot
        RemotingDecoders.verify<PreflightOutcomeView> draws seed preflightOutcomeView
        RemotingDecoders.verify<PreflightSnapshotView> draws seed preflightSnapshotView
        RemotingDecoders.verify<JobSchedulerTelemetryView> draws seed jobSchedulerTelemetryView
        RemotingDecoders.verify<DegradedCapability> draws seed degradedCapability
        RemotingDecoders.verify<AIDenialGroupCount> draws seed aiDenialGroupCount
        RemotingDecoders.verify<AIDenialToolModulePair> draws seed aiDenialToolModulePair
        RemotingDecoders.verify<RecentAIDenial> draws seed recentAIDenial
        RemotingDecoders.verify<AIDenialRollup> draws seed aiDenialRollup
        RemotingDecoders.verify<ToolDataCount> draws seed toolDataCount
        RemotingDecoders.verify<ToolSummary> draws seed toolSummary
        RemotingDecoders.verify<ActiveAiSummary> draws seed activeAiSummary
        RemotingDecoders.verify<DeploymentContext> draws seed deploymentContext
        RemotingDecoders.verify<HomeOverview> draws seed homeOverview
        RemotingDecoders.verify<HomePinningState> draws seed homePinningState
        RemotingDecoders.verify<ModuleQueryResponse> draws seed moduleQueryResponse
        RemotingDecoders.verify<ModuleQueryError> draws seed moduleQueryError
        RemotingDecoders.verify<ModuleVisibilityRule> draws seed moduleVisibilityRule
        RemotingDecoders.verify<ModuleVisibilityProfile> draws seed moduleVisibilityProfile
        RemotingDecoders.verify<NotificationCategory> draws seed notificationCategory
        RemotingDecoders.verify<PreferenceChannel> draws seed preferenceChannel
        RemotingDecoders.verify<DigestFrequency> draws seed digestFrequency
        RemotingDecoders.verify<DeliveryPreference> draws seed deliveryPreference
        RemotingDecoders.verify<CategoryChannelPreference> draws seed categoryChannelPreference
        RemotingDecoders.verify<QuietHours> draws seed quietHours
        RemotingDecoders.verify<UserNotificationPreferences> draws seed userNotificationPreferences
        RemotingDecoders.verify<NotificationPreferenceView> draws seed notificationPreferenceView
        RemotingDecoders.verify<TenantLifecyclePhase> draws seed tenantLifecyclePhase
        RemotingDecoders.verify<LifecycleHookResult> draws seed lifecycleHookResult
        RemotingDecoders.verify<LifecycleHookOutcome> draws seed lifecycleHookOutcome
        RemotingDecoders.verify<LifecycleSummary> draws seed lifecycleSummary
        RemotingDecoders.verify<LifecycleJobHandle> draws seed lifecycleJobHandle
        RemotingDecoders.verify<LifecyclePreviewItem> draws seed lifecyclePreviewItem
        RemotingDecoders.verify<LifecyclePreview> draws seed lifecyclePreview
        RemotingDecoders.verify<LifecycleExportArchive> draws seed lifecycleExportArchive
        RemotingDecoders.verify<ExportThenDeprovisionResult> draws seed exportThenDeprovisionResult
        RemotingDecoders.verify<OffboardConfirmation> draws seed offboardConfirmation
        RemotingDecoders.verify<ScheduledDeprovision> draws seed scheduledDeprovision
        RemotingDecoders.verify<TeamRole> draws seed teamRole
        RemotingDecoders.verify<PrincipalSummary> draws seed principalSummary
        RemotingDecoders.verify<PresenceLocation> draws seed presenceLocation
        RemotingDecoders.verify<PresencePeer> draws seed presencePeer
        RemotingDecoders.verify<EntityLockRef> draws seed entityLockRef
        RemotingDecoders.verify<LockLease> draws seed lockLease
        RemotingDecoders.verify<LockOutcome> draws seed lockOutcome
        RemotingDecoders.verify<WireProvenanceCaps> draws seed wireProvenanceCaps
        RemotingDecoders.verify<WireProvenanceNodeKind> draws seed wireProvenanceNodeKind
        RemotingDecoders.verify<WireProvenanceNode> draws seed wireProvenanceNode
        RemotingDecoders.verify<WireWithheldNode> draws seed wireWithheldNode
        RemotingDecoders.verify<WireProvenanceNodeAnswer> draws seed wireProvenanceNodeAnswer
        RemotingDecoders.verify<WireProvenanceEdgeKind> draws seed wireProvenanceEdgeKind
        RemotingDecoders.verify<WireProvenanceEdge> draws seed wireProvenanceEdge
        RemotingDecoders.verify<WireProvenanceEdgeSet> draws seed wireProvenanceEdgeSet
        RemotingDecoders.verify<WireProvenanceChainPage> draws seed wireProvenanceChainPage
        RemotingDecoders.verify<ProvenanceQueryError> draws seed provenanceQueryError
        RemotingDecoders.verify<CredentialOrigin> draws seed credentialOrigin
        RemotingDecoders.verify<ProviderHealthStatus> draws seed providerHealthStatus
        RemotingDecoders.verify<ProviderHealth> draws seed providerHealth
        RemotingDecoders.verify<ProviderEntryView> draws seed providerEntryView
        RemotingDecoders.verify<RoutingRule> draws seed routingRule
        RemotingDecoders.verify<FallbackChain> draws seed fallbackChain
        RemotingDecoders.verify<ProviderProfileView> draws seed providerProfileView
        RemotingDecoders.verify<ModulePermission> draws seed modulePermission
        RemotingDecoders.verify<ServiceAccountStatus> draws seed serviceAccountStatus
        RemotingDecoders.verify<ServiceAccount> draws seed serviceAccount
        RemotingDecoders.verify<ServiceAccountTokenView> draws seed serviceAccountTokenView
        RemotingDecoders.verify<MintedServiceAccountTokenView> draws seed mintedServiceAccountTokenView
        RemotingDecoders.verify<OverallStatus> draws seed overallStatus
        RemotingDecoders.verify<StatusSeverity> draws seed statusSeverity
        RemotingDecoders.verify<SectionSummary> draws seed sectionSummary
        RemotingDecoders.verify<ServiceStatusSnapshot> draws seed serviceStatusSnapshot
        RemotingDecoders.verify<SessionStatus> draws seed sessionStatus
        RemotingDecoders.verify<SessionRecord> draws seed sessionRecord
        RemotingDecoders.verify<TeamInviteIssueResult> draws seed teamInviteIssueResult
        RemotingDecoders.verify<TeamInviteAcceptResult> draws seed teamInviteAcceptResult
        RemotingDecoders.verify<TeamInviteSummary> draws seed teamInviteSummary
        RemotingDecoders.verify<PendingInviteByEmail> draws seed pendingInviteByEmail
        RemotingDecoders.verify<TeamInviteExpiredPayload> draws seed teamInviteExpiredPayload
        RemotingDecoders.verify<TeamInfo> draws seed teamInfo
        RemotingDecoders.verify<UserSummary> draws seed userSummary
        RemotingDecoders.verify<AuthoredBy> draws seed authoredBy
        RemotingDecoders.verify<BIFriendlyType> draws seed biFriendlyType
        RemotingDecoders.verify<FieldSensitivity> draws seed fieldSensitivity
        RemotingDecoders.verify<UserSchemaField> draws seed userSchemaField
        RemotingDecoders.verify<SchemaMigration> draws seed schemaMigration
        RemotingDecoders.verify<UserAuthoredSchema> draws seed userAuthoredSchema
        RemotingDecoders.verify<UserSchemaError> draws seed userSchemaError
        RemotingDecoders.verify<MigrationOutcome> draws seed migrationOutcome
        RemotingDecoders.verify<WebhookStatus> draws seed webhookStatus
        RemotingDecoders.verify<WebhookSubscription> draws seed webhookSubscription
        RemotingDecoders.verify<WebhookTestResult> draws seed webhookTestResult
        RemotingDecoders.verify<WebhookDeliveryOutcome> draws seed webhookDeliveryOutcome
        RemotingDecoders.verify<WebhookDelivery> draws seed webhookDelivery
        RemotingDecoders.verify<Trigger> draws seed trigger
        RemotingDecoders.verify<IdempotencyKey> draws seed idempotencyKey
        RemotingDecoders.verify<JobRetryPolicy> draws seed jobRetryPolicy
        RemotingDecoders.verify<JobPrecision> draws seed jobPrecision
        RemotingDecoders.verify<JobStatus> draws seed jobStatus
        RemotingDecoders.verify<JobRunStatus> draws seed jobRunStatus
        RemotingDecoders.verify<JobDefinition> draws seed jobDefinition
        RemotingDecoders.verify<ExternalHandle> draws seed externalHandle
        RemotingDecoders.verify<JobRun> draws seed jobRun
        RemotingDecoders.verify<ScheduleError> draws seed scheduleError
        RemotingDecoders.verify<ModelExecutionJobRef> draws seed modelExecutionJobRef
        RemotingDecoders.verify<ModelExecutionScoreRefusal> draws seed modelExecutionScoreRefusal
        RemotingDecoders.verify<ComputeBudgetDenial> draws seed computeBudgetDenial
        RemotingDecoders.verify<ModelExecutionGateVerdict> draws seed modelExecutionGateVerdict
        RemotingDecoders.verify<ModelExecutionRefusal> draws seed modelExecutionRefusal
        RemotingDecoders.verify<ModelExecutionReceipt> draws seed modelExecutionReceipt
        RemotingDecoders.verify<ModelExecutionArtifactRef> draws seed modelExecutionArtifactRef
        RemotingDecoders.verify<ModelExecutionTiming> draws seed modelExecutionTiming
        RemotingDecoders.verify<ModelExecutionCost> draws seed modelExecutionCost
        RemotingDecoders.verify<ModelExecutionOutcome> draws seed modelExecutionOutcome
        RemotingDecoders.verify<ModelExecutionOutcomePage> draws seed modelExecutionOutcomePage
        RemotingDecoders.verify<ModelExecutionDatasetVersion> draws seed modelExecutionDatasetVersion
        RemotingDecoders.verify<ModuleExposure> draws seed moduleExposure
        RemotingDecoders.verify<GrantState> draws seed grantState
        RemotingDecoders.verify<PartyRef> draws seed partyRef
        RemotingDecoders.verify<GrantPolicy> draws seed grantPolicy
        RemotingDecoders.verify<ModuleGrantRecord> draws seed moduleGrantRecord
        RemotingDecoders.verify<TeamPermissions> draws seed teamPermissions
        RemotingDecoders.verify<PlatformKnowledgeBaseMode> draws seed platformKnowledgeBaseMode
        RemotingDecoders.verify<PlatformInfo> draws seed platformInfo
        RemotingDecoders.verify<TeamMembership> draws seed teamMembership
        RemotingDecoders.verify<TeamCreationPolicy> draws seed teamCreationPolicy
        RemotingDecoders.verify<TeamSummary> draws seed teamSummary
        RemotingDecoders.verify<ProviderOrigin> draws seed providerOrigin
        RemotingDecoders.verify<UsageRecord> draws seed usageRecord
        RemotingDecoders.verify<UsageAggregateRow> draws seed usageAggregateRow
        RemotingDecoders.verify<ColumnMappingTypes.Conversion list> draws seed (Decode.list conversion)
        RemotingDecoders.verify<Result<unit, string>> draws seed (Decode.result Decode.asUnit Decode.asString)
        RemotingDecoders.verify<ColumnMappingTypes.ConversionRecord list> draws seed (Decode.list conversionRecord)
        RemotingDecoders.verify<Result<ColumnMappingTypes.DryRunReport, string>> draws seed (Decode.result dryRunReport Decode.asString)
        RemotingDecoders.verify<Result<ProcessedDataTypes.FileUploadResponse, string>> draws seed (Decode.result fileUploadResponse Decode.asString)
        RemotingDecoders.verify<Result<DataManagementTypes.DataFileUpload, string>> draws seed (Decode.result dataFileUpload Decode.asString)
        RemotingDecoders.verify<Result<ProcessedDataTypes.ProcessedFileEntry, string>> draws seed (Decode.result processedFileEntry Decode.asString)
        RemotingDecoders.verify<Result<int, string>> draws seed (Decode.result Decode.asInt32 Decode.asString)
        RemotingDecoders.verify<unit> draws seed Decode.asUnit
        RemotingDecoders.verify<Result<byte[], string>> draws seed (Decode.result Decode.asBytes Decode.asString)
        RemotingDecoders.verify<Result<DataSubjectRequestApi.ErasurePreview, string>> draws seed (Decode.result erasurePreview Decode.asString)
        RemotingDecoders.verify<Result<DataSubjectRequestApi.ErasureRunResult, string>> draws seed (Decode.result erasureRunResult Decode.asString)
        RemotingDecoders.verify<Result<string, string>> draws seed (Decode.result Decode.asString Decode.asString)
        RemotingDecoders.verify<Result<ExportStatus, string>> draws seed (Decode.result exportStatus Decode.asString)
        RemotingDecoders.verify<Result<SignedExportEnvelope, string>> draws seed (Decode.result signedExportEnvelope Decode.asString)
        RemotingDecoders.verify<Result<AuditEventPage, string>> draws seed (Decode.result auditEventPage Decode.asString)
        RemotingDecoders.verify<Result<string list, string>> draws seed (Decode.result (Decode.list Decode.asString) Decode.asString)
        RemotingDecoders.verify<Result<CompositionView, string>> draws seed (Decode.result compositionView Decode.asString)
        RemotingDecoders.verify<Result<SurfacesView, string>> draws seed (Decode.result surfacesView Decode.asString)
        RemotingDecoders.verify<Result<RulesView, string>> draws seed (Decode.result rulesView Decode.asString)
        RemotingDecoders.verify<Result<DisclosureView, string>> draws seed (Decode.result disclosureView Decode.asString)
        RemotingDecoders.verify<Result<ProvenanceView, string>> draws seed (Decode.result provenanceView Decode.asString)
        RemotingDecoders.verify<ModuleConfigEntry list> draws seed (Decode.list moduleConfigEntry)
        RemotingDecoders.verify<Result<ModuleConfigView, string>> draws seed (Decode.result moduleConfigView Decode.asString)
        RemotingDecoders.verify<Map<string, Map<string, string>>> draws seed (Decode.asMap Decode.asString (Decode.asMap Decode.asString Decode.asString))
        RemotingDecoders.verify<DataSourceConfig list> draws seed (Decode.list dataSourceConfig)
        RemotingDecoders.verify<DataSourceConfig option> draws seed (Decode.option dataSourceConfig)
        RemotingDecoders.verify<Result<Guid, string>> draws seed (Decode.result Decode.asGuid Decode.asString)
        RemotingDecoders.verify<IngestionRun list> draws seed (Decode.list ingestionRun)
        RemotingDecoders.verify<TokenStatus option> draws seed (Decode.option tokenStatus)
        RemotingDecoders.verify<MigrationDataTypeInfo list> draws seed (Decode.list migrationDataTypeInfo)
        RemotingDecoders.verify<MigrationStatus list> draws seed (Decode.list migrationStatus)
        RemotingDecoders.verify<Result<MigrationStatus, string>> draws seed (Decode.result migrationStatus Decode.asString)
        RemotingDecoders.verify<Result<DeploymentReadiness.DeploymentReadinessReport, string>> draws seed (Decode.result deploymentReadinessReport Decode.asString)
        RemotingDecoders.verify<Result<DeploymentVerification.DeploymentVerificationReport, string>> draws seed (Decode.result deploymentVerificationReport Decode.asString)
        RemotingDecoders.verify<Result<ExternalContact list, string>> draws seed (Decode.result (Decode.list externalContact) Decode.asString)
        RemotingDecoders.verify<Result<ExternalContact, string>> draws seed (Decode.result externalContact Decode.asString)
        RemotingDecoders.verify<Map<string, FlagValue>> draws seed (Decode.asMap Decode.asString flagValue)
        RemotingDecoders.verify<FeatureFlag list> draws seed (Decode.list featureFlag)
        RemotingDecoders.verify<Result<Map<string, FlagValue>, string>> draws seed (Decode.result (Decode.asMap Decode.asString flagValue) Decode.asString)
        RemotingDecoders.verify<Result<HealthSnapshot, string>> draws seed (Decode.result healthSnapshot Decode.asString)
        RemotingDecoders.verify<Result<PreflightSnapshotView, string>> draws seed (Decode.result preflightSnapshotView Decode.asString)
        RemotingDecoders.verify<Result<JobSchedulerTelemetryView, string>> draws seed (Decode.result jobSchedulerTelemetryView Decode.asString)
        RemotingDecoders.verify<Result<DegradedCapability list, string>> draws seed (Decode.result (Decode.list degradedCapability) Decode.asString)
        RemotingDecoders.verify<Result<AIDenialRollup option, string>> draws seed (Decode.result (Decode.option aiDenialRollup) Decode.asString)
        RemotingDecoders.verify<(Result<ModuleQueryResponse, ModuleQueryError>) option> draws seed (Decode.option (Decode.result moduleQueryResponse moduleQueryError))
        RemotingDecoders.verify<ModuleVisibilityResolution option> draws seed (Decode.option moduleVisibilityResolution)
        RemotingDecoders.verify<Result<ModuleVisibilityProfile option, string>> draws seed (Decode.result (Decode.option moduleVisibilityProfile) Decode.asString)
        RemotingDecoders.verify<string list> draws seed (Decode.list Decode.asString)
        RemotingDecoders.verify<Result<NotificationPreferenceView, string>> draws seed (Decode.result notificationPreferenceView Decode.asString)
        RemotingDecoders.verify<Result<LifecycleSummary, string>> draws seed (Decode.result lifecycleSummary Decode.asString)
        RemotingDecoders.verify<Result<LifecycleSummary option, string>> draws seed (Decode.result (Decode.option lifecycleSummary) Decode.asString)
        RemotingDecoders.verify<Result<LifecycleJobHandle, string>> draws seed (Decode.result lifecycleJobHandle Decode.asString)
        RemotingDecoders.verify<Result<LifecyclePreview, string>> draws seed (Decode.result lifecyclePreview Decode.asString)
        RemotingDecoders.verify<Result<ExportThenDeprovisionResult, string>> draws seed (Decode.result exportThenDeprovisionResult Decode.asString)
        RemotingDecoders.verify<Result<OffboardConfirmation, string>> draws seed (Decode.result offboardConfirmation Decode.asString)
        RemotingDecoders.verify<Result<ScheduledDeprovision, string>> draws seed (Decode.result scheduledDeprovision Decode.asString)
        RemotingDecoders.verify<Result<ScheduledDeprovision option, string>> draws seed (Decode.result (Decode.option scheduledDeprovision) Decode.asString)
        RemotingDecoders.verify<Result<PrincipalSummary list, string>> draws seed (Decode.result (Decode.list principalSummary) Decode.asString)
        RemotingDecoders.verify<PresencePeer list> draws seed (Decode.list presencePeer)
        RemotingDecoders.verify<LockLease option> draws seed (Decode.option lockLease)
        RemotingDecoders.verify<Result<WireProvenanceChainPage, ProvenanceQueryError>> draws seed (Decode.result wireProvenanceChainPage provenanceQueryError)
        RemotingDecoders.verify<Result<ProviderProfileView, string>> draws seed (Decode.result providerProfileView Decode.asString)
        RemotingDecoders.verify<Result<(Tuple<string, ProviderHealth>) list, string>> draws seed (Decode.result (Decode.list (Decode.tuple2 Decode.asString providerHealth)) Decode.asString)
        RemotingDecoders.verify<Result<ServiceAccount list, string>> draws seed (Decode.result (Decode.list serviceAccount) Decode.asString)
        RemotingDecoders.verify<Result<ServiceAccount, string>> draws seed (Decode.result serviceAccount Decode.asString)
        RemotingDecoders.verify<Result<ServiceAccountTokenView list, string>> draws seed (Decode.result (Decode.list serviceAccountTokenView) Decode.asString)
        RemotingDecoders.verify<Result<MintedServiceAccountTokenView, string>> draws seed (Decode.result mintedServiceAccountTokenView Decode.asString)
        RemotingDecoders.verify<Result<ServiceStatusSnapshot, string>> draws seed (Decode.result serviceStatusSnapshot Decode.asString)
        RemotingDecoders.verify<Result<SectionSummary, string>> draws seed (Decode.result sectionSummary Decode.asString)
        RemotingDecoders.verify<Result<SessionRecord list, string>> draws seed (Decode.result (Decode.list sessionRecord) Decode.asString)
        RemotingDecoders.verify<Result<TeamInviteIssueResult, string>> draws seed (Decode.result teamInviteIssueResult Decode.asString)
        RemotingDecoders.verify<Result<TeamInviteAcceptResult, string>> draws seed (Decode.result teamInviteAcceptResult Decode.asString)
        RemotingDecoders.verify<Result<TeamInviteSummary list, string>> draws seed (Decode.result (Decode.list teamInviteSummary) Decode.asString)
        RemotingDecoders.verify<Result<(Tuple<string, PendingInviteByEmail>) list, string>> draws seed (Decode.result (Decode.list (Decode.tuple2 Decode.asString pendingInviteByEmail)) Decode.asString)
        RemotingDecoders.verify<Result<TeamInviteExpiredPayload list, string>> draws seed (Decode.result (Decode.list teamInviteExpiredPayload) Decode.asString)
        RemotingDecoders.verify<Result<TeamInfo option, string>> draws seed (Decode.result (Decode.option teamInfo) Decode.asString)
        RemotingDecoders.verify<Result<UserSummary list, string>> draws seed (Decode.result (Decode.list userSummary) Decode.asString)
        RemotingDecoders.verify<Result<UserAuthoredSchema list, UserSchemaError>> draws seed (Decode.result (Decode.list userAuthoredSchema) userSchemaError)
        RemotingDecoders.verify<Result<UserAuthoredSchema, UserSchemaError>> draws seed (Decode.result userAuthoredSchema userSchemaError)
        RemotingDecoders.verify<Result<MigrationOutcome, UserSchemaError>> draws seed (Decode.result migrationOutcome userSchemaError)
        RemotingDecoders.verify<Result<unit, UserSchemaError>> draws seed (Decode.result Decode.asUnit userSchemaError)
        RemotingDecoders.verify<Result<WebhookSubscription, string>> draws seed (Decode.result webhookSubscription Decode.asString)
        RemotingDecoders.verify<Result<WebhookSubscription list, string>> draws seed (Decode.result (Decode.list webhookSubscription) Decode.asString)
        RemotingDecoders.verify<Result<WebhookTestResult, string>> draws seed (Decode.result webhookTestResult Decode.asString)
        RemotingDecoders.verify<Result<WebhookDelivery list, string>> draws seed (Decode.result (Decode.list webhookDelivery) Decode.asString)
        RemotingDecoders.verify<JobDefinition list> draws seed (Decode.list jobDefinition)
        RemotingDecoders.verify<JobDefinition option> draws seed (Decode.option jobDefinition)
        RemotingDecoders.verify<JobRun list> draws seed (Decode.list jobRun)
        RemotingDecoders.verify<Result<Guid, ScheduleError>> draws seed (Decode.result Decode.asGuid scheduleError)
        RemotingDecoders.verify<Result<ModelExecutionReceipt, ModelExecutionRefusal>> draws seed (Decode.result modelExecutionReceipt modelExecutionRefusal)
        RemotingDecoders.verify<Result<ModelExecutionOutcome, ModelExecutionRefusal>> draws seed (Decode.result modelExecutionOutcome modelExecutionRefusal)
        RemotingDecoders.verify<Result<ModelExecutionOutcomePage, ModelExecutionRefusal>> draws seed (Decode.result modelExecutionOutcomePage modelExecutionRefusal)
        RemotingDecoders.verify<Result<ModelExecutionDatasetVersion, ModelExecutionRefusal>> draws seed (Decode.result modelExecutionDatasetVersion modelExecutionRefusal)
        RemotingDecoders.verify<Result<TeamPermissions, string>> draws seed (Decode.result teamPermissions Decode.asString)
        RemotingDecoders.verify<bool> draws seed Decode.asBool
        RemotingDecoders.verify<Result<TeamInfo, string>> draws seed (Decode.result teamInfo Decode.asString)
        RemotingDecoders.verify<TeamInfo list> draws seed (Decode.list teamInfo)
        RemotingDecoders.verify<TeamMembership list> draws seed (Decode.list teamMembership)
        RemotingDecoders.verify<string option> draws seed (Decode.option Decode.asString)
        RemotingDecoders.verify<Result<TeamSummary list, string>> draws seed (Decode.result (Decode.list teamSummary) Decode.asString)
        RemotingDecoders.verify<UsageRecord list> draws seed (Decode.list usageRecord)
        RemotingDecoders.verify<UsageAggregateRow list> draws seed (Decode.list usageAggregateRow)
        RemotingDecoders.verify<byte[]> draws seed Decode.asBytes
    ]

    /// Phase 801 — `registerAll`, gated: registers every decoder above
    /// only when every one verifies, and registers NOTHING otherwise, so
    /// a disagreement can never leave the table half-adopted.
    let registerAllVerified (draws: int) (seed: int) : Result<DecoderVerification list, DecoderRefusal list> =
        let outcomes = verifyAll draws seed

        let refusals =
            outcomes
            |> List.choose (function
                | Error refusal -> Some refusal
                | Ok _ -> None)

        if List.isEmpty refusals then
            registerAll ()

            Ok(
                outcomes
                |> List.choose (function
                    | Ok verification -> Some verification
                    | Error _ -> None)
            )
        else
            Error refusals
#endif

    /// The API records this module covers, shaped exactly as
    /// `RemotingDecoderFacet.inspect` takes them. The corpus flag is
    /// DECLARED by whoever generated this file, never inferred.
    let coveredApiRecords: (string * string list * bool) list = [
        "IConversionApi", [ typeof<ColumnMappingTypes.Conversion list>.FullName; typeof<Result<unit, string>>.FullName; typeof<ColumnMappingTypes.ConversionRecord list>.FullName; typeof<Result<ColumnMappingTypes.DryRunReport, string>>.FullName ], true
        "FileManagementApi", [ typeof<Result<ProcessedDataTypes.FileUploadResponse, string>>.FullName; typeof<ProcessedDataTypes.FileListSnapshot>.FullName; typeof<Result<DataManagementTypes.DataFileUpload, string>>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<ProcessedDataTypes.ProcessedFileEntry, string>>.FullName; typeof<Result<int, string>>.FullName; typeof<ProcessedDataTypes.SessionStoreInfo>.FullName ], true
        "AccessibilityApi", [ typeof<AccessibleModulesResponse>.FullName ], true
        "BudgetAccount", [ typeof<unit>.FullName ], true
        "DataCatalogApi", [ typeof<DataManagementTypes.DataCatalogResponse>.FullName ], true
        "IDataSubjectRequestApi", [ typeof<Result<byte[], string>>.FullName; typeof<Result<DataSubjectRequestApi.ErasurePreview, string>>.FullName; typeof<Result<DataSubjectRequestApi.ErasureRunResult, string>>.FullName; typeof<Result<string, string>>.FullName; typeof<Result<ExportStatus, string>>.FullName; typeof<Result<SignedExportEnvelope, string>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "IAuditViewApi", [ typeof<Result<AuditEventPage, string>>.FullName; typeof<Result<string list, string>>.FullName; typeof<Result<byte[], string>>.FullName ], true
        "ICompositionInspectorApi", [ typeof<Result<CompositionView, string>>.FullName; typeof<Result<SurfacesView, string>>.FullName; typeof<Result<RulesView, string>>.FullName; typeof<Result<DisclosureView, string>>.FullName; typeof<Result<ProvenanceView, string>>.FullName; typeof<Result<string, string>>.FullName ], true
        "IConfigApi", [ typeof<ModuleConfigEntry list>.FullName; typeof<Result<ModuleConfigView, string>>.FullName; typeof<Map<string, Map<string, string>>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "IDataIngestionApi", [ typeof<DataSourceConfig list>.FullName; typeof<DataSourceConfig option>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<Guid, string>>.FullName; typeof<IngestionRun list>.FullName; typeof<Result<string, string>>.FullName; typeof<CredentialStatus>.FullName; typeof<TokenStatus option>.FullName ], true
        "IDataMigrationApi", [ typeof<MigrationDataTypeInfo list>.FullName; typeof<MigrationStatus list>.FullName; typeof<Result<MigrationStatus, string>>.FullName ], true
        "IDeploymentReadinessApi", [ typeof<Result<DeploymentReadiness.DeploymentReadinessReport, string>>.FullName ], true
        "IDeploymentVerificationApi", [ typeof<Result<DeploymentVerification.DeploymentVerificationReport, string>>.FullName ], true
        "IExternalContactApi", [ typeof<Result<ExternalContact list, string>>.FullName; typeof<Result<ExternalContact, string>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "IFeatureFlagApi", [ typeof<Map<string, FlagValue>>.FullName; typeof<FeatureFlag list>.FullName; typeof<Result<Map<string, FlagValue>, string>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "IHealthMonitorApi", [ typeof<Result<HealthSnapshot, string>>.FullName; typeof<Result<PreflightSnapshotView, string>>.FullName; typeof<Result<JobSchedulerTelemetryView, string>>.FullName; typeof<Result<DegradedCapability list, string>>.FullName; typeof<Result<AIDenialRollup option, string>>.FullName ], true
        "IHomeOverviewApi", [ typeof<HomeOverview>.FullName; typeof<HomePinningState>.FullName ], true
        "IModuleQueryBusApi", [ typeof<(Result<ModuleQueryResponse, ModuleQueryError>) option>.FullName ], true
        "IModuleVisibilityApi", [ typeof<ModuleVisibilityResolution option>.FullName; typeof<Result<ModuleVisibilityProfile option, string>>.FullName; typeof<string list>.FullName; typeof<Result<unit, string>>.FullName ], true
        "INotificationPreferenceApi", [ typeof<Result<NotificationPreferenceView, string>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "IPlatformTenantApi", [ typeof<Result<LifecycleSummary, string>>.FullName; typeof<Result<LifecycleSummary option, string>>.FullName; typeof<Result<LifecycleJobHandle, string>>.FullName; typeof<Result<LifecyclePreview, string>>.FullName; typeof<Result<ExportThenDeprovisionResult, string>>.FullName; typeof<Result<OffboardConfirmation, string>>.FullName; typeof<Result<ScheduledDeprovision, string>>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<ScheduledDeprovision option, string>>.FullName; typeof<Result<PrincipalSummary list, string>>.FullName ], true
        "IPresenceApi", [ typeof<PresencePeer list>.FullName; typeof<unit>.FullName; typeof<LockOutcome>.FullName; typeof<LockLease option>.FullName ], true
        "IProvenanceQueryApi", [ typeof<WireProvenanceCaps>.FullName; typeof<WireProvenanceNodeAnswer>.FullName; typeof<WireProvenanceEdgeSet>.FullName; typeof<Result<WireProvenanceChainPage, ProvenanceQueryError>>.FullName ], true
        "IProviderProfileApi", [ typeof<Result<ProviderProfileView, string>>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<(Tuple<string, ProviderHealth>) list, string>>.FullName ], true
        "IServiceAccountApi", [ typeof<Result<ServiceAccount list, string>>.FullName; typeof<Result<ServiceAccount, string>>.FullName; typeof<Result<ServiceAccountTokenView list, string>>.FullName; typeof<Result<MintedServiceAccountTokenView, string>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "IServiceStatusBoardApi", [ typeof<Result<ServiceStatusSnapshot, string>>.FullName; typeof<Result<SectionSummary, string>>.FullName ], true
        "ISessionApi", [ typeof<Result<SessionRecord list, string>>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<int, string>>.FullName ], true
        "ITeamInviteApi", [ typeof<Result<TeamInviteIssueResult, string>>.FullName; typeof<Result<TeamInviteAcceptResult, string>>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<TeamInviteSummary list, string>>.FullName; typeof<Result<(Tuple<string, PendingInviteByEmail>) list, string>>.FullName; typeof<Result<TeamInviteExpiredPayload list, string>>.FullName; typeof<Result<TeamInfo option, string>>.FullName ], true
        "IUserDirectoryApi", [ typeof<Result<UserSummary list, string>>.FullName ], true
        "IUserSchemaApi", [ typeof<Result<UserAuthoredSchema list, UserSchemaError>>.FullName; typeof<Result<UserAuthoredSchema, UserSchemaError>>.FullName; typeof<Result<MigrationOutcome, UserSchemaError>>.FullName; typeof<Result<unit, UserSchemaError>>.FullName ], true
        "IWebhookApi", [ typeof<Result<WebhookSubscription, string>>.FullName; typeof<Result<WebhookSubscription list, string>>.FullName; typeof<Result<unit, string>>.FullName; typeof<Result<WebhookTestResult, string>>.FullName; typeof<Result<WebhookDelivery list, string>>.FullName ], true
        "JobApi", [ typeof<JobDefinition list>.FullName; typeof<JobDefinition option>.FullName; typeof<JobRun list>.FullName; typeof<Result<Guid, ScheduleError>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "MaintenanceApi", [ typeof<Result<int, string>>.FullName ], true
        "ModelExecutionApi", [ typeof<Result<ModelExecutionReceipt, ModelExecutionRefusal>>.FullName; typeof<Result<ModelExecutionOutcome, ModelExecutionRefusal>>.FullName; typeof<Result<ModelExecutionOutcomePage, ModelExecutionRefusal>>.FullName; typeof<Result<ModelExecutionDatasetVersion, ModelExecutionRefusal>>.FullName ], true
        "PermissionApi", [ typeof<Result<TeamPermissions, string>>.FullName; typeof<Result<unit, string>>.FullName ], true
        "PlatformAdminApi", [ typeof<bool>.FullName; typeof<string list>.FullName; typeof<Result<unit, string>>.FullName; typeof<PlatformKnowledgeBaseMode>.FullName ], true
        "PlatformInfoApi", [ typeof<PlatformInfo>.FullName ], true
        "TeamApi", [ typeof<Result<TeamInfo, string>>.FullName; typeof<TeamInfo list>.FullName; typeof<Result<unit, string>>.FullName; typeof<TeamMembership list>.FullName; typeof<string option>.FullName; typeof<TeamCreationPolicy>.FullName; typeof<Result<TeamSummary list, string>>.FullName ], true
        "IUsageQueryApi", [ typeof<UsageRecord list>.FullName; typeof<UsageAggregateRow list>.FullName; typeof<byte[]>.FullName ], true
    ]

