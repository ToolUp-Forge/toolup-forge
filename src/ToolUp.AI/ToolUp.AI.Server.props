<Project>
  <!-- Phase 11.B follow-up cross-tier cutover (2026-05-08): server source
       files moved to <Compile> in ../ToolUp.AI.Server/ and ship as
       ToolUp.AI.Server.dll referenced via <ProjectReference> from the
       consuming server project. This file is retained as an empty marker. -->
  <ItemGroup>
    <!-- (Server source items removed Phase 11.B follow-up.) -->
  </ItemGroup>
</Project>
