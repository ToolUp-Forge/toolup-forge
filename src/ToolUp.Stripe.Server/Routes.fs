namespace ToolUp.Stripe.Server

/// Skeleton. The real surface (Routes.stripeWebhook HttpHandler +
/// Customer-Portal session opener + Checkout-session creator) lands
/// at Phase 05 / 06. v0.1.0-alpha exists only to reserve the package
/// id and project layout in the local-nuget-feed.
module Routes =
    /// Placeholder so the module is not empty. Removed when
    /// Phase 05 lands the real `stripeWebhook` HttpHandler.
    let placeholderVersion = "0.1.0-alpha"