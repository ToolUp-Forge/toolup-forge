<Project>
  <!-- Phase 11.B follow-up companion cutover (2026-05-08): source files
       moved to <Compile> in LocalEmbeddingProvider.fsproj and ship as
       LocalEmbeddingProvider.dll referenced via <ProjectReference> from
       the consuming server project. This file is retained as an empty
       marker — consuming projects no longer need to import it. -->
  <ItemGroup>
    <!-- (Source items removed Phase 11.B follow-up.) -->
  </ItemGroup>
</Project>
