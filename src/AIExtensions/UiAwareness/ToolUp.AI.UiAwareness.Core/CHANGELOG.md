# Changelog — ToolUp.AI.UiAwareness.Core

All notable changes to the `ToolUp.AI.UiAwareness.Core` package are recorded here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Versions track the coordinated `ToolUp.Sdk`
meta-release.

## [Unreleased]

- Initial release (Phase 537). Part of the opt-in UI-awareness companion: the read-only,
  client-resident `_platform.ui.inspect_active_module` AI tool, which returns the live
  `UiStateReport` of the module the user is viewing. See the companion `README.md`.
